# RapidForensic QC-Prep Implementation List

Last updated: 2026-05-11

## Current Evidence Snapshot

- Branch: `codex/rapidforensic-complete`
- Latest reviewed commit: `a06f2e9 Carry source citations through review handoff`
- Commercial readiness score: `79`
- Commercial claim allowed: `false`
- Maturity gates: implemented `120/120`, usable `120/120`, validated `0/120`, commercial-grade `0/120`
- Taxonomy audit: `48/48` targets covered, partial `0`, missing `0`, strict pass `true`, artifact type literals `146`

Interpretation: RapidForensic has broad internal feature coverage and a usable single-case scaffold, but QC should not be treated as final acceptance yet. The next work must turn broad coverage into source-verifiable, large-case-tolerant, trusted-diff-ready workflows.

## QC-Prep Goal

Before formal QC, a Windows 11 E01 single-case workflow should let an analyst:

1. Select evidence and see dependency, image, partition, and risk status.
2. Process or resume the case without losing stage evidence.
3. Search files, documents, logs, browser/AI traces, OCR text, email, messenger, and normalized artifacts.
4. Open the original source, table row, record, offset, or attachment behind each result.
5. Mark review decisions, compare evidence, and include selected items in a report.
6. Export a defensible report bundle with hashes, provenance, warnings, limitations, and validation slots.

## Phase 1: Single-Case E01 Workflow

1. Wire E01 selection to one end-to-end command and GUI flow: preflight, partition selection, extraction, run, search, review, report. Current implementation emits `qc-prep-e01-end-to-end-handoff-v1` in E01 evidence preflight and shows an `e01-end-to-end-handoff` GUI card with Start/Search/Review/Report handoff actions; remaining QC still requires a real Windows 11 E01 smoke run and trusted extraction logs.
2. Add GUI partition browser with partition number, start sector, size, filesystem guess, recommendation, and manual override. Current implementation emits `e01-partition-browser-v1` in the E01 workflow and renders an `e01-partition-browser` GUI table with manual start-sector handoff into the run form; remaining QC still requires a real E01/mmls partition transcript and trusted-tool comparison evidence.
3. Add VSC discovery and extraction handoff for E01/RAW workflows, not only already-mounted folder comparison. Current implementation emits `vsc-image-workflow-handoff-v1` from E01/RAW extraction and run manifests, adds a GUI `vsc-workflow-handoff` card, and links `vsc-discover`, `vsc-compare`, `vsc-extract`, and Case DB import commands; remaining QC still requires trusted VSC mount/export transcripts and a known-answer deleted-file VSC corpus.
4. Surface stage checkpoint, resume, cancel, retry, and failure classification in GUI and JSON outputs. Current implementation emits `image-stage-control-contract-v1` from E01/RAW preflight, extraction, run source records, and workflow manifests, and renders an `image-stage-control-contract` GUI card with checkpoint/resume state, cancel/retry API routes, failed stages, and failure category; remaining QC still requires long-running cancel-under-load evidence and trusted checkpoint/resume transition diffs.
5. Add fresh Windows/macOS GUI launch smoke evidence for the single-case workflow. Current implementation extends `single-case-workbench-smoke-v1` with `fresh-gui-launch-smoke-evidence-v1`, Windows/macOS evidence slots, manifest hash, and smoke-script contract output; smoke scripts now save `workbench-smoke-contract.json` and the summary checker requires it. Remaining QC still requires running those scripts on fresh Windows 11 and macOS hosts and attaching screenshots/logs.

## Phase 2: Source Verification And Viewer Locators

6. Connect search-result rows to source viewer actions in the GUI. Current implementation adds `search-result-source-viewer-actions-v1` per search hit, a summary action contract, GUI action strips with `View / review`, `Open source`, `Search inside`, `Pin compare`, and `Mark`, plus tests that verify the API/DOM contract; remaining QC still requires browser E2E evidence that these controls open the viewer, save review state, and return to the same virtualized row under a large result set.
7. Add EVTX record locator with file path, record ID, channel, provider, event ID, record offset where available, source hash, and validation warning. Current implementation emits `eventlog-record-source-viewer-locator-v1` / `eventlog_record_locator_profile` on event rows, preserving source path, SHA256, record ID, channel, provider, event ID, native EVTX offset when available, report preconditions, and validation warnings; remaining QC still requires trusted EvtxECmd/Hayabusa record-level locator diffs and native deleted/corrupt offset fixture coverage.
8. Add Registry key/value/cell locator with hive path, key path, value name, cell offset, allocation/deleted status, source hash, and transaction replay status. Current implementation emits `registry-record-source-viewer-locator-v1` on .reg rows, native hive cells, key-tree nodes, and deleted key/value recovery candidates, preserving hive/key/value/cell coordinates, source SHA256, allocation/deleted state, transaction replay status, and report preconditions; remaining QC still requires RECmd/Registry Explorer row-level diffs and LOG1/LOG2 replay validation on real hives.
9. Add MFT/USN record locator with FRN, parent FRN, sequence, USN, reason flags, path confidence, and source citation. Current implementation emits `ntfs-record-source-viewer-locator-v1` / `ntfs_record_locator_profile` on native MFT and USN rows, preserving source path, SHA256, record offset/cursor, FRN/parent FRN, sequence, USN, reason flags, path confidence, viewer actions, and citation wording; remaining QC still requires MFTECmd/analyzeMFT/UsnJrnl2Csv record-level locator diffs and large-volume FRN replay validation.
10. Add email attachment locator with message ID, attachment index/name, hash, bounded preview, and export warning. Current implementation emits `email-attachment-source-viewer-locator-v1` on parsed EML/EMLX/Maildir/MBOX attachment metadata and a per-message `email_attachment_locator_profile`, preserving source path/SHA256, message index/ID, attachment index/name/content type/size/hash, bounded preview hash/hex, package endpoint, and export/legal validation warnings; remaining QC still requires trusted mailbox attachment diffs, native PST/OST/MSG attachment extraction, deleted attachment recovery, and broad mailbox corpus validation.
11. Connect SQLite row locator to the GUI source viewer and review note flow. Current implementation emits `sqlite-row-source-viewer-locator-v1` and `sqlite-row-review-note-citation-v1` on SQLite preview rows, source table pages, and current-file SQLite search hits; the GUI review-note action now appends structured row citations plus locator hashes so an analyst can preserve table/rowid/PK/query context from search to review. Remaining QC still requires browser E2E evidence for opening a searched SQLite row in the source table viewer, trusted sqlite3/DB Browser row-level diffs, and deleted-row/WAL recovery validation.
12. Connect hex range citations to report candidates and comparison pins. Current implementation emits `hex-range-review-link-profile-v1` from `/source-hex-range`, including a review-note citation, compare pin payload, report-candidate payload, range SHA256, source locator, and proof manifest hash; remaining QC still requires browser E2E pin/report-promotion evidence and trusted offset-manifest diffs.
13. Connect image, video, audio, OCR, and translation sidecar viewers to the evidence tray. Current implementation emits `evidence-tray-sidecar-viewer-contract-v1` from source previews, with image gallery, OCR queue, OCR/translation, media cue, and email attachment sidecar links where applicable; remaining QC still requires browser E2E selection, sidecar-rendering diffs, and large-folder gallery/transcript evidence.
14. Let current-file search hits append structured citations to review notes and report drafts. Current implementation emits `current-file-search-report-draft-profile-v1` in file-search citation profiles and report generation now preserves `Structured citation:` and `Source locator:` lines from review notes; remaining QC still requires end-to-end browser proof from search hit -> review note -> report draft and source-hash verification evidence.

## Phase 3: Windows Core Parser Depth

15. Complete EVTX BinXML grammar coverage beyond current best-effort structures. Current implementation has native mmap-bounded EVTX scanning, common System/EventData field extraction, template substitution fixtures, BinXML parse profiles, record provenance, and report-grade blockers; remaining QC still requires full BinXML token grammar, broad provider fixtures, and EvtxECmd/Hayabusa record-level diffs.
16. Add provider/resource message rendering matrix and unresolved-template warnings. Current implementation supports built-in templates, curated provider catalog rendering, Windows event manifest catalog rendering, rendering provenance, available-field summaries, and unresolved validation warnings; remaining QC still requires provider DLL/resource-table rendering on Windows hosts and provider/version matrix fixtures.
17. Build corrupt, slack, and deleted EVTX recovery fixtures with offset/confidence reporting. Current implementation has slack/deleted and corrupt/truncated EVTX fixture tests, allocation status, record offsets, chunk context, confidence bands, recovery evidence, and reportability decisions; remaining QC still requires broad corrupt/deleted corpus validation and independent tool confirmation.
18. Implement Registry LOG1/LOG2 transaction replay or prove equivalent output through trusted diffs. Current implementation detects LOG1/LOG2 transaction inputs, parses recognized log headers, records replay readiness, exposes `registry-transaction-replay-profile-v1`, and discloses present-not-replayed blockers in locators/manifests; remaining QC still requires actual transaction replay or trusted RECmd/Registry Explorer diff evidence proving equivalent final hive state.
19. Strengthen Registry deleted key/value recovery with allocator-state checks and false-positive fixtures. Current implementation emits deleted/free key/value recovery candidates with positive-size free-cell checks, allocator context, neighbor context, recovery validation profiles, reportability decisions, and false-positive blocker disclosure; remaining QC still requires known-answer deleted-cell corpora and second-parser validation at cell offset.
20. Deepen NTUSER/UsrClass user activity parsing: RunMRU, RecentDocs, TypedURLs, UserAssist, ComDlg32, MountPoints, ShellBags linkage. Current implementation normalizes exported and native-hive string-pivot user activity rows across RunMRU, RecentDocs, TypedURLs, UserAssist ROT13 hints, ComDlg32, MountPoints2, ShellBags, and network shares with source locators and review profiles; remaining QC still requires full binary value decoding, shell item decoding, transaction replay, and ShellBagsExplorer/SBECmd/RECmd diffs.
21. Deepen SAM/SECURITY/SYSTEM parsing for F/V records, aliases, memberships, LSA metadata, privileges, services, ControlSets, mounted devices. Current implementation emits `qc_prep_item_number: 21` inside `sam-security-system-deep-parser-v1`, covers SAM RID/name and conservative F/V metadata, group/alias hints, privilege inheritance, redacted LSA secret metadata, services, ControlSets, and mounted devices, and exposes normalized security context manifests; remaining QC still requires OS-build-complete SAM F/V layouts, native alias/member binary decoding, LOG1/LOG2 replay, domain SID context, and RECmd/Registry Explorer trusted diffs.
22. Deepen Amcache schema/version decoding and timestamp semantics. Current implementation emits `qc_prep_item_number: 22` inside `amcache-schema-v1`, covers reg-export mapping, native Amcache.hve bounded path/hash string pivots, timestamp-source labeling, citation manifests, reportability decisions, and AmcacheParser-style diff helpers; remaining QC still requires native schema row decoding by Windows build, timestamp-semantics known-answer fixtures, and attached trusted diffs on real cases.
23. Deepen ShimCache/AppCompatCache OS-version binary layouts and "not direct execution proof" UX. Current implementation emits `qc_prep_item_number: 23` inside `shimcache-caveat-v1`, preserves the not-execution-proof warning, maps reg exports, scans native SYSTEM hive bounded path clusters, preserves cache order/source offsets, and tests AppCompatCacheParser-style diff and order-mismatch blocking; remaining QC still requires OS-build-specific binary layouts, malformed-layout fixture corpus, and independent artifact corroboration before execution claims.
24. Deepen BAM/DAM binary value decoding and SID/path/timestamp correlation. Current implementation emits `qc_prep_item_number: 24` inside `bam-dam-decode-v1`, maps reg exports, scans native SYSTEM hive SID/path/timestamp clusters, normalizes device path/user SID/FILETIME source/ControlSet/source offset, and tests RECmd-style trusted diff plus SID-mismatch blocking; remaining QC still requires native BAM/DAM binary value decoding by Windows build, rotated/disabled BAM edge fixtures, and corroboration with Prefetch/SRUM/UserAssist/Event Logs.
25. Deepen SRUM native ESE table/page decoding and counter semantics. Current implementation emits `qc_prep_item_number: 25` inside `srum-ese-validation-v1`, covers SrumECmd-style source exports, native SRUDB.dat ESE header probing, bounded table/string/row-candidate clustering, counter/timestamp labeling, citation manifests, and SrumECmd-style counter diff tests; remaining QC still requires native ESE catalog/page/tagged-column decoding, Windows-build SRUM schema maps, row-level trusted diffs, and large SRUDB cursor stress evidence.
26. Deepen Windows.edb ESE table, property, content, and deleted-state decoding. Current implementation emits `qc_prep_item_number: 26` in `windows-edb-report-citation-manifest-v1`, imports source-tool exports, probes native Windows.edb ESE headers/page maps, correlates page-local path/URL/content/table markers, discloses deleted/index-state candidates, and tests WinSearchDBAnalyzer/libesedb-style diffs; remaining QC still requires native ESE catalog/table/property decoding, row-level timestamp/content/deleted-state semantics, long-value/space-tree handling, and trusted diffs on case evidence.
27. Complete MFT attribute-list, runlist, resident/nonresident data, parent path reconstruction, and deleted-state validation. Current implementation emits `qc_prep_item_number: 27` in `mft-full-parser-readiness-v1`, validates FILE record headers/USA, decodes STANDARD_INFORMATION and FILE_NAME, hashes resident data, previews nonresident runlists, builds bounded parent path candidates, and tests MFTECmd/analyzeMFT-style diffs; remaining QC still requires ATTRIBUTE_LIST extension resolution, full nonresident runlist validation, full-volume FRN parent reconstruction, and deleted-state/path/timestamp trusted diffs.
28. Complete USN v2/v3/v4 replay, FRN path cache, rename/delete reconstruction, and large-journal cursor handling. Current implementation emits `qc_prep_item_number: 28` in `usn-journal-replay-readiness-v1`, decodes native v2/v3 records and v4 extent previews, preserves reason/source/file-attribute flags, cursor pagination, bounded MFT path correlation, rename/delete previews, and tests UsnJrnl2Csv/MFTECmd row/state diff helpers; remaining QC still requires complete journal replay with full FRN path cache, known-answer lifecycle transition validation, and large-journal cursor stress evidence.
29. Deepen JumpList DestList, CFB/OLE stream, AppID mapping, deleted entry recovery, and embedded LNK linkage. Current implementation emits `qc_prep_item_number: 29` in `jumplist-destlist-depth-manifest-v1`, inventories CFB/OLE streams, parses DestList header/entry candidates, extracts embedded LNK destinations, records AppID hash provenance, warns on deleted/unlinked entries, and tests JLECmd/LECmd-style diffs; remaining QC still requires OS-version DestList field validation, AppID mapping database provenance, deleted/unlinked entry known-answer recovery, and trusted embedded-LNK/DestList diffs.
30. Deepen ShellBags binary shell item decoding, BagMRU/Bags relationship, transaction logs, and deleted/slack checks. Current implementation emits `qc_prep_item_number: 30` in `shellbag-depth-manifest-v1`, maps reg-export ShellBags, scans native NTUSER/UsrClass key-tree/string pivots, extracts BagMRU/Bags bag/node candidates, records cell/hbin offsets, allocation state, timestamp and transaction-log status, and tests ShellBagsExplorer/SBECmd-style diffs; remaining QC still requires binary shell-item decoding, BagMRU/Bags relationship trusted diffs, LOG1/LOG2 replay, and deleted/slack known-answer validation.
31. Deepen Prefetch version 17/23/26/30/31 support, compressed PF handling, volume/file metrics, and trace-chain evidence. Current implementation emits `qc_prep_item_number: 31` in `prefetch-execution-depth-manifest-v1`, preserves version-specific common-header layout metadata, compressed PF detection status, bounded referenced path/volume/file-reference candidates, source hashes, validation IDs, and reportability blockers; remaining QC still requires full native file-metrics array decoding, authoritative volume table parsing, trace-chain/directory section validation, decompression validation, and PECmd/known-answer cross-version diffs.
32. Deepen LNK ShellLinkHeader, LinkInfo, StringData, ExtraData, TrackerDataBlock, and target/source citation. Current implementation emits `qc_prep_item_number: 32` in `lnk-metadata-depth-manifest-v1`, preserves ShellLinkHeader flags/attributes/timestamps, LinkInfo and StringData target context, ExtraData block inventory, TrackerDataBlock candidates, source hashes, identity hashes, and reportability blockers; remaining QC still requires full Shell Item semantic decoding, property-store schema validation, OS-version LinkInfo/network-provider semantics, and LECmd/known-answer trusted diffs.

## Phase 4: Web, AI, Messenger, Mail, And Cloud Evidence

33. Reconstruct browser cache objects with request/response metadata and validation warnings. Current implementation emits `qc_prep_item_numbers: [33, 34]` in `browser-storage-depth-manifest-v1`, inventories cache stores by browser/user/profile, records source locators, file/hash samples, truncation controls, privacy warnings, and reportability blockers; remaining QC still requires full Chromium/Firefox cache entry decoding, request/response header/body reconstruction, deleted cache recovery, and trusted Hindsight/Velociraptor diffs.
34. Decode browser session, local storage, extension, sync, and IndexedDB stores with profile attribution. Current implementation inventories session restore, Session Storage, Local Storage/LevelDB, IndexedDB, Extensions, Extension State, Sync Data, Web Data, cookies, and credential stores with browser/profile/user attribution, storage counts, sensitive flags, and citation manifests; remaining QC still requires browser-version session schema decoding, extension-specific schema validation, sync account/scope semantics, deleted session recovery, and known-answer fixture diffs.
35. Keep cookies/passwords/tokens behind lawful authority gates, redaction, and audit logs. Current implementation emits `qc_prep_item_number: 35` in `browser-secret-authority-v1`, `browser-secret-authority-manifest-v1`, secret handling uplift, and reportability decisions; raw secret values are not decrypted or serialized, controlled reveal is disabled by default, source locators require authority, and audit/scope blockers are explicit. Remaining QC still requires lawful authority record attachment, controlled reveal audit events, DPAPI/keychain known-answer validation, and trusted secret authority diffs.
36. Parse AI service exports and browser traces for ChatGPT, Claude, Gemini, Perplexity, Copilot-style Q/A pairing. Current implementation emits `qc_prep_item_number: 36` in AI transcript analyst profiles, candidate manifests, schema validation manifests, and commercial uplift evidence; it detects major AI service domains, extracts browser-storage candidate Q/A rows, pairs prompts and answers with confidence/orphan tracking, preserves source offsets/hashes, and exposes reportability blockers. Remaining QC still requires provider-specific service export schema validators, deleted-fragment recovery fixtures, and trusted ChatGPT/Claude/Gemini/Perplexity/Copilot export diffs.
37. Formalize PC KakaoTalk legacy and post-patch schema/version matrix with fixtures and Windows packaging notes. Current implementation emits `qc_prep_item_number: 37` in `kakaotalk-parser-manifest-v1`, preserves authorized export rows, database inventories, legacy/post-BigBang strategy status, message hashes, attachment metadata, source row citations, and chat-viewer controls; remaining QC still requires post-BigBang known-answer corpus, trusted KakaoTalk export/native DB diffs, schema-version parser maps, attachment-byte validation, and deleted-record validation.
38. Add WhatsApp export/native parser with message, contacts, calls, media, deleted-row limitations, and lawful key workflow. Current implementation emits `qc_prep_item_number: 38` in `whatsapp-parser-manifest-v1`, preserves export rows, msgstore/wa.db inventory, JID attribution, media metadata, crypt-key authority status, source citations, and review controls; remaining QC still requires lawful crypt key workflow evidence, trusted WhatsApp export/native DB diffs, msgstore/wa.db schema known answers, deleted-row validation, media locality checks, and timezone/read-state semantics validation.
39. Add Telegram export/native parser with account, media, cache, and encrypted-store warning. Current implementation emits `qc_prep_item_number: 39` in `telegram-parser-manifest-v1`, preserves export/cache rows, account/dialog attribution, media-cache metadata, edited/deleted/secret hints, source citations, and encrypted local-store warnings; remaining QC still requires local-store decryption validation, trusted Telegram export/native DB diffs, account/dialog peer known answers, secret-chat deleted semantics fixtures, and cache media locality validation.
40. Add Signal parser with SQLCipher/key handling separated into secure lawful workflow. Current implementation emits `qc_prep_item_number: 40` in `signal-parser-manifest-v1`, preserves export rows, SQLCipher DB inventory, thread/recipient attribution, attachment metadata, key authority status, source citations, and review controls; remaining QC still requires lawful SQLCipher key workflow evidence, trusted Signal export/native DB diffs, recipient/thread schema known answers, attachment locality validation, and deleted/disappearing-message fixtures.
41. Add LINE, Discord, Instagram, WeChat service-specific export/native schema mappers. Current implementation emits `qc_prep_item_number=41` on extended messenger manifests and uplift/reportability profiles, normalizes service/thread/actor/media/reaction/read/edit/deleted pivots, and keeps known-answer schema matrix, trusted service diff, attachment-byte validation, and ephemeral/deleted semantics as explicit blockers.
42. Add PST/OST mailbox parsing depth for folders, messages, attachments, deleted items, headers, and threading. Current implementation emits `qc_prep_item_number=42` on email mailbox manifests and uplift/reportability profiles, preserves bounded MAPI inventory/citation/attachment locator evidence, and keeps native MAPI decode, folder flags, deleted/recoverable items, corrupt-store recovery, threading/dedup, attachment-byte validation, and mailbox corpus diff as blockers.
43. Add Google Takeout product matrix for Gmail, Drive, Photos, Activity, Location, account, and device attribution. Current implementation emits `qc_prep_item_number=43` on Google parser manifests and cloud uplift/reportability profiles, normalizes product row pivots and source locators, and keeps selected-products manifest, original archive hash, sidecar/timezone validation, provider diff, and deleted/retention/sharing scope as blockers.
44. Add iCloud export parser for Photos, albums, shares, devices, EXIF, and account context. Current implementation emits `qc_prep_item_number=44` on iCloud parser manifests and cloud uplift/reportability profiles, normalizes account/file/photo/mail pivots and ADP/shared-album/container metadata, and keeps Apple export scope, original archive hash, ADP/shared-album validation, sidecar/EXIF merge, and provider diff as blockers.
45. Add M365/Teams/OneDrive/eDiscovery parser for permissions, reactions, attachments, SharePoint, and audit exports. Current implementation emits `qc_prep_item_number=45` on M365 parser manifests and cloud uplift/reportability profiles, normalizes Teams/OneDrive/SharePoint/eDiscovery/audit pivots, and keeps Purview scope/package hash, Graph API paging/scope, Teams compliance reconciliation, SharePoint permission graph, retention/deleted/version history, and provider diff as blockers.
46. Deepen iOS backup parser for Manifest.db, domains, app DB mapping, SMS, media, and encrypted-backup lawful key workflow. Current implementation emits `qc_prep_item_number=46` on iOS backup manifests, uplift profiles, and reportability decisions, preserves Manifest.db domain/fileID/logical path rows, Info/Status backup scope, SMS/media/app DB candidates, source locators, and lawful encrypted-backup/keychain redaction boundaries; remaining QC still requires encrypted-backup unlock evidence, protected-data class validation, application DB payload validation, trusted iOS backup diffs, and broad known-answer corpus.
47. Deepen Android artifact parser for SMS, call log, contacts, browser, media, app DBs, packages, signatures, and permissions. Current implementation emits `qc_prep_item_number=47` on Android parser manifests, uplift profiles, and reportability decisions, preserves APK package/permission/component/DEX/native/signing inventory plus app-data SQLite table/artifact-family matrices and source locators; remaining QC still requires binary AndroidManifest decode, signature-chain validation, native backup payload decoding, app-specific schemas, deleted/encrypted-store validation, trusted Android tool diffs, and known-answer corpus.

## Phase 5: Search And Large-Case Performance

48. Add a SearchBackend abstraction so the UI and CLI can target different indexing engines consistently. Current implementation adds `search-backend-contract-v1` to unified search output with selected backend, candidate backend list, UI/CLI query contract, source locator requirements, cursor/resume/truncation expectations, and a stable contract hash.
49. Keep SQLite FTS as the default local backend with explicit limits and query-plan metadata. Current implementation selects `sqlite-fts-local` by default, emits `sqlite-fts-backend-plan-v1` with effective interactive limit, cursor requirement, searched/total row disclosure, query-plan metadata requirement, and explicit large-case blockers.
50. Evaluate a local Lucene/Tantivy-style backend for million-row text/artifact search. Current implementation emits `local-inverted-index-candidate-evaluation-v1` without adding a dependency, records million-row target, parity/rebuild/corruption/benchmark requirements, and blocks enablement until packaging and SQLite parity evidence exists.
51. Add optional Elasticsearch/OpenSearch adapter for lab/server deployments without making it mandatory. Current implementation emits `external-search-adapter-contract-v1` inside the search backend contract with explicit opt-in, cluster health, TLS/auth capture, source-locator, point-in-time/search-after cursor, SQLite fallback, and evidence-text export guard requirements; remaining QC still requires a real Elasticsearch/OpenSearch integration test, index-template diff, fallback validation, and privacy policy approval.
52. Normalize index schema across documents, file metadata, EVTX, Registry, OCR, email, messenger, browser, AI, and timeline rows. Current implementation emits `normalized-index-schema-contract-v1` with required common fields, family-specific locator/parser/hash/timezone requirements, schema evolution rules, and a stable contract hash; remaining QC still requires fixture schema diffs across every artifact family and source-locator completeness benchmarks.
53. Add 100k, 1M, and 10M synthetic benchmark generators with reproducible manifests. Current implementation emits `synthetic-benchmark-generator-manifest-v1` with deterministic seed, target row counts, artifact mix, expected keyword/source-locator count requirements, and manifest hash; remaining QC still requires executing and attaching 100k/1M/10M runtime evidence plus known-answer count diffs.
54. Make cursor pagination uniform across files, docs, artifacts, search, timeline, report candidates, and review queues. Current implementation emits `uniform-cursor-pagination-contract-v1` with per-collection cursor/resume/stable-sort/count/truncation/source-locator requirements and an opaque cursor shape; remaining QC still requires endpoint-level cursor tests, resume-after-restart tests, and filter-mutation safety tests.
55. Enforce true UI virtualization and persisted viewport for massive tables. Current implementation emits `ui-virtualization-contract-v1` for file/artifact/search/timeline/review/report views with DOM windowing, overscan, keyboard focus, selection survival, viewport restore, deep-link, and persistence requirements; remaining QC still requires browser E2E evidence on 100k-row tables, keyboard triage focus retention, and restore-after-navigation tests.
56. Replace any silent SQLite row cap in current-file search with full cursor scan, truncation disclosure, and resume state. Current implementation removes the default fixed SQLite row cap from `source-search`, emits `source-search-full-cursor-scan-contract-v1`, reports scanned table/row counts, distinguishes result-limit truncation from row-scan truncation, and emits resume state when the interactive result limit is reached; remaining QC still requires API resume-token E2E, trusted locator diffs, and large SQLite current-file search benchmarks.
57. Persist hash cache across runs with size/mtime/inode/content-hash invalidation. Current implementation emits `hash-cache-persistence-contract-v1`, links the existing `hash-cache-persistence-manifest-v1` snapshot/export path, and fixes required invalidation fields and path-disclosure policy; remaining QC still requires automatic on-disk load/save, large-case hit-ratio benchmarks, and trusted hash-cache manifest diffs.
58. Add duplicate grouping for exact hashes, fuzzy text, and perceptual image/video candidates. Current implementation emits `duplicate-grouping-contract-v1` with exact content hash, fuzzy text shingle, perceptual image hash, and perceptual video keyframe hash tracks plus representative/member/source-locator/collapse-state controls; remaining QC still requires fuzzy-text corpus, perceptual media corpus, and trusted duplicate group manifest diffs.
59. Move risky/heavy parsers behind subprocess isolation with crash quarantine. Current implementation emits `parser-isolation-contract-v1`, maps risky parser families to required subprocess, bounded stdout/stderr, crash report, quarantine, and continue-after-crash behaviors, and links existing parser-crash ledger manifests; remaining QC still requires enabling subprocess isolation for all risky parsers, hostile crash corpus, and quarantine cleanup E2E.
60. Add hard memory caps where OS support exists and clear warnings where only cooperative caps exist. Current implementation emits `memory-cap-contract-v1` with platform capability disclosure, requested cap bytes, stage-boundary RSS checks, over-cap action recording, and cooperative-warning requirements linked to existing memory-cap manifests; remaining QC still requires OS-level hard-limit validation, per-parser RSS tests, and large-evidence memory regression evidence.

## Phase 6: Review, Reporting, And Evidence Packages

61. Harden evidence tray state for selected, pinned, compared, and report-candidate evidence. Current implementation emits `evidence-tray-state-contract-v1` from review marks and Case DB report export, preserving selected/report-candidate counts, source locator requirements, explicit compare slot requirements, and report-candidate state rules; remaining QC still requires browser E2E tray persistence, large-case virtualized tray tests, and multi-user conflict tests.
62. Stabilize relevant, needs-review, excluded, include-in-report, tags, notes, assignee, priority, and verification status. Current implementation emits `review-state-contract-v1`, records observed and required review/verification statuses, confirms tags/priority/report-inclusion normalization rules, and attaches the contract to individual review marks plus exports; remaining QC still requires trusted review-state audit diffs, role-based queue conflict tests, and browser E2E.
63. Persist A/B/C compare notes and connect compared snippets to report candidates. Current implementation emits `compare-notes-contract-v1` with A/B/C slot requirements, snippet hash, note, reviewer, source locator, and report-candidate link requirements; remaining QC still requires a persistent compare-note table, A/B/C browser E2E, and trusted compare-note export diffs.
64. Strengthen report citation manager with source path, source hash, parser version, offset/row/record locator, confidence, and limitation text. Current implementation emits `report-citation-manager-contract-v1` alongside existing report citation manager output, counting citation rows, source records, source hash/parser version coverage, and source viewer locators; remaining QC still requires source-hash completeness validation, parser-version completeness validation, and trusted citation-index diffs.
65. Strengthen selected evidence version history with immutable local history hashes. Current implementation emits `selected-evidence-history-contract-v1` alongside existing evidence selection history, preserving history row counts, row hash counts, include-in-report changes, append-only/hash-chain/history viewer requirements, and export manifest requirements; remaining QC still requires signed multi-user history, trusted evidence-history diffs, and DB trigger enforcement review.
66. Automate court exhibit bundle generation with selected evidence, report outputs, manifest, hashes, provenance, and signing slot. Current implementation emits `court-exhibit-bundle-contract-v1` inside Case DB report export, linking exhibit count, exhibit manifest hash, package hash, selected evidence, report outputs, source provenance, and external signing slot requirements; remaining QC still requires external signature/notarization, copied-source-file bundle validation, and independent court exhibit review.
67. Strengthen chain-of-custody workflow and acquisition metadata capture. Current implementation emits `custody-acquisition-contract-v1`, linking existing custody manifests, custody chain hashes, evidence source/custody event counts, acquisition metadata missing-field counts, and required operator/source/write-blocker/tool/hash/handoff fields; remaining QC still requires signed custody handoff, write-blocker logs, and trusted custody manifest diffs.
68. Strengthen audit hash chain and tamper-evident bundle export. Current implementation emits `tamper-evident-audit-contract-v1`, linking audit event counts, audit manifest hash, hash chain head, append-only/export-time hash chain requirements, bundle manifest hash, and external signature slot; remaining QC still requires external signature attestation, trusted audit hash-chain diffs, and tamper-evident bundle review.
69. Generate per-run validation package with commands, tool versions, source hashes, output hashes, parser versions, diffs, warnings, limitations, and reviewer status. Current implementation emits `run-validation-package-contract-v1`, tying report generation manifest/hash bundle outputs to required validation sections and selected item warnings; remaining QC still requires operator command transcripts, trusted validation package manifest diffs, and independent reviewer signoff.
70. Add trusted-tool import wizard for EvtxECmd, Hayabusa, RECmd, MFTECmd, JLECmd, PECmd, ShellBagsExplorer, SrumECmd, and libesedb outputs. Current implementation emits `trusted-tool-import-wizard-contract-v1`, listing supported tool families, required wizard steps, normalized output fields, and diff-manifest expectations; remaining QC still requires GUI import wizard E2E, trusted-tool output schema fixtures, and mismatch dashboard review.
71. Add mismatch dashboard for trusted diffs with missing rows, extra rows, field mismatches, truncation, and severity.
72. Add false-positive/false-negative recording UI and JSON export.
73. Calibrate parser confidence scoring and reportability state per artifact family.
74. Add limitation/legal warning copy per artifact type with report wording guardrails.
75. Generate a QC checklist automatically from the run, validation package, and remaining blockers.

## Phase 7: Validation Corpus And QC Execution

76. Connect NIST CFReDS/CFTT and other public forensic corpora where licensing permits.
77. Add EvtxECmd and Hayabusa diff runners for EVTX validation.
78. Add RECmd and Registry Explorer diff runners for Registry validation.
79. Add MFTECmd, analyzeMFT, and UsnJrnl2Csv diff runners for NTFS validation.
80. Add SrumECmd, libesedb, and Windows Search export diff runners for ESE validation.
81. Add PECmd, JLECmd, and ShellBagsExplorer diff runners for execution and user activity artifacts.
82. Build or attach a real Windows 11 E01 known-answer case manifest.
83. Build corrupt, deleted, slack, encrypted, and malformed fixture corpora.
84. Build large-case performance corpus and browser trace evidence.
85. Generate final QC report from validation results, mismatch dashboard, performance runs, reviewer signoff, and remaining blocker ledger.

## Recommended Execution Order

1. Finish Phase 1 and Phase 2 first so the analyst workflow is usable end-to-end.
2. Work Phase 3 in small trusted-diff-ready batches: EVTX, Registry, MFT/USN, ESE, then execution artifacts.
3. Add Phase 5 search/large-case work before testing large real cases.
4. Complete Phase 6 reporting and validation packages before formal QC.
5. Run Phase 7 with real corpora and trusted tool outputs.

## Non-Negotiable QC Rule

Do not treat taxonomy coverage, smoke fixtures, or internal manifests as final QC. A feature becomes QC-passable only when it has source-viewer proof, source hashes, row/record/offset locator, tests, limitation text, and either known-answer validation or trusted-tool diff evidence.
