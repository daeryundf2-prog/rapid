# rapidtriage output schemas

This document pins the current JSON contract for `rapidtriage` outputs.

Regression coverage lives in `tests/test_rapidtriage_output_samples.py`, which generates fresh command output and compares it against the checked-in sample JSON under `docs/rapidtriage-output-samples/`.

## Placeholder conventions

The sample JSON uses a few placeholders so the examples stay stable across machines:

- `<ROOT>` — scanned fixture root
- `<GENERATED_AT>` — runtime timestamp generated by the command
- `<PLATFORM>` — host platform string from the runtime

## Samples

### `validation`

- Output: `rapidtriage-validation-package.json`
- Top-level keys:
  - `command`
  - `generated_at`
  - `platform`
  - `score_target`
  - `internal_roadmap_score`
  - `commercial_readiness_score`
  - `status`
  - `output_dir`
  - `outputs`
  - `checks`
  - `commercial_gap_assessment`
  - `recommended_commands`
  - `required_documents`
  - `known_limits`
  - `release_decision`
- The Markdown sidecar is `rapidtriage-validation-report.md`.

### `commercial-readiness`

- Output: `rapidtriage-commercial-readiness.json` and `rapidtriage-commercial-readiness.md`.
- Top-level keys:
  - `command`
  - `generated_at`
  - `backlog_path`
  - `status`
  - `commercial_claim_allowed`
  - `release_claim`
  - `readiness_score`
  - `item_count`
  - `commercial_ready_count`
  - `non_commercial_count`
  - `status_counts`
  - `severity_counts`
  - `category_counts`
  - `maturity_gate_definitions`
  - `maturity_gate_summary`
  - `commercial_blocker_matrix`
  - `blocker_execution_package`
  - `commercial_uplift_plan`
  - `validation_evidence_summary`
  - `priority_work_plan`
  - `all_items`
  - `critical_non_commercial_items`
  - `non_commercial_items`
  - `required_release_evidence`
  - `operator_guidance`
- Each row in `all_items` preserves the 120-item commercial parity backlog entry with `number`, `title`, `status`, `category`, `severity`, `remaining_gap`, `commercial_grade_ready`, `commercial_blockers`, `release_gate`, `maturity_gates`, `highest_maturity_stage`, and `next_required_gate`.
- `maturity_gates` always evaluates `implemented`, `usable`, `validated`, and `commercial_grade` in order. A higher gate must not be claimed until every earlier gate passes.
- `maturity_gate_summary` includes `next_gate_samples` and `next_gate_blocker_counts` so reviewers can see where work is blocked without reading all 120 items.
- `commercial_blocker_matrix` classifies every non-commercial item by one or more blocker lanes (`native-parser-depth`, `known-answer-validation`, `large-scale-performance`, `security-legal-assurance`, `platform-release-evidence`, or `external-operator-evidence`), separates internally actionable work from external/trusted evidence requirements, and exposes `top_internal_items` plus `top_external_evidence_items` for the next execution batch.
- `blocker_execution_package` uses `commercial-blocker-execution-package-v1` to turn the blocker matrix into bounded internal and external-evidence batches, acceptance-evidence checklists, rerun/template commands, item-number lists, and a stable package hash. For #1~#30 it can attach `trusted_diff_runner_hint` rows pointing EVTX blockers to EvtxECmd/Hayabusa, Registry blockers to RECmd/Registry Explorer, SAM/SECURITY/SYSTEM plus Amcache/ShimCache/BAM/DAM blockers to RECmd/RegRipper/AmcacheParser/AppCompatCacheParser, SRUM and Windows.edb blockers to SrumECmd/libesedb/Windows Search DB Analyzer, MFT/USN blockers to MFTECmd/analyzeMFT/UsnJrnl2Csv, JumpList/ShellBags blockers to JLECmd/ShellBagsExplorer/SBECmd validation, Prefetch blockers to PECmd validation, LNK blockers to LECmd/JLECmd validation, Windows system artifact blockers to Velociraptor/Chainsaw/Autoruns validation, browser storage/timeline/AI blockers to Hindsight/BrowserHistoryView/DB Browser/Velociraptor/service-export validation, E01/RAW/virtual-disk/container workflow blockers to libewf/Sleuth Kit/qemu-img/FTK Imager/vendor-export validation, and mobile/iOS/Android/APK blockers to Cellebrite/XRY/GrayKey/AXIOM/iLEAPP/ALEAPP/MVT/apktool validation. It is an operator checklist only; it never satisfies validation or commercial-grade gates without attached evidence.
- `priority_work_plan` lists the next highest-priority backlog items with `current_stage`, `next_gate`, and `required_action`; use `--next-gate validated --limit 10` to focus JSON output on one maturity gate.
- `commercial_uplift_plan` selects the highest-priority non-commercial items, defaults to 70 goals, and splits them into five-item batches. Each goal includes `objective`, `implementation_track`, `acceptance_evidence`, `large_data_strategy`, `remaining_gap`, `external_evidence_required`, and `internal_next_step`. The plan-level `large_data_strategy` records the streaming, checkpointing, cursor API, virtualized UI, and benchmark proof expectations for large evidence sets.
- `validation_evidence_summary` is populated by one or more `--validation-package` arguments. Repeating the option combines batch manifests, records `validation_package_count` and `validation_package_paths`, and maps every passing dataset to its backlog items. Only passing known-answer datasets that explicitly name backlog item numbers can satisfy an item's `validated` gate; this never bypasses `commercial_grade` blockers.
- `--write-known-answer-template PATH` writes a not-run known-answer manifest template plus Markdown sidecar for the selected `--next-gate`/`--limit` items. The generated datasets include `backlog_items`, `required_assertions`, `reference_tools`, `minimum_evidence`, and `status: "not-run"` so validation evidence cannot be accidentally treated as passed.
- `--write-known-answer-template-dir DIR --template-items 1-120 --template-batch-size 5` writes 24 not-run template pairs plus `known-answer-template-batches.index.json/.md`, allowing the 120-item backlog to be validated in repeatable five-item batches.

### `manifest`

- Sample: `docs/rapidtriage-output-samples/manifest-windows-artifacts.json`
- Top-level keys:
  - `generated_at`
  - `root`
  - `platform`
  - `keywords`
  - `providers`
- Each provider row includes:
  - `name`
  - `description`
  - `target_platform`
  - `supported`
  - `artifacts`

### `case-search`

- Output: JSON from `rapidtriage case-search`.
- Top-level keys include `summary`, `documents`, `large_case_search_plan`, `review_workflow_summary`, `case_search_result_window_manifest`, `reportability_decision`, and `matches`.
- `summary.search_index_health_status` and `summary.search_index_missing_rows` mirror the embedded `large_case_search_plan.search_index_health` block. Each source plan row also includes `search_index_status`, `missing_index_rows`, and `orphan_fts_rows`; non-healthy requested sources set `partial_coverage_warning` so a no-hit result cannot be treated as absence until `case-db --rebuild-search-indexes` is run.
- `summary.cursor_api.profile_version` is `case-search-cursor-v1`. `summary.next_cursor` is an opaque token for the next page when `summary.has_more` is true; it is bound to the case id, keywords, source filters, metadata filters, review status, and verification status. Reusing it with a different query is rejected instead of restarting silently. CLI uses `--cursor`; the web/API request body uses `cursor`.
- `case_search_result_window_manifest.profile_version` is `case-search-result-window-manifest-v1`. It records the stable query hash, cursor offset/page size, retrieval and scan caps, page-window hash, per-hit row hashes, source-viewer locators, source/backend counts, partial source warnings, review assignment manifest hash, and #61/#78/#79 accuracy gates. `summary.case_search_result_window_manifest_hash` mirrors the manifest hash so API/UI clients can prove that a page of hits was not silently changed before a reviewer cites it.
- Artifact `matches[].source_reference` preserves parser-native source handoff fields when imported rows provide them: `source_viewer_locator`, `source_locator`, `source_locator_hash`, `row_citation_hash`, and `parser_manifest_hashes`. Cloud, email, and AI export rows therefore remain tied to the original provider-export, mailbox, or transcript row through Case DB search, review queue, and report export instead of degrading to only a generic Case DB row ID.
- `reportability_decision.profile_version` is `case-search-reportability-decision-v1`. It declares case-search rows as review leads, not standalone proof, and exposes `ready_for_absence_claim`, `ready_for_court_report=false`, source-plan status, search-index health, partial sources, blockers, required report steps, and a decision hash. UI/report code should block no-hit/absence wording whenever this decision says `ready_for_absence_claim=false`.
- `large_case_search_plan.profile_version` is `case-search-large-case-plan-v1`. Each source row records whether it was requested, whether it used `sqlite-fts5`, `bounded-scan`, or `skipped`, the scan candidate cap, and `partial_coverage_warning` when a table has more rows than the bounded scan window can inspect. File path/extension/hash searches use `file_record_fts` when rows are present, artifact/indicator searches use maintained `artifact_fts` rows, and timeline event type/time/target/description/source searches use `event_fts` when rows are present; bounded-scan warnings remain a reason to narrow filters, search an FTS-backed source, or attach further large-case validation evidence before claiming absence.
- `case-db --search-index-health` adds `search_index_health.profile_version=case-db-search-index-health-v1`. It reports per-source `source_rows`, `indexed_rows`, `missing_index_rows`, `orphan_fts_rows`, `status`, and `recommendation` for documents, files, artifacts/indicators, and timeline search indexes. `case-db --rebuild-search-indexes` adds `rebuilt_search_indexes.profile_version=case-db-search-index-rebuild-v1` with `before`, `after`, and per-index `actions[]`, so stale or missing FTS rows can be repaired and rechecked before a large-case search claim.
- `summary.document_error_count` and `documents.errors[]` surface document text extraction failures recorded while importing a completed run into the Case DB. These rows include path, kind, audit citation, target document id, error type/message, and `case-search-documents-partial-coverage` effect. Treat them the same way as completed-run `documents.errors[]`: the Case DB search continued, but the listed documents were not text-indexed and need source-viewer byte-window search, a dedicated parser, or a trusted external extractor before absence can be asserted.

### `source-read`

- Output: JSON from `rapidtriage source-read`.
- `profile_version`: `source-read-v1`.
- Top-level keys include `source`, `analysis_root`, `path`, `relative_path`, `hashes`, `preview`, `source_locator`, `source_citation_package`, `forensic_read_profile`, and `reportability_decision`.
- `preview.preview_type` is one of `text`, `sqlite-table`, `hex`, or `error`; all preview modes are bounded so large files remain responsive.
- `--path archive.zip::entry.json` opens a bounded single ZIP member without extraction. The payload keeps `path` as the archive path, `relative_path` as `archive.zip::entry.json`, and adds `container_relative_path`, `archive_entry`, entry hashes, CRC32, compressed/uncompressed sizes, and `preview.strategy=bounded-zip-entry-text` or a bounded hex preview.
- `source_locator` is the stable viewer locator for the current preview, such as `text-preview`, `sqlite-table-page`, `byte-range`, `zip-entry-text-preview`, or `zip-entry-byte-range`.
- `source_citation_package` uses `source-read-citation-package-v1` and includes copy-safe `citation_text`, `source_locator`, optional `source_sha256`, `snippet`, `snippet_sha256`, `review_note_template`, `package_hash`, and report-selection blockers. Use the `Current-file hit:` template to carry verified source-viewer hits into review marks or report candidates without losing hash/locator provenance.
- `case-review --source-read-json <source-read.json>` imports that `review_note_template`, citation text, and package hash into the Case DB review note and also stores the full `source_citation_package` in `review_mark.source_citation_package_json`. The resulting review mark exposes `source_citation_package_hash` and `source_review_handoff`, so later report export can retain the exact source-viewer locator/hash/snippet proof instead of relying on note text alone.

### `source-search`

- Output: JSON from `rapidtriage source-search`.
- `profile_version`: `source-search-cli-v1`.
- Searches only one bounded source preview at a time, including `--path archive.zip::entry.json` ZIP members without extraction. SQLite `.db`/`.sqlite`/`.sqlite3` files are opened read-only and scanned across bounded text columns; `--sqlite-row-scan-limit` controls the CLI scan cap and is echoed as `summary.sqlite_row_scan_limit`. Large plain-text sources can be searched by byte window with `--byte-offset` and `--max-search-bytes`; the summary then includes `plain_text_stream_search`, `stream_status`, `byte_offset`, `bytes_read`, `next_byte_offset`, `stream_truncated`, and `offset_semantics`.
- It returns `relative_path`, optional `container_relative_path`, `archive_entry`, normalized `keywords`, `summary.match_count`, `summary.zip_entry_search`, optional SQLite `summary.sqlite_search/sqlite_scanned_tables/sqlite_scanned_rows/sqlite_searchable_columns`, and `matches[]` with `keyword`, `line`, `offset`, `line_offset`, `snippet`, `citation`, `source_path`, plus SQLite `table`, `rowid`, and `column` when applicable. Plain-text stream matches also include `byte_offset` and `window_offset`.
- `source_locator` and `source_citation_package` mirror the matching `source-read` locator so terminal hit context can be verified in the web source viewer and carried into Case DB review notes without losing archive-entry provenance.
- `ready_for_review_note` means the package is usable as a reviewer handoff. `ready_for_court_report` remains `false` until analyst sign-off, original evidence provenance, and any parser-specific validation are attached.
- When `source-read.json` or `source-search.json` is attached to `commercial-readiness --mac-first-evidence`, the readiness row now carries `source_required_before_report[]`, `source_required_before_report_count`, `source_reportability_blockers[]`, and `source_reportability_blocker_count`. These fields preserve the source-viewer warning chain in the commercial-readiness bundle, so search hits stay review leads until the analyst opens the locator, records review status, verifies hashes/provenance, and cites the exact line/offset/table locator.

### `large-case-readiness`

- Output: JSON from `rapidtriage large-case-readiness`.
- `profile_version`: `large-case-readiness-v1`.
- `item_numbers`: large-case readiness targets covered by the report: #66, #67, #74, #78, and #79.

### Mac-First Cloud Export Evidence Attachment

- `commercial-readiness --mac-first-evidence cloud-export-artifacts.json` summarizes preparatory provider export evidence without converting it into a commercial-grade pass.
- Cloud rows include `cloud_export_service_counts`, `cloud_export_ai_service_counts`, parser/archive manifest hash counts, AI conversation and complete-pair counts, orphan question/answer counts, `cloud_export_ai_min_completeness_score`, `cloud_export_ai_incomplete_conversation_count`, `cloud_export_reportability_blockers[]`, and `cloud_export_required_before_report[]`.
- These fields are evidence triage controls for #21/#37/#38/#39. They are designed to make incomplete AI transcript pairing, missing provider scope, original export hash gaps, and trusted provider/export diff blockers visible before a user relies on a Gmail/iCloud/M365/AI export row in a report. Google rows add `google_takeout_report_grade_validation_plan`; iCloud rows add `icloud_export_report_grade_validation_plan`; M365 rows add `m365_export_report_grade_validation_plan` for Teams, OneDrive/SharePoint, audit, eDiscovery-scope, provider-diff, and retention/permission row reportability slots.
- `status`: `internal-evidence-present` only when attached evidence passes the local checks; otherwise `needs-large-case-evidence`.
- `checks[]`: stable pass/fail rows for attached SQLite FTS benchmark JSON, 100k/1M/10M coverage, seeded hit-count parity, p95 threshold, Case DB attachment, FTS tables, hot-path indexes, search diagnostics, and search-index health.
- `benchmarks[]`: normalized summary of each `sqlite-fts-benchmark` JSON with record count, p50/p95 latency, expected-vs-returned hit counts, query-plan hash, and proof manifest hash.
- `case_db_profile`: optional read-only profile of the RapidTriage Case DB, including table counts, FTS tables, index names, SQLite pragmas, the existing Case DB FTS optimization assessment, `search_diagnostics`, and `search_index_health`.
- `case_db_profile.search_diagnostics`: `case-db-search-diagnostics-v1` records the readiness keyword, per-FTS-table row counts, keyword `MATCH` counts, `EXPLAIN QUERY PLAN` details, stable table hashes, blockers, and a profile hash. It also embeds `case-db-cursor-diagnostics-v1`, which records deterministic rowid-ordered page-one/page-two row hashes, next-offset hashes, non-overlap proof, and pagination-proven table counts when a Case DB has at least two matching rows. This is Mac-local evidence that Case DB search is wired to FTS paths and cursor-sized page windows; it is still not a commercial large-case claim without 1M/10M benchmark JSON, UI virtualization evidence, and trusted-tool/known-answer diffs.
- `case_db_profile.search_index_health`: `case-db-search-index-health-summary-v1` aggregates `case-db-search-index-health-v1` across Case DB cases and records `status`, `ready_for_large_case_search`, `missing_index_rows`, `orphan_fts_rows`, unhealthy case IDs, blockers, and a profile hash. `summary.case_db_search_index_healthy=false` means no-hit/absence claims are blocked until `case-db --rebuild-search-indexes` has repaired every unhealthy case.
- `search_backend_contract`: the current search backend contract for the representative keyword and corpus estimate.
- `commercial_grade_ready` is intentionally `false` until real 1TB/10TB stress evidence, UI virtualization evidence, cursor API regression evidence, and independent/lab validation are attached.

### `vsc-compare`

- Output: `rapidtriage-vsc-compare.json`
- Top-level keys:
  - `generated_at`
  - `tool`
  - `current_root`
  - `snapshot_roots`
  - `options`
  - `summary`
  - `comparisons`
  - `notes`
- Each comparison includes `snapshot_root`, `snapshot_label`, a per-snapshot `summary`, and change `records`.
- Change records include `status` (`deleted`, `added`, or `modified`), `relative_path`, and `snapshot`/`current` file payloads with path, size, mtime, and optional SHA256.
- `case-db --import-vsc-compare` stores each change record as `vsc-deleted-file`, `vsc-added-file`, or `vsc-modified-file` artifact rows so they can be searched, reviewed, tagged, and included in reports.
- `case-search --metadata KEY=VALUE` can narrow artifact results by promoted metadata such as `event_id=4104`, `event_family=execution`, `channel_family=powershell`, `channel=Security`, `status=deleted`, or `snapshot_label=VSC1`.

### `commercial-readiness`

- Output: JSON from `rapidtriage commercial-readiness --json`.
- `stdout_limit_profile.profile_version` is `commercial-readiness-stdout-limit-v1` when CLI stdout arrays are compacted by `--limit`. Complete, untrimmed JSON and Markdown reports are written when `--output-dir` is supplied.

### `vsc-extract`

- Output: `OUTPUT_DIR/rapidtriage-vsc-extract.json`
- Top-level keys:
  - `generated_at`
  - `tool`
  - `current_root`
  - `snapshot_roots`
  - `output_dir`
  - `evidence_root`
  - `options`
  - `summary`
  - `comparison_summary`
  - `copied`
  - `skipped`
  - `notes`
- `copied` rows include `status`, `snapshot_label`, `relative_path`, source/destination paths, size, source SHA256, destination SHA256, mtime, and evidence-strength label.
- Deleted and modified rows preserve the snapshot-side file by default; selecting `--status added` preserves the current-side file.

### `compare`

- Output: `rapidtriage-compare.json`
- Top-level keys:
  - `command`
  - `generated_at`
  - `options`
  - `inputs`
  - `summary`
  - `results`
- Each result includes `comparison_id`, `status`, `timestamp`, `path`, `left_path`, `right_path`, `summary`, per-field differences, input descriptors, and a bounded unified text diff when both files are safe UTF-8 text under the configured byte cap.
- With three or more input files, `options.mode` is `multi`, `inputs.baseline` is the first file, `inputs.items` lists every file, and `results[]` contains one pairwise comparison from the baseline to each remaining file.
- `case` can bookmark `/results/<index>` rows from compare JSON, and `case-report` renders those reviewed rows under `A/B compare review pivots`.

### `docs`

- Sample: `docs/rapidtriage-output-samples/docs-keyword-search.json`
- Top-level keys:
  - `command`
  - `root`
  - `generated_at`
  - `summary`
  - `manifest`
  - `candidates`
  - `results`
  - optional `extraction_errors`
  - optional `index` metadata when a processed-text index sidecar is written
- `summary` currently records:
  - `candidate_count`
  - `match_count`
  - `supported_extensions`
  - optional `extraction_error_count` and `skipped_document_count` when large/corrupt documents are skipped
- Supported document extensions include text/config/log/data, HTML/RTF, PDF, Office/OpenDocument files, EML/MBOX email, and bounded Outlook MSG/PST/OST string extraction.
- `extraction_errors[]` records path, kind, size, reason, error type, and `document-skipped-search-continues` effect so analysts can see that a missing hit is a partial-search condition, not proof that the keyword is absent.
- Optional `docs-index` sidecars use a processed-text inverted index and do not store full extracted text.
- `rapidtriage docs-index-search <docs-index.json> -k <keyword>` queries that sidecar without re-extracting document text. Output command `docs-index-search` includes matched document locators, text hashes, score, matched terms, result hashes, limit/truncation state, and an explicit `source-viewer-hit-context-validation-required` blocker because analysts must open the source document for context before reporting.

### `files`

- Sample: `docs/rapidtriage-output-samples/files-default-scan.json`
- Top-level keys:
  - `command`
  - `root`
  - `generated_at`
  - `filters`
  - `summary`
  - `known_good_suppression_profile`
  - `known_good_suppressed_candidates`
  - `file_signature_profile`
  - `signature_mismatch_candidates`
  - `candidates`
- Each candidate row currently includes:
  - `path`
  - `name`
  - `extension`
  - `size`
  - `modified_at`
  - `modified_epoch`
  - `categories`
  - `reasons`
- Default categories include documents, archives, databases, executables, emails, AXIOM-aligned disk/mobile images, memory dumps, vehicle exports, and image/OCR candidates.
- File scan summaries include #76 hash-cache, #77 duplicate-detection metadata, and analyst-supplied known-good suppression metadata. `hash_cache_manifest` records `hash-cache-manifest-v1` with hashed paths, basename, path/size/mtime/inode/device cache metadata, hit/miss/invalidation counters, recent cache events, and a manifest hash. `hash_cache_assessment` also carries `hash_cache_report_grade_validation_plan` with validation-plan hash plus ready/blocking slot counts for snapshot, invalidation, trusted-manifest, and large-case hit-ratio review. `duplicate_group_count` and `duplicate_file_count` summarize `duplicate_content_groups`, while `duplicate_content_manifest` records `duplicate-content-manifest-v1` with stable duplicate group IDs/fingerprints, representative paths, not-suppressed analyst-review policy, and a manifest hash. `duplicate_detection_assessment` carries `duplicate_content_report_grade_validation_plan` with exact/fuzzy grouping, review-matrix, perceptual-media, suppression-workflow, and large-case blocker slots.
- `--known-good-hash-feed` accepts TXT/CSV/JSON MD5/SHA1/SHA256 feeds, bounded ZIP archives containing those feeds, and analyst-supplied NSRL RDS CSV headers such as `SHA-1`, `MD5`, `FileName`, `FileSize`, `ProductCode`, and `OpSystemCode`. Matching candidates get `known_good_status`, `known_good_match`, `known_good_match.source_detail`, and `report_suppression_status`; `--hide-known-good` removes those rows from `candidates` while preserving bounded `known_good_suppressed_candidates`. This is practical De-NISTing/whitelisting support for local feeds, not a bundled NSRL database download/update workflow.
- `known-good-index` writes `known-good-index-v1` JSON with normalized `records`, `feed_summaries`, source feed paths, duplicate/rejected counts, ZIP feed/member counts, NSRL RDS feed/row counts, and `manifest_hash`. The same JSON can be passed back to `--known-good-hash-feed`. NSRL RDS-style `.txt` files such as `NSRLFile.txt` are parsed as structured CSV when their header contains RDS fields, including when they are inside a bounded ZIP feed.
- File candidates with known extensions also get bounded header checks in `file_signature`. PE/PDF/ZIP/SQLite/PNG/JPEG/GIF/OLE/RAR/7z/GZIP magic mismatches are summarized in `file_signature_profile` and bounded `signature_mismatch_candidates`; the row remains visible and marked for analyst review.

### `extract`

- Samples:
  - `docs/rapidtriage-output-samples/extract-from-files.json`
  - `docs/rapidtriage-output-samples/extract-from-docs.json`
- Top-level keys:
  - `command`
  - `source_command`
  - `input_json`
  - `root`
  - `generated_at`
  - `output_dir`
  - `filters`
  - `summary`
  - `entries`
  - `skipped`
- Common entry keys:
  - `original_path`
  - `extracted_path`
  - `relative_path`
  - `sha256`
  - `modified_at`
  - `size`
- Source-specific entry keys:
  - from `files`: `categories`
  - from `docs`: `kind`, `matched_keywords`

### `run`

- Output: `rapidtriage-run-summary.json`, `rapidtriage-run-report.md`, `rapidtriage-run-fingerprint.json`, `rapidtriage-run-checkpoints.json`, and `rapidtriage-parser-scheduler.json`.
- Summary `outputs` includes `fingerprint`, `checkpoints`, and `parser_scheduler` in addition to manifest, docs, files, artifacts, timeline, indicators, report, and extraction manifests.
- Summary `workflow` uses `run-workflow-contract-v1` to expose the GUI-facing single-case pipeline: `ingest`, `extract`, `parse`, `index`, `review`, and `report`. Each stage carries status, step names, output keys, warnings, primary GUI handoff, `handoff_outputs` rows, and `analyst_checklist` rows. `analyst_checklist_summary` aggregates ready/warning/blocked/pending items so the workbench can show what must be opened or resolved before an analyst treats a stage as reliable.
- `rapidtriage-run-report.md` mirrors that checklist in a "Workflow analyst checklist" section so exported markdown preserves the same stage verification obligations shown in the GUI.
- Run output previews use `/api/runs/{run_id}/outputs/{output_name}/preview` and emit `run-output-preview-v1` with bounded renderer metadata, rewritten output download actions, and a reportability decision that treats the preview as an analyst review aid.
- `safety.resume`, `safety.resume_effective`, `safety.resume_disabled_reason`, and `safety.reused_outputs` describe whether stage output reuse was requested, allowed, and applied.
- `resource_caps` records bounded extraction/fingerprint/preview settings, and `safety.artifact_scheduler` records the bounded parallel artifact scheduler strategy.
- `rapidtriage-run-fingerprint.json` stores a bounded source tree fingerprint with scanned file count, total size, latest mtime, max files, truncation status, bounded per-file SHA-256 records, and a content-hash policy.
- `rapidtriage-run-fingerprint.json` stores #68 bounded path/size/mtime fingerprint metadata, bounded content hashes, `incremental-reuse-plan-v1` added/removed/changed/unchanged counts, `incremental-reuse-decision-manifest-v1` per-path reuse/rebuild row hashes, `incremental_indexing_report_grade_validation_plan` hashes/ready slots/blocking slots, truncation status, incremental-indexing validation guidance, #68 `core_accuracy_gates`, and #68 `commercial_uplift_evidence` with reuse-safety controls plus full large-file, row-level delta, trusted manifest, Case DB dedup, cross-platform, and large-case blockers.
- `rapidtriage-run-checkpoints.json` stores #70 per-stage output paths, existence, size, reused flag, row hash, status counts, resume assessment, `checkpoint-integrity-profile-v1` head hash, `checkpoint-resume-decision-manifest-v1` per-stage decision row hashes, the input fingerprint used for resume decisions, #70 `core_accuracy_gates`, and #70 `commercial_uplift_evidence` on both summary and stage checkpoint records.
- Run `processing` now includes #71 parser-crash isolation, #72 memory-cap enforcement, and #75 parallel parser scheduler assessments. It also emits `runtime_defensibility_profiles` for #71-#75 so analysts can review parser-crash, memory-cap, preview-sandbox, large SQLite/FTS, and scheduler blockers in one place. Isolated parser failures are written as parser-specific artifact JSON with `parser_errors`, per-error hashes, crash context, parser-error inventory metadata, and `parser-crash-continuation-manifest-v1` row hashes instead of silently dropping the stage. Memory-cap assessments include `memory-cap-policy-profile-v1` plus `memory-cap-stage-telemetry-manifest-v1` with per-stage RSS row hashes, cap configuration, utilization percentage, over-cap state, platform, and breach action. `rapidtriage-parser-scheduler.json` records `parser-scheduler-run-manifest-v1` with worker quota policy hash, deterministic output order, per-parser duration telemetry, event row hashes, reused/completed/error counts, output paths, and a manifest hash; the #75 processing assessment also carries `parser_scheduler_report_grade_validation_plan` with validation-plan hash and ready/blocking slot counts.
- `commercial-readiness` emits `review_scale_resilience_progress` for #76-#80, mapping hash cache, duplicate grouping, cursor APIs, UI virtualization, and cancel/retry controls to their primary outputs and required trusted manifests; #80 job payloads include `retry_lineage_profile`, `job-partial-output-policy-v1`, `cancellation-retry-manifest-v1` hashes, and `cancellation-retry-report-grade-validation-plan-v1` ready/blocking slots.
- `commercial-readiness` emits `validation_spine_progress` for #81-#85, mapping known-answer manifests, parser fixture corpora, FP/FN risk notes, independent validation signoff, and validation package hashes into one evidence chain.
- `commercial-readiness` emits `forensic_integrity_progress` for #86-#90, mapping Case DB custody, acquisition hashes, audit hash chains, reproducibility, and report-item provenance to the trusted manifests required for court-grade claims.
- `commercial-readiness` emits `report_quality_progress` for #91-#95, mapping parser confidence, validation warnings, legal limitation wording, court exhibit packages, and external tool version capture to their report-quality evidence chain and trusted-manifest blockers.
- `commercial-readiness` emits `acquisition_quality_progress` for #96-#100, mapping acquisition metadata, timezone normalization, clock-skew analysis, contamination warnings, and tamper-evident audit bundles to signed-handoff/trusted-manifest blockers.
- `commercial-readiness` emits `release_operations_progress` for #101-#105, mapping Windows signing, macOS notarization, Linux packaging, signed update channels, and local crash reporting to their release evidence chain and external signing/smoke-test blockers. #101 points at `windows_signed_installer.windows_signing_evidence_manifest.manifest_hash`, `windows_signed_installer.windows_signing_report_grade_validation_plan_hash`, and signing evidence slots; #102 points at `macos_notarized_package.macos_notarization_evidence_manifest.manifest_hash`, `macos_notarized_package.macos_notarization_report_grade_validation_plan_hash`, and notarization evidence slots; #103 points at `linux_package.linux_package_evidence_manifest.manifest_hash`, `linux_package.linux_package_report_grade_validation_plan_hash`, and distro package evidence slots; #104 points at `update-manifest.auto_update_evidence_manifest.manifest_hash`, `update-manifest.auto_update_report_grade_validation_plan_hash`, and signed-channel evidence slots; #105 points at `crash-report.crash_export_evidence_manifest.manifest_hash`, `crash-report.crash_report_grade_validation_plan_hash`, and redaction/export evidence slots.
- `commercial-readiness` emits `enterprise_governance_progress` for #106-#110, mapping local-only enterprise policy, offline license handling, RBAC policy, multi-user server guardrails, and collaboration audit scope to their trusted governance blockers. #106 points at `enterprise-policy.telemetry.local_only_evidence_manifest.manifest_hash`, `enterprise-policy.telemetry.local_only_report_grade_validation_plan_hash`, no-egress/remote-auth evidence slots, and `enterprise-policy.network`; #107 points at `enterprise-policy.license_activation.license_evidence_manifest.manifest_hash`, `enterprise-policy.license_activation.license_report_grade_validation_plan_hash`, offline-activation/evidence-touch slots, and the trusted license-authority blocker; #108 points at `enterprise-policy.rbac.rbac_evidence_manifest.manifest_hash`, `enterprise-policy.rbac.rbac_report_grade_validation_plan_hash`, per-action/export-control evidence slots, and the trusted RBAC enforcement blocker.
- `commercial-readiness` emits `operations_continuity_progress` for #111-#115, mapping backup/restore, release-note discipline, LTS/hotfix policy, support SLA, and training curriculum to their restore-drill/CI/staffing/training-delivery blockers.
- `commercial-readiness` emits `final_delivery_progress` for #116-#120, mapping analyst quickstart labs, admin deployment guidance, security hardening, malicious-evidence sandboxing, and dependency monitoring to their lab-run/AppSec/corpus/CI/SBOM blockers.
- `macos-live-smoke` emits `readiness_attachment` using `macos-live-smoke-readiness-attachment-v1`. It gives the exact folder-based `commercial-readiness --mac-first-evidence <qc-dir> ...` command, API query hint, GUI note, supported Mac-preparable backlog item numbers, and required follow-up evidence so a Mac smoke run can be attached without hand-written command reconstruction. `outputs.readiness_attachment_script` points at an executable `attach-mac-evidence-to-readiness.sh` helper that reruns that command locally. When `--case-db <case.db>` is supplied, `inputs.case_db_path_hash` is populated and the embedded `large_case_readiness.case_db_profile` records Case DB FTS/index/table-count evidence without exposing the local path unless `--include-path-details` is explicitly used.
- `commercial-readiness --mac-first-evidence <qc-dir>` attaches a `mac_first_evidence_summary` using `mac-first-evidence-attachment-v1`. It accepts individual `macos-live-smoke`, `large-case-readiness`, `email-external-parse`, `source-read`, `source-search`, and `cloud-export` JSON files, or a QC directory containing known evidence filenames (`macos-live-smoke.json`, `large-case-readiness.json`, `email-external-parser.json`, `source-read.json`, `source-search.json`, `cloud-export-artifacts.json`, `cloud-artifacts.json`, `cloud-ai-archive-artifacts.json`, `cloud-archive-artifacts.json`, `cloud-csv-artifacts.json`, `iaas-cloud-artifacts.json`). It hashes each attached evidence JSON, records smoke/large-case status, large-case search diagnostics readiness/hash/table counts, large-case cursor diagnostics readiness/hash/pagination-proof counts, large-case search-index health status/hash/missing rows, source viewer/search handoff readiness, source citation package hashes, source match counts, email external-parser manifest hashes/export counts/trusted-diff readiness, cloud export artifact/service counts, cloud parser/archive manifest hash counts, AI service export conversation counts, failed checks, commercial blockers, and supported Mac-preparable backlog areas (#21, #36, #37, #38, #39, #52, #55, #61, #64, #65, #66, #68, #74, #78, #79, #81, #85, #90, #95 when corresponding evidence is attached). This is intentionally preparatory evidence only; it does not satisfy `validated` or `commercial_grade` gates without trusted validation packages and external Windows/hardware/signoff evidence.
- `image-workflow-validate` emits `image-workflow-trusted-diff-v1` for #22~#25. The output records `command`, `gap_id`, `trusted_tool`, `status`, indexed row counts, matched/mismatch/missing/extra counts, mismatch samples, `commercial_grade_evidence`, `reportability_decision`, compared output paths, and preserved Rapid/trusted row counts. Use it with libewf `ewfverify`, Sleuth Kit `mmls/fls/tsk_recover`, `qemu-img`, FTK Imager, or a verified X-Ways/EnCase/AXIOM vendor export manifest. This is Mac-preparable trusted-diff evidence only; report-grade use still needs source hashes, exact commands, tool versions, corpus scope, and reviewer signoff.
- `e01-known-answer` and `e01-smoke` emit `e01-ex01-report-grade-validation-plan-v1` for #22. The plan records `case_id`, `source_path`, `output_root`, `validation_commands` with both shell command strings and `argv`, `evidence_slots`, `ready_slot_ids`, `blocking_slot_ids`, expected artifact assertions, source/segment/preflight context, commercial blockers, and a stable `manifest_sha256`. The required command set includes `rapidtriage e01-hash`, `rapidtriage evidence`, `ewfverify`, `rapidtriage e01-smoke`, and `rapidtriage image-workflow-validate`; this makes the next report-grade evidence collection step explicit without claiming native EWF parser completeness.
- `cross-tool-validate` now emits `mobile_export_field_comparison` and `mobile_app_field_comparison` for #26~#30 mobile/app rows. Mobile export diffs compare source tool/record ID, service, conversation/message IDs, timestamp, sender/recipient, text/media hashes, contact/call fields, iOS backup domain/path/file/protection-class, and keychain redaction fields. Mobile app diffs compare package/bundle ID, app label/version/build, permission sets, dangerous-permission counts, cert/APK hashes, DEX/native-library counts, app-data/database/table pivots, and risk-model fields. The loader expands nested RapidTriage mobile export rows from `details.messages`/`contacts`/`calls`/`media`/`ios_backup_files`/`keychain_rows` and Android app-data rows from `details.app_data_rows`/`databases`/`tables` before comparing them with vendor/iLEAPP/ALEAPP/apktool exports. Missing or mismatched mobile fields fail the comparison so vendor-export key overlap cannot be treated as parser parity.
- `cross-tool-validate` also emits `ai_transcript_field_comparison` for #21 and `chat_app_field_comparison`, `email_field_comparison`, `cloud_export_field_comparison`, and `cloud_api_field_comparison` for #31~#40 rows. AI transcript diffs compare service, conversation/pair IDs, question, answer, timestamp, source hashes, source paths/offsets, storage area, pairing confidence, and completeness fields against service exports or trusted browser-storage exports. Generated AI export rows additionally preserve `source_json_pointer`, `source_locator_type`, `text_sha256`, `row_citation_hash`, `export_schema_id`, and `export_schema_profile` so source viewers and reports can cite the original JSON/JSONL object rather than only the containing export file. The messenger/email/cloud diffs compare messenger service/conversation/message/timestamp/participant/hash fields, mailbox Message-ID/subject/folder/attachment/body-hash fields, provider export record/item/actor/action/hash fields, and API request/endpoint/status/response-hash/pagination fields. The loader expands nested RapidTriage rows from messenger `details.messages`/`message_rows`/`chat_messages`, email `details.messages`/`email_messages`/`mailbox_messages`/`exported_messages`, cloud `details.rows`/`files`/`items`/`events`/`audit_rows`, and API `details.responses`/`pages`/`requests`/`api_rows`; family guards prevent generic message lists from being counted as multiple artifact domains. Missing or mismatched fields fail the comparison so service/provider export overlap cannot be treated as parser parity.
- `commercial-readiness --mac-first-evidence <qc-dir>` attaches a `mac_first_evidence_summary` using `mac-first-evidence-attachment-v1`. It accepts individual `macos-live-smoke`, `large-case-readiness`, `email-external-parse`, `source-read`, `source-search`, `cloud-export`, and `image-workflow-validate` JSON files, or a QC directory containing known evidence filenames (`macos-live-smoke.json`, `large-case-readiness.json`, `email-external-parser.json`, `source-read.json`, `source-search.json`, `image-workflow-validate.json`, `image-workflow-trusted-diff.json`, `e01-workflow-trusted-diff.json`, `raw-workflow-trusted-diff.json`, `virtual-disk-workflow-trusted-diff.json`, `container-workflow-trusted-diff.json`, `cloud-export-artifacts.json`, `cloud-artifacts.json`, `cloud-ai-archive-artifacts.json`, `cloud-archive-artifacts.json`, `cloud-csv-artifacts.json`, `iaas-cloud-artifacts.json`). It hashes each attached evidence JSON, records smoke/large-case status, large-case search diagnostics readiness/hash/table counts, large-case cursor diagnostics readiness/hash/pagination-proof counts, large-case search-index health status/hash/missing rows, image workflow trusted-diff status for #22~#25, source viewer/search handoff readiness, source citation package hashes, source match counts, email external-parser manifest hashes/export counts/trusted-diff readiness, cloud export artifact/service counts, cloud parser/archive manifest hash counts, AI service export conversation counts, failed checks, commercial blockers, and supported Mac-preparable backlog areas (#21, #22, #23, #24, #25, #36, #37, #38, #39, #52, #55, #61, #64, #65, #66, #68, #74, #78, #79, #81, #85, #90, #95 when corresponding evidence is attached). This is intentionally preparatory evidence only; it does not satisfy `validated` or `commercial_grade` gates without trusted validation packages and external Windows/hardware/signoff evidence.
- The GUI/API readiness gate also returns `mac_first_evidence_summary` when `/api/commercial-readiness?mac_first_evidence=<path>` is used, so a local dashboard can display attached Mac smoke evidence separately from validation packages.
- Validation packages include #81 known-answer manifest status plus `known-answer-report-grade-validation-plan-v1` ready/blocking evidence slots, #82 parser fixture corpus coverage plus `fixture-corpus-report-grade-validation-plan-v1` ready/blocking evidence slots, #83 parser false-positive/false-negative notes plus `parser-fp-fn-report-grade-validation-plan-v1` ready/blocking evidence slots, #84 independent validation report hash metadata plus `independent-validation-report-grade-validation-plan-v1` ready/blocking evidence slots, and #85 validation package/hash-manifest assessment fields plus `validation-package-report-grade-validation-plan-v1` ready/blocking evidence slots. The #85 package assessment records required output presence for the generated JSON, Markdown, and artifact manifest; the artifact manifest carries SHA256 and size rows for generated package files.
- Case DB report exports include #86 custody workflow with `custody-report-grade-validation-plan-v1`, #87 acquisition hash workflow with `acquisition-hash-report-grade-validation-plan-v1`, #88 export-time audit hash chain with `immutable-audit-report-grade-validation-plan-v1`, #89 deterministic report reproducibility hash with `report-reproducibility-report-grade-validation-plan-v1`, and #90 source provenance metadata on each selected report item with `source-provenance-report-grade-validation-plan-v1` plan hashes/counts.
- Report items also include #91 parser confidence scoring with `parser-confidence-report-grade-validation-plan-v1` plan hashes/counts, #92 validation-required warning metadata with `validation-warning-report-grade-validation-plan-v1` plan hashes/counts, and #93 legal limitation assessment with `legal-limitation-report-grade-validation-plan-v1` plan hashes/counts. Reviewer bundles include #94 court exhibit index metadata with exhibit row hashes, selected-evidence manifest hashes, `court-exhibit-package-manifest-v1`, `court-exhibit-report-grade-validation-plan-v1` plan hashes/counts, and external signing slots; validation packages include #95 external tool version capture assessment with command argv, command hashes, capture-state hashes, version-output hashes, per-tool row hashes, `external-tool-version-manifest-v1`, and `external-tool-version-report-grade-validation-plan-v1` plan hashes/counts.
- Case DB report exports additionally expose #96 acquisition/write-blocker metadata validation with acquisition metadata row hashes, evidence-source row hashes, `acquisition-metadata-handoff-manifest-v1`, `acquisition-metadata-input-manifest-v1`, and `acquisition-metadata-report-grade-validation-plan-v1` plan hashes/counts; #97 timezone normalization checks with timezone sample row hashes, `timezone-normalization-manifest-v1`, `time-semantics-manifest-v1`, and `timezone-normalization-report-grade-validation-plan-v1` plan hashes/counts; #98 clock-skew warnings with `clock-skew-baseline-manifest-v1` and `clock-skew-report-grade-validation-plan-v1` plan hashes/counts; and #99 evidence-contamination warnings with `contamination-checklist-manifest-v1`, `contamination-acquisition-context-manifest-v1`, and `contamination-warning-report-grade-validation-plan-v1` plan hashes/counts. Reviewer bundles include #100 tamper-evident audit-bundle hash-chain metadata plus `tamper-evident-report-grade-validation-plan-v1` plan hashes/counts.
- Release and operations outputs expose #101 Windows signing evidence gates plus `windows-signing-evidence-manifest-v1` and `windows-signing-report-grade-validation-plan-v1`, #102 macOS notarization gates plus `macos-notarization-evidence-manifest-v1` and `macos-notarization-report-grade-validation-plan-v1`, #103 Linux package gates plus `linux-package-evidence-manifest-v1` and `linux-package-report-grade-validation-plan-v1`, #104 manual update-manifest policy plus `auto-update-channel-evidence-manifest-v1` and `auto-update-report-grade-validation-plan-v1`, #105 local crash reporting plus `crash-export-evidence-manifest-v1` and `crash-reporting-report-grade-validation-plan-v1`, #106 telemetry-free local-only mode plus `local-only-enterprise-evidence-manifest-v1` and `local-only-enterprise-report-grade-validation-plan-v1`, #107 offline license handling plus `license-activation-evidence-manifest-v1` and `license-activation-report-grade-validation-plan-v1`, #108 role policy plus `rbac-enforcement-evidence-manifest-v1` and `rbac-enforcement-report-grade-validation-plan-v1`, #109 multi-user server guardrails plus `multi-user-server-evidence-manifest-v1`, #110 collaboration audit-trail scope plus `collaboration-audit-evidence-manifest-v1`, #111 backup/restore migration readiness, #112 release notes discipline, #113 LTS/hotfix policy, #114 support SLA template, #115 training curriculum, #116 analyst quickstart lab, #117 admin deployment guide, #118 hardening review baseline, #119 malicious-evidence sandboxing limits, and #120 dependency vulnerability monitoring.
- Paged API wrappers include #78 `pagination.cursor`, `next_cursor`, `previous_cursor`, offset fields, corrected last-page `has_more`, `pagination-cursor-manifest-v1` page-window IDs/manifest hashes, snapshot-policy warnings, `pagination-cursor-report-grade-validation-plan-v1` ready/blocking slots, and pagination assessment metadata. Web tables expose #79 bounded rendering notices, `ui-virtualization-manifest-v1` row-window IDs/manifest hashes, and `ui-virtualization-report-grade-validation-plan-v1` ready/blocking slots when a result array is larger than the DOM row window; search and Case DB tables use 300-row previous/next windows plus `[`/`]` shortcuts.

### `search`

- Output: `rapidtriage-search.json`
- Top-level keys:
  - `command`
  - `generated_at`
  - `run_summary`
  - `keywords`
  - `options`
  - `summary`
  - `matches`
  - `documents`
  - `ocr`
  - `analysis` unless `--no-analysis` is used
- Each match includes `source`, `kind`, `path`, `title`, `matched_keywords`, `preview`, `pointer`, `metadata`, and `search_match`.
- `documents.errors[]` carries document extraction failures imported from `docs.extraction_errors[]` plus any completed-run re-extraction failures. `summary.document_error_count` must be treated as a partial-search warning: the remaining sources were searched, but the listed documents require source-viewer byte-window search, a dedicated parser, or a trusted external extractor before an absence claim is safe.
- `options.search_mode` is `exact`, `fuzzy`, or `regex`; `search_match` records #61 matching mode, fuzzy distance where applicable, proximity-window result when requested, and validation-required status. `advanced_search_profile` records active mode, query validation including invalid-regex warnings, match-mode counts, proximity matched count, query-hit manifest hash, hit row hash count, source locator count, report-grade validation-plan hash/ready slot/blocking slot counts, web/CLI controls, review warnings, and source-verification requirements. Each match includes `advanced_search_hit_manifest` with matched keywords, mode/options, source-viewer locator, hit row hash, manifest hash, and report-use blockers. Top-level `advanced_search_query_hit_manifest` records query hash, bounded hit rows, per-hit row hashes, source locators, query-result head hash, validation blockers, and `advanced_search_query_hit_manifest_hash`; top-level `advanced_search_report_grade_validation_plan` (`advanced-search-report-grade-validation-plan-v1`) records six internally ready slots and six external-required slots for source-row hash verification, relevance/false-positive corpora, trusted query-hit diff, browser query-builder validation, and large-case performance proof; top-level `core_accuracy_gates` records #61 query-mode, matching, proximity, pointer, query-hit manifest, row-hash, source-locator, report-grade plan, and source-verification checks.
- `options.hide_known_good` records whether unified search hid hits inherited from file `known_good_status`/`known_good_match`. `summary.known_good_match_count`, `summary.known_good_suppressed_count`, and `known_good_search_suppression_profile` preserve the reviewable/suppressed counts, bounded suppressed previews, applicable sources, and source-traceability note. The GUI exposes the same control as "Hide known-good / NSRL file hits."
- `analysis.clusters` groups repeated hits by source, kind, extension, folder, and keyword, with representative match indices, #46 status, review hints, `cluster_review_profile`, and `cluster_citation_manifest` so large searches can be triaged before opening every row.
- `analysis.clusters.cluster_review_profile` records family counts, high-volume cluster count, representative-first review queue, default `unreviewed`/`pending` review state, noise/report candidate hints, persistent-review-state blocker, and near-duplicate text/media clustering blocker.
- `analysis.clusters.cluster_citation_manifest` records bounded cluster entries, representative source viewer locators, match-level citations, manifest hash, and persistent-review/near-duplicate blockers for high-volume cluster review.
- `analysis.clusters.cluster_report_grade_validation_plan` (`search-cluster-report-grade-validation-plan-v1`) records the #46 plan hash, six internally ready slots, six external-required slots, and report guidance that blocks reviewed-cluster claims until persistent review state, near-duplicate validation, trusted review diff, false-positive corpus, large-case performance evidence, and independent review are attached.
- `analysis.entities` extracts bounded pattern/field-based people, account, email, phone, URL, domain, IPv4, and hash pivots linked back to match indices with #47 status, plus `entity_review_profile` for unreviewed entity queues, merge/split candidates, multi-source pivots, and analyst-verification blockers.
- `analysis.entities.entity_citation_manifest` records bounded hash-only entity entries, value shape, match-level citations, source viewer locators, merge/split review flags, and trusted entity-review blockers so raw entity values do not need to be duplicated into the citation manifest.
- `analysis.entities.entity_report_grade_validation_plan` (`search-entity-report-grade-validation-plan-v1`) records the #47 plan hash, six internally ready slots, six external-required slots, and report guidance that blocks identity-resolution claims until persistent review state, analyst-verified resolution, merge/split workflow, trusted entity diff, false-positive corpus, and independent review are attached.
- `analysis.graph` emits capped nodes and cited edges for match-path-keyword-entity relationships for the web relationship view with #48 graph limitations, plus `graph_interaction_profile` for node/edge filter metadata, bounded edge-page metadata, source-citation coverage, and saved-layout/server-paging blockers.
- `analysis.graph.graph_citation_manifest` records bounded edge citation entries, source viewer locators, node/edge type metadata, causal-proof flags, graph citation counts, server-side paging blockers, and saved-layout blockers for source-backed graph review.
- `analysis.graph.graph_report_grade_validation_plan` (`search-graph-report-grade-validation-plan-v1`) records the #48 plan hash, six internally ready slots, six external-required slots, and report guidance that blocks graph relationship claims until interactive review, server-side paging, saved layouts, trusted graph source-citation diff, large-case validation, and independent review are attached.
- `analysis.timeline` extracts timestamp anchors from matched metadata and sorts them for quick chronological correlation with #49 validation warnings, plus `timeline_correlation_profile` for source/kind/timezone distributions, bounded event-page metadata, missing-timezone counts, and full-case join/clock-skew/cursor blockers.
- `analysis.timeline.timeline_citation_manifest` records bounded event citations, source viewer locators, timestamp keys, timezone labels, path hashes, cursor/full-case/clock-skew blockers, and manifest hash for source-backed timeline review.
- `analysis.timeline.timeline_report_grade_validation_plan` (`search-timeline-report-grade-validation-plan-v1`) records the #49 plan hash, six internally ready slots, six external-required slots, and report guidance that blocks chronology claims until full-case joins, timezone/skew validation, cursor-paged timeline APIs, review annotation overlays, trusted timeline diffs, and large-case validation are attached.
- `analysis.deduplication` emits #60 duplicate groups with fingerprints, representative previews, representative hit indices, hidden duplicate counts, match indices, paths, source lists, duplicate/unique counts, candidate duplicate-resolution status, `dedup_review_profile` collapse-review metadata, `search_dedup_manifest` (`search-dedup-citation-manifest-v1`) with member row hashes and source-viewer locators, `search_dedup_report_grade_validation_plan` with validation-plan hash and ready/blocking slot counts, and report-grade validation blockers.
- `analysis.workbook` creates draft hypotheses, review questions, next actions, and `workbook_review_profile` with hypothesis review queue, evidence attachment counts, edit/persistence/version-history blockers, and #50 status; it is a triage aid and must be verified against source rows and hashes.
- `analysis.workbook.workbook_citation_manifest` records bounded draft hypothesis citations, evidence cluster refs, hypothesis source viewer locators, editable-workbook blockers, report-section blockers, and version-history blockers.
- `analysis.workbook.workbook_report_grade_validation_plan` (`search-workbook-report-grade-validation-plan-v1`) records the #50 plan hash, six internally ready slots, six external-required slots, and report guidance that blocks finding/report-section claims until editable Case DB persistence, source-row evidence attachments, reviewer assignments, report export, version history, and trusted workbook rubric validation are attached.
- `analysis.core_accuracy_gates` emits #46~#50 and #60 gate objects showing which required clustering, entity, graph, timeline, workbook, and duplicate-hit checks are satisfied and which remain validation-required.
- `analysis.commercial_uplift_evidence` emits #46~#50 batch evidence with passed/failed validation IDs by item, search-analysis caps, truncation flags, report blockers, and explicit reminders that persistent review state, full-case reindexing, graph paging, timeline skew validation, and editable workbook versioning are not yet commercial-ready.
- Top-level search output emits #61 `commercial_uplift_evidence` with query mode, fuzzy/proximity options, result caps, query-hit manifest hash, report-grade validation-plan hash, hit row hash counts, source locator counts, passed checks, and blockers for source-row hash verification, multilingual relevance corpora, trusted query-hit manifests, query-builder validation, large-case performance validation, and measured FP/FN tuning.
- Case DB search output emits #51 `review_workflow_summary` with review/verification/assignee/priority counts, bounded review queue, per-row source-viewer locators, queue row hashes, report-candidate counts, active review filters, `review_assignment_manifest_hash`, `core_accuracy_gates`, and blockers for role-based queues, notifications, multi-user conflict handling, and trusted review-audit diffs.
- Keyword-pack API output emits #62 `keyword-pack-manifest-v1` per pack, `keyword-pack-library-manifest-v1` on the library assessment, pack/keyword counts, keyword row hashes, expansion head hashes, manifest hashes, `keyword_pack_report_grade_validation_plan` (`keyword-pack-report-grade-validation-plan-v1`) with ready/blocking slot counts, `commercial_uplift_evidence`, provenance refs, deduplication state, and signed-pack/editor blockers. Completed-run search responses with `keyword_pack` parameters add `keyword_pack_selection_profile` plus `keyword-pack-selection-manifest-v1` with selected pack names, expanded keyword count, keyword rows, row hashes, expansion head hash, provenance refs, report-grade validation-plan hash, `core_accuracy_gates`, and selected-pack report-use warnings.
- Indicator summaries emit #63 `ioc-ti-feed-manifest-v1` on local feed sources, `ioc-ti-indicator-manifest-v1` on indicator rows, top-level `ioc-ti-enrichment-manifest-v1` with indicator/feed row hashes and manifest hashes, `ioc_ti_report_grade_validation_plan` (`ioc-ti-report-grade-validation-plan-v1`) with ready/blocking slot counts, plus `commercial_uplift_evidence` with offline TI feed refs, local-only/no-external-call controls, indicator/source caps, row-hash counts, validation-plan hashes, and signed/STIX/TAXII/confidence-decay/source-correlation blockers.
- Case DB report exports emit #64/#65 `commercial_uplift_evidence` on report citation and evidence-selection history managers with citation/history counts, source refs, selected-item controls, citation row hashes, citation source-viewer locators, citation-index manifest hashes, `report_citation_report_grade_validation_plan` hashes/ready slots/blocking slots, `evidence_selection_report_grade_validation_plan` hashes/ready slots/blocking slots, and exhibit-numbering/jurisdiction-template/reviewer-signoff/multi-user-signing/conflict/RBAC blockers.

### `source-preview` API

- Endpoint: `GET /api/runs/{run_id}/source-preview?path=...`
- Common keys: `command`, `path`, `name`, `size`, `mime_type`, `download_url`, `metadata_url`, `search_url`, `viewer_actions`, `viewer_limitations`, `viewer_sandbox`, `viewer_metadata`, `review_workflow`, `compare_workflow`, and `preview_type`.
- Structured preview types include `text`, `image`, `sqlite`, `json`, `xml`, `email`, `media`, and `hex`.
- `path=archive.zip::entry.json` opens one bounded ZIP member without extraction. The response keeps `path` as the full archive-entry locator, adds `container_path`, `container_size`, `container_modified_at`, `archive_entry`, `zip_entry`, and `source_locator`, points `download_url`/`metadata_url` at the original ZIP container, and uses `viewer_metadata.parser=rapidtriage.source-viewer.zip-entry` or `rapidtriage.source-viewer.zip-entry-json`. JSON/JSONL ZIP members get the same structured JSON summary as standalone files, while preserving entry CRC32, compressed/uncompressed sizes, entry hashes, and archive-completeness blockers. The returned `search_url` calls `/source-search` with the same `archive.zip::entry` locator for bounded current-entry search.
- `viewer_sandbox` states #73 read-only bounded rendering, no content execution, active-content blocking status, external-network prohibition, byte/character caps, `preview-sandbox-policy-profile-v1` renderer/path policy metadata, `source-preview-sandbox-manifest-v1` source policy row hashes, `preview-sandbox-report-grade-validation-plan-v1` ready/blocking slots, validation-plan hashes, and sandbox limitations.
- SQLite previews include #74 `large_sqlite_fts_optimization` metadata, `sqlite-preview-query-plan-profile-v1`, query-plan row hashes, and an explicit optimization assessment so large tables stay bounded in the viewer while indexed case search handles broader review. Case DB optimization metadata includes `case-db-query-plan-profile-v1` head hashes for repeatable query-plan comparison.
- Review and compare viewer actions expose #51/#52 status fields plus `core_accuracy_gates` and `commercial_uplift_evidence` for reviewer, assignee, priority, due date, report inclusion, A/B/C pinning, and unresolved multi-user/specialized-diff blockers, but final report use still requires source verification. #51 review workflows also carry `review_workflow_report_grade_validation_plan` (`case-review-workflow-report-grade-validation-plan-v1`) with ready/blocking slots for persisted review state, source locators, audit history, role-based queues, notifications, multi-user conflict handling, SLA dashboards, reviewer SOP signoff, and trusted audit diffs. #52 compare workflows carry `compare_report_grade_validation_plan` (`multi-evidence-compare-report-grade-validation-plan-v1`) with ready/blocking slots for baseline rows, hashes, status/diff counts, review queue, source/diff locators, selection rationale/notes, specialized diff viewers, persistent notes, citation signoff, and trusted expected diffs.
- `compare` output includes #52 `compare_review_profile` with input inventory, comparison review queue, optional selection rationale, bounded review notes, note/rationale counts, and blockers for persistent compare notes plus binary/image/SQLite/timeline-aware semantic diff. It also emits `compare_citation_manifest` with per-side source-viewer locators, diff locators, comparison entry hashes, and a manifest hash, plus `compare_report_grade_validation_plan` with a validation-plan hash so a reviewer can reopen the exact compared sources and see the report blockers before report use.
- Hex, SQLite, and email previews expose #53/#54/#55 `commercial_uplift_evidence` with passed viewer checks, byte/table/message caps, source refs, and explicit blockers for interactive jump UI, trusted offset validation, WHERE/deleted-row recovery, PST/OST/MSG conversations, attachment extraction, and report-citation export. Hex previews include `hex_preview_manifest` row hashes and source locators plus `hex_viewer_report_grade_validation_plan` with ready/blocking slots; `/source-hex-range` includes `hex_range_proof_manifest` with range hashes, source locator, row hashes, `hex_viewer_report_grade_validation_plan`, and copy-safe citation text. SQLite previews include `sqlite_preview_manifest` with schema/table/sample-row hashes plus `sqlite_viewer_report_grade_validation_plan` ready/blocking slots and a validation-plan hash; `/source-sqlite-table` includes `sqlite_table_page_manifest` with query hash, page row hashes, a table-page source locator, and the same report-grade validation-plan hash. Email previews include `email_conversation_manifest` with message/thread hashes, conversation source locators, `email_viewer_report_grade_validation_plan` ready/blocking slots, and validation-plan hashes; `/source-email-attachment` includes `email_attachment_proof_manifest` with attachment hashes, copy-safe citation metadata, and the same email report-grade validation plan.
- Image and media previews expose #56/#57 `commercial_uplift_evidence` with gallery/transcript checks, thumbnail/perceptual-hash/cue/source-hash controls, and blockers for virtualized gallery review, playback/ASR execution, and selected evidence export. Image previews include `image_gallery_manifest` with source locator and image row hash plus `image_gallery_report_grade_validation_plan` ready/blocking slots; `/source-image-gallery` includes `image_gallery_page_manifest` with page row hashes, bucket counts, gallery-page locator, and the same validation-plan hash. Media previews include `media_transcript_manifest` plus `media_transcript_report_grade_validation_plan` ready/blocking slots; `/source-media-cue` includes `media_cue_proof_manifest` and the same validation-plan hash.
- OCR queue and media-image rows expose #58/#59 `commercial_uplift_evidence` with queue counts, OCR/translation sidecar refs, language/confidence controls, queue/item/page manifest hashes, OCR queue report-grade validation-plan hashes, and explicit blockers for native OCR execution, engine retry logs, Korean OCR calibration, and certified translation workflows. `ocr-queue` command output includes `ocr_queue_manifest` (`ocr-queue-source-manifest-v1`), `ocr_queue_report_grade_validation_plan`, and each item includes `ocr_queue_item_manifest` (`ocr-queue-item-manifest-v1`). Image source previews also expose `ocr_queue_profile` and `korean_ocr_translation_profile`; `/source-ocr-queue` returns a bounded parent-folder `source-ocr-queue-page-v1` queue view with `source_ocr_queue_page_manifest`, `source_ocr_queue_report_grade_validation_plan`, source page validation-plan hash/slot counts, and `/source-ocr-translation` returns a bounded `source-ocr-translation-review-v1` side-by-side OCR/translation package with sidecar hashes, text hashes, quality metrics, `source_ocr_translation_review_manifest` (`source-ocr-translation-review-manifest-v1`), and copy-safe citation text.
- `hex` previews are bounded read-only rows with `offset`, `offset_hex`, `hex`, `ascii`, `bytes_read`, `max_bytes`, `offset_navigation`, `range_citation_profile`, `hex_viewer_report_grade_validation_plan`, #53 `core_accuracy_gates`, #53 assessment, and `truncated`; `/source-hex-range` returns a bounded JSON citation package with range hashes, rows, `hex_viewer_report_grade_validation_plan`, and copy-safe citation text for a requested byte range. These outputs are for triage and byte-offset orientation, not a replacement for trusted source-file validation.
- `sqlite` previews include table profiles, schema SQL, column details, index hints, bounded rows, text-column search support, `table_page_profile`, `sqlite_viewer_report_grade_validation_plan`, #54 `core_accuracy_gates`, and #54 assessment. The #74 `large_sqlite_fts_optimization` block records source/Case DB query-plan hashes, optimization manifests, `large-sqlite-fts-report-grade-validation-plan-v1` ready/blocking slots, and 10M-row/WAL/deleted-row/browser-E2E blockers. `/source-sqlite-table` returns `sqlite-table-page-v1` pages for a schema-validated table with `offset`, `limit`, optional `where_column` + `where_contains`, optional validated `order_by`, bounded rows, pagination metadata, `sqlite_viewer_report_grade_validation_plan`, and copy-safe row-page citation text; arbitrary SQL is intentionally rejected.
- `email` previews include bounded messages plus `threads` and `conversation_view` with `thread_id`, subject, participants, message order, date range, attachment counts, `attachment_package_profile`, `email_viewer_report_grade_validation_plan`, #55 `core_accuracy_gates`, and #55 assessment. `/source-email-attachment` returns `email-attachment-package-v1` metadata with filename/content type/size/hash, `email_viewer_report_grade_validation_plan`, copy-safe attachment citation text, and optional base64 content only under the bounded export cap.
- `image` previews include dimensions, thumbnails, perceptual-hash/similarity metadata, tag suggestions, `gallery_page_profile`, `ocr_queue_profile`, `korean_ocr_translation_profile`, OCR/translation sidecar status, `image_gallery_report_grade_validation_plan`, #56/#58/#59 `core_accuracy_gates`, and report-selection guidance. `/source-image-gallery` returns `image-gallery-page-v1` bounded folder pages with image hashes, dimensions, thumbnail availability, similarity buckets, bucket counts, keyboard-triage metadata, `image_gallery_report_grade_validation_plan`, and copy-safe image citation text.
- `media` previews include MIME type, bounded metadata, WAV duration/channel/sample-rate when available, #57 transcript sidecar rows, cue counts, sidecar hashes when bounded, `media-transcript-source-manifest-v1` with sidecar row hashes, cue hashes, source locator, `cue_package_profile`, `media_transcript_report_grade_validation_plan`, #57 `core_accuracy_gates`, review-aid validation status, and explicit playback/transcript verification limitations. `/source-media-cue` returns `media-cue-citation-package-v1` records with cue timestamps, cue text hash, sidecar hash, optional source hashes, `media-cue-proof-manifest-v1`, `media_transcript_report_grade_validation_plan`, source-cue locator, and copy-safe cue citation text.

### `ArtifactRecordV1`

- Schema: `rapidtriage/schemas/artifact-record-v1.schema.json`
- Producer: `rapidtriage worker-parse` and future Rust parser workers.
- Staging output: newline-delimited JSON, one `ArtifactRecordV1` object per line, with a sidecar manifest from the JSONL artifact store.
- Required forensic fields: `artifact_id`, `artifact_family`, `artifact_type`, `parser`, `parser_version`, `source`, `confidence`, `validation_required`, `commercial_grade_ready`, `commercial_grade_blockers`, `legal_limitations`, and `fields`.
- `source` preserves `case_id`, `source_id`, `source_path`, optional byte `offset`/`length`, and source hashes when available.
- The record is intentionally strict: parser workers must disclose confidence, validation requirements, and commercial-readiness blockers before rows can enter high-volume stores such as Parquet.

### `columnar-benchmark`

- Output: `columnar-benchmark.json`, `columnar-benchmark.md`, `artifact-records.jsonl`, and optional `artifact-records.parquet`.
- Top-level keys:
  - `command`
  - `generated_at`
  - `record_count`
  - `keyword`
  - `capabilities`
  - `environment`
  - `plan`
  - `jsonl_baseline`
  - `parquet`
  - `duckdb_parquet_query`
  - `resource_usage`
  - `commercial_readiness`
  - `outputs`
- `jsonl_baseline` includes write timing, records/sec, manifest path, SHA256, p50/p95 keyword scan timing, and match count.
- `parquet` is either `written` with row-group manifest metadata or `skipped` with an install hint when `pyarrow` is unavailable.
- `duckdb_parquet_query` is either `queried` with p50/p95 query timing or `skipped` with an install hint when DuckDB/Parquet evidence is unavailable.
- `commercial_readiness.ready_for_1m_10m_claim` must remain `false` for synthetic-only runs; release verification checks this disclosure to prevent overclaiming.

### `benchmark` And `stress-plan`

- `benchmark` outputs include #66 scale matrix rows, ingest/search metrics, memory/output sizes, run-summary links, `benchmark-scale-proof-manifest-v1` with covered/missing 100k/1M/10M targets, metric/environment/threshold hashes, `benchmark_report_grade_validation_plan` plus ready/blocking slot counts, report-grade assessment, #66 `core_accuracy_gates`, and #66 `commercial_uplift_evidence` with hardware-matrix/release-threshold/reproduction blockers.
- `stress-plan` outputs include #67 TB-scale scenarios, resource caps, failure thresholds, required evidence bundles, per-scenario `stress-run-log-template-v1`, `stress-evidence-capture-profile-v1` telemetry/artifact requirements, `stress-execution-proof-manifest-v1` with run-log template hashes and unattached execution status, `stress_report_grade_validation_plan` plus ready/blocking slot counts, real-hardware validation warnings, #67 `core_accuracy_gates`, and #67 `commercial_uplift_evidence` on the plan and each scenario.
- Background run-job payloads expose #69 `commercial_uplift_evidence` on the queue assessment and each job step, preserving status/progress/state-file/cancel/retry controls plus `job-transition-log-profile-v1` transition counts/head hashes and `job-queue-execution-manifest-v1` transition/step row hashes while explicitly blocking distributed-worker and parser-level telemetry claims.

### `columnar-convert`

- Output: machine-readable stdout and `OUTPUT.parquet.conversion.json`.
- Input: `ArtifactRecordV1` JSONL from `worker-parse` or another validated worker pipeline.
- Top-level keys:
  - `command`
  - `generated_at`
  - `input_jsonl`
  - `input_jsonl_size_bytes`
  - `input_jsonl_sha256`
  - `output_parquet`
  - `output_parquet_size_bytes`
  - `seconds`
  - `records_per_second`
  - `record_count`
  - `rejected_count`
  - `row_group_size`
  - `manifest`
  - `manifest_path`
  - `capabilities`
  - `resource_usage`
  - `commercial_readiness`
- The command requires `pyarrow`; without the optional `columnar` dependency group it exits with a clear install hint instead of silently falling back.

### `indicators`

- Schema: `rapidtriage/schemas/indicators.schema.json`
- Output: `rapidtriage-indicators.json`
- Top-level keys:
  - `command`
  - `generated_at`
  - `run_summary`
  - `options`
  - `summary`
  - `indicators`
- Each indicator row includes `type`, `value`, `count`, `classification`, `risk_flags`, and `sources`.
- Supported indicator types are `url`, `domain`, `ip`, `md5`, `sha1`, and `sha256`.
- Source rows keep the originating run output, output path, JSON pointer, and path/artifact/event context where available.
- When `--rules` is provided, matching local IOC rules are emitted as `matched_rules`; no external threat-intelligence API is contacted by default.
- Local rule/IOC hits from run component outputs are additionally normalized into `ioc_scanner_hits` with rule id, hit type/value, count, source pointer, source-viewer locator, row hash, and top-level `ioc_scanner_manifest`. This makes hash/domain/URL/keyword hits searchable and importable as Case DB indicator artifacts. `--rules` accepts RapidForensic JSON/YAML rules and YARA-lite `.yar/.yara` literal string IOC rules; the manifest records `rule_set_format`, `rule_set_rule_count`, and `yara_string_rule_import`. The implementation is a bounded local IOC scanner, not a complete native YARA grammar engine.
- When `--ti-feed` is provided, matching local feed rows are emitted as #63 `ti_enrichment` with `severity`, `classification`, `source`, `note`, `feed_row_hash`, feed provenance including feed SHA-256/size, validation status, and local-only assessment. `summary.enriched_indicator_count` counts enriched IOCs, and top-level `core_accuracy_gates` records #63 extraction, rule, feed, match-mode, feed manifest, enrichment manifest, row-hash, report-grade validation-plan, and no-external-call checks.
- API endpoint `/api/runs/{run_id}/indicators/ti-enrichment` emits `indicator-ti-enrichment` / `ioc-ti-enrichment-review-package-v1` for existing completed-run indicators plus one or more local feed paths. It returns bounded matched/unmatched review rows, feed provenance, feed/indicator/enrichment manifests, summary counts, `reportability_decision`, and #63 `commercial_uplift_evidence`.

### `case-db-report`

- Output: `case-db-report` JSON from `rapidtriage case-db-report`.
- Top-level keys include `command`, `generated_at`, `database`, `case`, `options`, `summary`, `citation_index`, and `items`.
- `citation_index` links #64 review decision citations to source-record citations with target type/id, title, path, source-reference metadata, optional `source_citation_package_hash`, `source_review_handoff`, report-use hints, `copy_safe_citation`, `citation_row_hash`, source-viewer locator metadata, upstream parser-native `source_viewer_locator` metadata, parser manifest hashes, and citation `core_accuracy_gates` where available. `report_citation_manager.citation_index_manifest` (`report-citation-index-manifest-v1`) records citation row hashes, source-viewer locator counts, source-hash/parser-version coverage, a citation rows head hash, and the manifest hash used by #64 `commercial_uplift_evidence`. `report_citation_manager.report_citation_report_grade_validation_plan` (`report-citation-report-grade-validation-plan-v1`) separates ready slots for review/source citation pairs, copy-safe strings, row hashes, source locators, source-reference coverage, and the citation-index manifest from external-required slots for source-hash/parser-version completeness, trusted citation-index diff, exhibit numbering UI, jurisdiction template review, and reviewer signoff corpus.
- Each item includes `review_citation_id`, `target_citation_id`, `review`, `review_history`, `source_citation_package`, `source_citation_package_hash`, `source_review_handoff`, `source_reference`, `review_priority`, #64/#65 gap metadata, and the underlying source metadata. When the item was marked with `--source-read-json`, `source_reference` is enriched with the source-viewer locator, package hash, source SHA256, snippet hash, row/table/offset hints, and `rapidtriage.source-read` parser identity where the target record did not already provide stronger parser metadata. When the target is an imported cloud/email/AI artifact row, `source_reference` and `provenance` also preserve the parser-native `source_viewer_locator`, `source_locator_hash`, `row_citation_hash`, and `parser_manifest_hashes` from the artifact metadata.
- `review_history` is ordered by version and records #65 target type/id, changed fields, actor, timestamp, previous state, current state, `history_viewer_locator`, per-row `row_hash`, and evidence-selection `core_accuracy_gates` for report-inclusion and review decision traceability.
- `evidence_selection_version_history.integrity_profile` emits #65 export-time row hash counts, changed-field counts, include-in-report change counts, a bounded hash-chain preview, append-only trigger names, and a `head_hash`. `evidence_selection_version_history.history_manifest` (`evidence-selection-history-manifest-v1`) records history row hashes, source-viewer locator counts, changed-field counts, append-only trigger metadata, history rows head hash, and the manifest hash used by #65 `commercial_uplift_evidence`. `evidence_selection_version_history.evidence_selection_report_grade_validation_plan` (`evidence-selection-history-report-grade-validation-plan-v1`) separates ready slots for version rows, changed fields, previous/current state, report-inclusion changes, row hashes/hash chain, source locators, and local append-only controls from external-required slots for signed multi-user history, trusted history diff, conflict handling, trigger review, reviewer identity/RBAC, and replay corpus. This is still not signed multi-user history; signed conflict-aware collaboration remains a commercial blocker.

### `submission-manifest`

- Schema: `rapidtriage/schemas/submission-manifest.schema.json`
- Top-level keys:
  - `command`
  - `generated_at`
  - `case_id`
  - `title`
  - `hash_algorithms`
  - `options`
  - `summary`
  - `items`
  - `skipped`
- Each evidence row includes:
  - `bookmark_id`
  - `summary`
  - `tags`
  - `note`
  - `review`
  - `reference`
  - `evidence.path`
  - `evidence.size`
  - `evidence.hashes.md5`
  - `evidence.hashes.sha1`
  - `evidence.hashes.sha256`

### `case-report`

- Output: `rapidtriage-case-report.md`, `rapidtriage-case-report.html`, `rapidtriage-case-report.docx`, `rapidtriage-case-report.pdf`, `rapidtriage-case-report.exports.json`
- Built from:
  - `rapidtriage-case.json`
  - `rapidtriage-submission-manifest.json`
  - run summary metadata
- Reviewed indicator bookmarks are rendered as an IOC/Indicator review pivot section even when they are not directly hashable as standalone evidence files.
- The report draft includes case metadata, analysis scope, run steps, reviewed/report-candidate counts, evidence hashes, skipped hash rows, conclusion/opinion text, and attachment references. HTML supports quick browser review/printing; DOCX supports portable analyst/legal editing; PDF supports quick handoff; the export manifest hashes each generated report file.

## Artifact collector rows

Android APK collector rows:

- `android-apk-artifacts`
  - `android-apk`
  - `android-app-data`

Current Android APK rows carry `parser`, `parser_version`, `source_path`, `source_format`, `hashes`, `package`, `version_name`, `permissions`, `dangerous_permissions`, `components`, `component_counts`, `dex_count`, `native_library_count`, `entry_hashes`, `native_architectures`, `string_pivots`, `apk_analysis_profile`, `apk_signing_inventory`, `validation_checks`, `commercial_uplift_evidence`, `commercial_grade_ready`, `commercial_grade_blockers`, `forensic_review`, `risk_flags`, and `risk_score` under `details`. `apk_signing_inventory` hashes META-INF signature-block, signature-file, and JAR manifest entries and explicitly marks certificate-chain parsing/signing-lineage verification as not performed until trusted APK tooling is attached. #30 APK rows also emit `android_apk_deep_analysis_manifest` (`android-apk-deep-analysis-manifest-v1`) with manifest/package/permission/component inventory, DEX/native inventory, signing entry inventory, bounded string pivots, risk score/flags, explicit no-malware/no-trust-verdict status, source viewer locator, capability limits, commercial blockers, and a stable manifest hash. The same APK rows emit `android_apk_report_grade_validation_plan` (`android-apk-report-grade-validation-plan-v1`) with source APK integrity, ZIP entry inventory, manifest/package context, permission/component normalization, DEX/native pivot boundary, signing-entry inventory, source viewer locator, trusted APK tool diff, binary-manifest validation, signature-chain/lineage validation, malware-behavior known-answer corpus, and independent review evidence slots plus a stable plan hash. Android app-data export rows carry package inferred from path, file hashes, category, `android_app_data_profile`, validation checks, blockers, legal warnings, #29/#30 `commercial_uplift_evidence`, and #29/#30 `forensic_review`; file payloads and secrets are not decoded. When an app-data file is SQLite, `android_app_data_profile.sqlite_schema_inventory` opens it read-only and records table names, row counts, column names, and table family hints only; `artifact_family_matrix` classifies SMS/call/contact/browser/media/app DB candidates from path/table/column names without reading row values. #29 app-data rows also emit `android_app_data_deep_parser_manifest` (`android-app-data-deep-parser-manifest-v1`) with package/path attribution, source layout, SQLite table summaries, artifact-family matrix, redaction and secret boundary, source viewer locator, large-data caps, commercial blockers, and a stable manifest hash. The same rows emit `android_app_data_report_grade_validation_plan` (`android-app-data-report-grade-validation-plan-v1`) with source file integrity, package/path attribution, read-only SQLite inventory, artifact-family matrix, redaction proof, source viewer locator, acquisition-manifest attribution, trusted Android export diff, app-schema fixture, encrypted/deleted-store corpus, and independent review evidence slots plus a stable plan hash.

Mobile export collector rows:

- `mobile-export-artifacts`
  - `mobile-message`
  - `mobile-contact`
  - `mobile-call`
  - `mobile-app`
  - `mobile-file`
  - `mobile-account`
  - `mobile-media`
  - `mobile-browser`
  - `mobile-chat-database`
  - `mobile-correlation-summary`
  - `mobile-export-source`
  - `ios-backup-file`
  - `ios-backup-source`
  - `ios-backup-metadata`
  - `ios-keychain-inventory`

Current mobile export rows carry `parser`, `parser_version`, `source_path`, `source_format`, `source_tool`, `source_index`, `source_record_id`, `source_hashes`, normalized timestamps, message service family, conversation IDs/titles, participants, message IDs, text/text hash, media references, reactions, contact/call fields, app package/version fields, file path/hash fields, account identifiers and hashes, media/browser pivots, `validation_checks`, `commercial_uplift_evidence`, `commercial_grade_ready`, `commercial_grade_blockers`, `forensic_review`, `legal_warning`, `risk_flags`, and the original normalized row under `raw`. Source summary rows add `mobile_export_source_profile` with input/emitted/unclassified row counts, common key samples, row-cap status, vendor-tool hint status, schema-registry warning, and source-level #26 review gates. Cloud export/API rows add provider strategy fields: `cloud_provider_strategy_profile` for Google/iCloud/M365 export imports, `cloud_api_collection_strategy_profile` for manifest-driven API collection, and `credential_strategy_profile` for #41 token redaction and authority boundaries. iCloud account/file/photo/mail rows add `icloud_export_report_grade_validation_plan` (`icloud-export-report-grade-validation-plan-v1`) with source hash, row citation, product pivot, source viewer, account identity, file/photo references, mail policy, Photos sidecar/EXIF/album/share, Apple export scope, original export completeness, ADP/container scope, provider diff, device association, schema, and independent-review slots. Browser sensitive-store inventory rows add `browser_secret_authority_manifest` for #42 with per-store reveal/audit slots, source viewer locators, blocked-by-default reveal state, no raw secret serialization, and a manifest hash, plus `browser_secret_report_grade_validation_plan` (`browser-secret-report-grade-validation-plan-v1`) with redaction, manifest, lawful-authority, controlled-audit, DPAPI/keychain, browser-version, RBAC, trusted-diff, and independent-review slots. KakaoTalk, WhatsApp, Telegram, Signal, WeChat, LINE, Discord, Instagram, iMessage, Facebook Messenger, Viber, Skype, Slack, Microsoft Teams, Reddit, X/Twitter, TikTok, Snapchat, Matrix/Element, Wire, Threema, Session, and Wickr authorized export rows are normalized as service-labeled `mobile-message` candidates and add service-specific #31~#35 `chat_app_forensic_review`, `chat_app_commercial_uplift_evidence`, `chat_app_scope_profile`, `chat_app_strategy_profile`, and `chat_app_issue_matrix`; KakaoTalk rows also add `kakaotalk_compatibility_assessment` with a legacy/post-BigBang `strategy_profile`, `kakaotalk_message_review_profile` for chat-bubble review, attachment class, reaction/read-deleted-state metadata, and `kakaotalk_parser_manifest` with a stable hash, source row citation, source viewer locator, parser track matrix, message/database review state, hash-only text policy, and report blockers. KakaoTalk 25.7.2 / 2025-08-13 and later remains marked as not compatible with legacy decoding assumptions until independently validated. WhatsApp rows add `whatsapp_message_review_profile` for JID attribution, media class, crypt-key authority, quoted/read-deleted-state metadata, chat-bubble review display guidance, `whatsapp_parser_manifest` with export/msgstore parser tracks, source row citation, source viewer locator, JID/media/database review state, crypt-key authority boundary, and deleted/media blocker evidence, plus `whatsapp_report_grade_validation_plan` (`whatsapp-report-grade-validation-plan-v1`) with source export/msgstore integrity, source row citation, message/JID/media normalization, msgstore DB inventory boundary, crypt/export strategy classification, hash-only text policy, source-viewer locator, trusted WhatsApp diff, crypt authority, schema-version, deleted-row, contact/call/media, timezone/ack/read-state, attachment-byte, and independent-review evidence slots. Telegram rows add `telegram_message_review_profile` for account/dialog attribution, media/cache class, edited/deleted/secret-chat hints, local-store decryption status, cache/source-track review guidance, and `telegram_parser_manifest` with export/cache/DB parser tracks, source row citation, source viewer locator, account/dialog/media-cache review state, local-store decryption boundary, and secret/deleted/cache blocker evidence. WhatsApp/Telegram/Signal/extended messenger strategy profiles expose expected pivots, key/authority requirements, attachment/ephemeral blockers, and required trusted diff evidence before report-grade message claims. App SQLite/DB candidates emit `mobile-chat-database` inventory rows with table/column/count summaries, message-table candidates, hashes, the same chat-app review/uplift fields, and no secret/decryption output; KakaoTalk DB candidates expose `kakaotalk_database_review_profile` and `kakaotalk_parser_manifest` when detected, WhatsApp DB candidates expose `whatsapp_database_review_profile`, and Telegram DB/cache candidates expose `telegram_database_review_profile` for dialog/account/media cache boundaries. `mobile-correlation-summary` rows summarize message/media/contact/call counts, services, participants, message-media links, `mobile_timeline_correlation_profile`, `mobile_correlation_citation_manifest`, unified contact/call/SMS actor pivots, `mobile_actor_citation_manifest`, schema versions, `mobile_schema_version_manifest`, #43~#45 `forensic_review`, #43~#45 `mobile_correlation_commercial_uplift_evidence`, pass/fail validation IDs, bounded row controls, device-wide timeline blockers, and correlation readiness checks for unified mobile review. `mobile_timeline_correlation_profile` stores a bounded message/media/call event list, event cap/truncation state, missing timestamp count, resolved/unresolved media-link counts, and timezone/device-wide known-answer validation blockers. `mobile_correlation_citation_manifest` stores bounded source-row, message-media-link, and timeline-event citations with source viewer locators and hashable citation/event/link IDs so a reviewer can pivot from a summary to the underlying mobile export rows without treating the summary as device-wide proof. `mobile_actor_citation_manifest` stores bounded hash-only actor entries, message/contact/call link counts, source viewer locators, merge/split review flags, and actor diff blockers; it intentionally does not serialize raw actor values. `mobile_schema_version_manifest` stores bounded per-app/schema/event entries, linked source row refs, source viewer locators, fixture IDs, migration-matrix blockers, and release-gate state for every unvalidated app/schema combination. iOS backup rows inventory `Manifest.db` file/domain/path candidates, `ios_backup_file_profile`/`ios_backup_scope_profile`, plus Info/Status plist metadata with #27 `commercial_uplift_evidence` and #27 `forensic_review`; keychain rows inventory table names, columns, row counts, `ios_keychain_scope_profile`, redaction status, #28 `commercial_uplift_evidence`, and #28 `forensic_review` only.

Mobile correlation summary rows also carry `mobile_timeline_report_grade_validation_plan` (`mobile-timeline-report-grade-validation-plan-v1`). The plan records #43 source-row count preservation, timeline profile/citation manifest emission, timeline event citations, message-media link citations, row-cap/truncation disclosure, device-wide join, timezone/skew, attachment-byte recovery, trusted vendor/native timeline diff, known-answer corpus, independent-review slots, plan hash, and ready/blocking counts for reportability decisions.

M365/Teams/OneDrive/SharePoint/audit rows also carry `m365_export_report_grade_validation_plan` (`m365-export-report-grade-validation-plan-v1`). The plan records source hash integrity, row citation, workload pivot, source-viewer locator, Teams thread/attachment/reaction inventory, OneDrive/SharePoint file-permission and retention/version/deleted pivots, audit-operation pivot, Purview/eDiscovery scope, original package hash, Graph scope/pagination, provider-native row diff, schema tracking, independent review, and ready/blocking slot counts for reportability decisions.

Telegram rows also carry `telegram_report_grade_validation_plan` (`telegram-report-grade-validation-plan-v1`). The plan records source export/cache/database integrity, source row citation, message/account/dialog/media normalization, Telegram DB/cache inventory boundary, export/cache strategy classification, hash-only text policy, source-viewer locator, trusted Telegram export/native DB diff, local-store decryption authority workflow, export schema-version corpus, account/dialog/peer attribution corpus, secret/edited/deleted semantics corpus, cache media locality validation, and independent-review evidence slots. `core_accuracy_gates`, `chat_app_commercial_uplift_evidence`, and `messenger_export_functional_profile` include the plan hash and ready/blocking slot counts.

#44 `mobile_actor_review_profile` records the unified contact/call/SMS actor review queue, actor counts, message/call/contact counts, phone/email/name counts, merge-split review requirements, multi-identifier actor counts, review priority, and device-wide identity-resolution blockers. It is intended for analyst review triage only until trusted vendor/known-answer actor diffs and explicit merge/split decisions are attached. `mobile_actor_report_grade_validation_plan` (`mobile-actor-report-grade-validation-plan-v1`) stores ready/blocking slots for actor pivot construction, review profile emission, hash-only actor citations, source viewer locators, review queue cap disclosure, device-wide identity resolution, merge/split review history, cross-app dedupe, trusted vendor/native actor diff, known-answer corpus, and independent review.

#45 `mobile_schema_compatibility_profile` records app identifiers, schema/app versions, row counts, validated/unvalidated entry counts, release-gate entries, known-answer fixture requirements, and schema migration matrix blockers. It keeps app/schema combinations in `commercial_release_blocked` state until version-specific fixtures and migration validation are attached.

#45 `mobile_schema_report_grade_validation_plan` (`mobile-schema-report-grade-validation-plan-v1`) records the schema plan hash, six internally ready slots, six external-required slots, and report guidance that blocks release-safe parser claims until fixture corpora, migration matrices, trusted diffs, release approval, upgrade/deleted-state corpora, and independent review are attached.

#26 mobile vendor export source rows can include `vendor_export_manifest_profile` when a sidecar `.export-metadata.json`, `.manifest.json`, `.export-manifest.json`, `export-metadata.json`, or `export_manifest.json` is present. This profile records vendor tool/version, parser/schema version, source export SHA256 match, original acquisition hash presence, export settings presence, missing required metadata, manifest SHA256, and validation status. `mobile_export_source_profile.vendor_schema_registry_profile` maps Cellebrite/XRY/GrayKey/AXIOM families to observed artifact families, expected families, sampled normalized keys, detected schema versions, and known-answer validation blockers. Source rows also emit `mobile_vendor_schema_mapper_manifest` (`mobile-vendor-schema-mapper-manifest-v1`) with #26 mapper evidence: supported vendor families, observed/expected artifact family coverage, message/contact/call/app/file/account/media/browser mapper statuses, source hash and sidecar linkage, source viewer locator, row-cap controls, commercial blockers, and a stable manifest hash for citation/review. Source rows additionally emit `mobile_vendor_export_validation_plan` (`mobile-vendor-export-report-grade-validation-plan-v1`) with rerunnable `rapidtriage manifest`, `rapidtriage artifacts`, sidecar-review, and `cross-tool-validate` command templates; evidence slots for source integrity, vendor family, sidecar linkage, vendor version/settings, original acquisition hash, schema mapper/row identity, trusted vendor diff, per-version fixtures, deleted-record corpus, and independent signoff; ready/blocking slot IDs; and a stable plan hash. It remains report-validation-blocked until the trusted vendor diff is attached, and commercial-grade blocked until external vendor-version/deleted-record/signoff evidence exists.

#27 iOS backup source rows include `ios_backup_root_profile` alongside `ios_backup_scope_profile`. The root profile records `Manifest.db`, `Info.plist`, and `Status.plist` presence/hashes, `keychain-2.db` presence/hash, manifest read status, file table status, row/domain/category counts, device name, product version, last backup date, snapshot state, full-backup flag, encrypted-backup hint, and validation status. Source rows also emit `ios_backup_deep_parser_manifest` (`ios-backup-deep-parser-manifest-v1`) with #27 evidence for required-file integrity, keychain linkage, domain/category summaries, app database candidate samples, source viewer locator, row caps, capability limits, and commercial blockers. `ios_backup_report_grade_validation_plan` (`ios-backup-report-grade-validation-plan-v1`) adds report-required evidence slots and rerunnable commands for backup-root hashing, iOS backup import, encrypted-backup authority review, and trusted iLEAPP/Cellebrite/AXIOM diff. It remains inventory-only: protected file payload decoding, encrypted backup unlock, app database semantics, and deleted-record recovery still require authority and known-answer validation.

#28 iOS keychain rows remain redacted inventory artifacts. `ios_keychain_scope_profile` now records table classes, sensitive column names, protected-value column counts, redaction policy, and inventory validation status; `ios_keychain_authority_gate` records that secret reveal is blocked by default, requires lawful authority, requires audit before reveal, and requires specialized keybag/protected-data validation. Rows also emit `ios_keychain_deep_inventory_manifest` (`ios-keychain-deep-inventory-manifest-v1`) with table inventory, sensitive/protected column counts, source viewer locator, redaction policy, authority gate, capability statement, commercial blockers, and a stable manifest hash. `ios_keychain_report_grade_validation_plan` (`ios-keychain-report-grade-validation-plan-v1`) adds report-required evidence slots and rerunnable commands for keychain DB hashing, redacted import regeneration, controlled reveal authority review, and trusted keychain inventory diff. RapidTriage does not emit passwords, tokens, certificates, private keys, or protected values from keychain tables.

#34 Signal rows remain authority-gated inventory/review artifacts. `signal_message_review_profile` records thread/recipient attribution, attachment class, SQLCipher key authority status, disappearing/deleted-state boundaries, and chat-bubble review display guidance. `signal_database_review_profile` records message, recipient/thread, attachment, SQLCipher, disappearing-message, and deleted-row validation boundaries without decrypting Signal stores or exposing secret values. `signal_report_grade_validation_plan` (`signal-report-grade-validation-plan-v1`) records source export/SQLCipher integrity, source row citation, thread/recipient/message/attachment normalization, Signal DB/SQLCipher inventory boundaries, SQLCipher strategy classification, hash-only text policy, source-viewer locator, trusted Signal export/native DB diff, lawful SQLCipher key authority workflow, recipient/thread schema corpus, attachment locality validation, deleted/disappearing semantics, delivery/read-state semantics, and independent-review evidence slots plus ready/blocking counts.

#35 WeChat/LINE/Discord/Instagram rows add `extended_messenger_message_review_profile` for service attribution, thread/channel/room pivots, actor/recipient attribution, attachment class, reaction/read/edit/delete/ephemeral hints, and chat-bubble metadata-collapsed display guidance. Matching SQLite/cache candidates can add `extended_messenger_database_review_profile` with message, actor/thread, attachment, and state-semantics table candidates while remaining schema/row-count inventory only.

#35 extended messenger rows also add `extended_messenger_parser_manifest` with a stable manifest hash, row citation, source viewer locator, service source track, message/database review state, metadata-collapsed virtualized viewer guidance, and explicit blockers for trusted export/native DB diff, media locality, reaction/read/edit/delete state, and ephemeral/encrypted store validation.

#35 extended messenger rows also carry `extended_messenger_report_grade_validation_plan` (`extended-messenger-report-grade-validation-plan-v1`) with source export/native row integrity, service row citation, message/media/reaction normalization, database schema inventory boundary, service source-track classification, hash-only text policy, source-viewer locator, trusted extended messenger export/native DB diff, service schema-version corpus, encrypted/ephemeral authority workflow, media locality validation, reaction/read/edit/delete semantics, service coverage matrix, and independent-review evidence slots plus ready/blocking counts.

#36 email rows add `email_mailbox_parser_manifest` with a stable manifest hash, message/mailbox row citation, source viewer locator, thread/participant profile, attachment metadata boundary, bounded PST/OST/MSG candidate inventory state, metadata-collapsed viewer guidance, and blockers for trusted mailbox diff, native MAPI decoding, deleted-item recovery, privilege review, and mailbox known-answer validation. Message rows also expose `email_thread_profile` with normalized subject, participants, Message-ID/References/In-Reply-To pivots, and UTC-normalized date when parseable.

#36 email rows also carry `email_report_grade_validation_plan` (`email-report-grade-validation-plan-v1`) with source mailbox hash integrity, message/mailbox row citation, header/body/attachment inventory, thread/participant profile, bounded MAPI inventory boundary, source-viewer locator, hash-only body policy, attachment source locator, trusted mailbox export/native diff, native MAPI decoding or trusted export, deleted-item recovery corpus, thread/dedup/timezone corpus, attachment-byte validation, auth/signature/crypto validation, privilege/scope review, and independent-review evidence slots plus ready/blocking counts.

#37 Google Takeout/Gmail rows add `google_takeout_parser_manifest` with a stable manifest hash, product family, row citation, source viewer locator, row pivots, sidecar/timezone/provider-diff status, metadata-collapsed product-matrix viewer guidance, and blockers for selected-products scope, original archive hash, provider-native diff, and sidecar merge validation. Google rows also add `google_takeout_report_grade_validation_plan` with source hash, row citation, product pivot, source-viewer locator, Gmail thread/body policy, Drive/Photos sidecar, Location/Activity timezone/source, selected-products scope, original archive completeness, provider-native row diff, duplicate/thread/timezone corpus, deleted/retention/sharing semantics, schema-version tracking, independent-review slots, and ready/blocking counts. ZIP provider exports emit `cloud-export-archive` rows with `source_format=zip`, `cloud_archive_manifest`, `cloud_archive_manifest_hash`, bounded entry manifests, provider/product/suffix counts, JSON/CSV entry counts, original archive SHA256 linkage, archive viewer locators, and explicit selected-products, split-archive, known-answer, and provider-diff blockers before report-grade archive-scope claims. Small Gmail Takeout `.mbox` ZIP entries can also emit `cloud-mail` rows with `source_format=zip-mbox-entry`, archive entry name/index/CRC32, message index, body hash, Google row citation pivots, and the source archive hash. These MBOX rows now preserve `gmail_thread_profile`, `thread_root_id`, `thread_parent_id`, and `normalized_thread_subject` from `Message-ID`, `In-Reply-To`, and `References` headers so the viewer/report flow can group reply chains without claiming provider-native Gmail conversation completeness. Bounded JSON and CSV ZIP entries can emit searchable `cloud-mail`, `cloud-file`, `cloud-location`, `cloud-activity`, `cloud-message`, or `cloud-audit` rows with `source_format=zip-json-entry` or `source_format=zip-csv-entry`, `archive_entry_name`, `archive_entry_index`, `archive_entry_crc32`, `archive_entry_size`, `archive_entry_modified_at`, and `archive_json_row_index` or `archive_csv_row_index`; those rows remain bounded triage pivots until provider scope, full archive pagination, and trusted provider diffs are attached. ChatGPT/Claude/Gemini/Perplexity-style service export JSON/JSONL files under a cloud export root, including bounded JSON/JSONL entries inside service export ZIP archives and `Takeout.zip`-style archives with AI-named entries, can emit `ai-service-export-conversation` rows with `source_format=json`, `jsonl`, or `zip-json-entry`, `ai_service_export_parser_manifest`, `conversation_candidates`, `transcript_pairs`, `source_json_pointer`, `text_sha256`, `row_citation_hash`, `export_schema_profile`, archive entry context where applicable, and provider-scope/export-completeness/trusted-diff blockers. Standalone cloud CSV rows use `source_format=csv`, `csv_row_index`, parser manifests, and the same provider validation blockers.

Email artifact rows:

- `email-artifacts`
  - `email-message`
  - `email-mailbox`

Current email rows carry `parser`, `parser_version`, `source_path`, `source_format`, `source_hashes`, message IDs, thread parent IDs, subject/from/to/cc/date headers, body previews and hashes, attachment filename/content-type/size/hash metadata, mailbox candidate strings/email/subject pivots for PST/OST/MSG, `email_format_profile`, `email_mailbox_strategy_profile`, `email_issue_matrix`, `validation_checks`, `commercial_uplift_evidence`, `commercial_grade_ready=false`, `commercial_grade_blockers`, legal warnings, and risk flags. EML, EMLX, MBOX, and Maildir rows are parsed as triage messages; PST/OST/MSG rows are inventory candidates only, not full native mailbox decoding, and their strategy profile records the required libpff/readpst/Outlook/Purview-style trusted export diff before folder, deleted-item, attachment, or threading conclusions.

#36 PST/OST/MSG rows also include `mapi_container_review_profile` when bounded scans find candidate mailbox strings. The profile records scan-window bounds, email/subject/string counts, folder-path hints, message-class hints, attachment-name hints, deleted-item hints, native decode status, folder/deleted/attachment/threading limitations, and external validation tools required before report-grade mailbox claims.

`email-external-parse` output uses `email-external-parser-wrapper-v2`. The wrapper JSON includes `tool_availability_matrix`, `selected_tool`, `execution.command_argv_sha256`, stdout/stderr hashes and bounded tails, export file hashes and source-viewer locators, `validation_checks`, `forensic_review.review_profile=email_external_parser_review_profile`, `commercial_uplift_evidence`, and `evidence_manifest.manifest_version=email-external-parser-evidence-manifest-v1`. It also emits `export_review_profile` with `profile_version=email-external-export-review-profile-v1`, a stable `profile_sha256`, exported EML/MBOX message candidates, header/body-preview hashes, attachment counts, folder candidates, row hashes, metadata-collapsed review tabs, large-data caps, and trusted-diff/known-answer/deleted-item blockers. This output is validation evidence only: `commercial_grade_ready` stays false until an independent trusted diff, known-answer mailbox corpus, and deleted-item/threading validation are attached.

#37 Google export rows add `google_takeout_review_profile` for product-family classification across Gmail, Drive, Photos, My Activity, Location History, and account-style rows. It records expected/present pivots, Takeout path hints, sidecar merge status, original archive hash status, timezone/deleted-retention validation status, and provider-native diff requirements before report-grade Google Takeout claims. Collaboration SaaS message exports such as Slack/Discord/Dropbox/Box/Zoom/Notion/Atlassian/GitHub add `collaboration_message_review_profile` with workspace/channel/thread/message pivots, attachment inventory, reaction summary, actor hashes, edited/deleted hints, metadata-collapsed review defaults, and workspace/provider-diff blockers.

#38 Apple/iCloud export rows add `icloud_export_review_profile` for account, iCloud Drive, iCloud Photos, iCloud Mail, and device-association style rows. It records expected/present pivots, Apple/iCloud path hints, Advanced Data Protection status, shared-album semantics, photo sidecar/EXIF merge status, third-party container visibility, original export hash status, and provider-native diff requirements before report-grade iCloud claims. iCloud rows also add `icloud_export_parser_manifest` with a stable manifest hash, product family, row citation, source viewer locator, account/file/photo/mail pivots, ADP/shared-album/third-party-container state, sidecar/EXIF status, metadata-collapsed product-matrix viewer guidance, and blockers for Apple export scope, original archive hash, provider-native diff, and photo sidecar validation.

#39 Microsoft 365 export rows add `m365_export_review_profile` for Exchange, Teams, OneDrive/SharePoint, audit, and tenant-account style rows. It records expected/present pivots, Purview/eDiscovery/Graph source hints, Teams compliance-record status, SharePoint permission graph status, retention/hold policy status, deleted/version-history status, and provider-native diff requirements before report-grade Microsoft 365 claims. Teams message rows also add `teams_message_review_profile`, `teams_attachment_review_profile`, and `teams_reaction_review_profile` with reply/root pivots, edited/deleted hints, attachment counts/names/URL hashes, reaction counts/types, and reaction actor hashes for metadata-collapsed chat review. OneDrive/SharePoint file rows add `m365_file_permission_review_profile` and `m365_file_state_review_profile` with permission counts, permission roles, principal/link hashes, version IDs/counts, retention labels, and deleted-state hints. M365 rows also add `m365_export_parser_manifest` with a stable manifest hash, workload family, row citation, source viewer locator, Teams/OneDrive/SharePoint/Audit pivots, Graph scope, Teams compliance, SharePoint permission graph, retention/deleted/version state, metadata-collapsed workload viewer guidance, and blockers for Purview/eDiscovery scope, original package hash, Teams compliance reconciliation, permission graph, retention validation, and provider-native diff.

#40 Cloud API collection payloads add `cloud_api_provider_scope_profile` and per-request `request_acquisition_profile`. These record provider/account/scope hashes, legal authority presence, retry/backoff configuration, pagination/delta declarations, endpoint path templates, bounded response controls, redacted credential handling, and provider-native diff requirements. Request rows also add `cloud_api_response_parser_manifest` with stable manifest hash, response row citation, source viewer locator, response sidecar path/SHA256/size, no-body-in-metadata control, metadata-collapsed viewer guidance, and provider-diff/pagination/legal-hold blockers. Case output also adds `cloud_api_report_grade_validation_plan` (`cloud-api-report-grade-validation-plan-v1`) with stable plan hash, source manifest hash, acquisition manifest, request profile inventory, response hash/sidecar boundary, response parser manifest, credential redaction, provider scope, OAuth consent/legal authority, OAuth/device-flow, pagination/delta, retry/throttle/backoff, provider-native diff, legal-hold export package, schema-version, and independent-review slots plus ready/blocking counts. #41 credential payloads now also add `credential_authority_profile`, `credential_authority_manifest`, and `credential_report_grade_validation_plan` (`cloud-credential-report-grade-validation-plan-v1`) under `credential_handling`; they link the manifest provider/scope profile to token handling, record controlled-reveal default denial, sensitive header names, no raw token/hash output, hash-only OAuth consent/legal/vault/rotation record fingerprints, vault integration status, token rotation/revocation audit status, and stable ready/blocking slots for redaction, no-secret serialization, storage boundary, authority manifest, scope, legal authority, OAuth consent, vault/rotation records, enterprise vault integration, controlled reveal, RBAC, rotation enforcement, authority-audit diff, and independent review. Pagination/delta declarations are captured as acquisition metadata, but provider-complete OAuth/device-flow, pagination execution, legal hold, enterprise vault, and known-answer validation remain blockers.

Media image collector rows:

- `media-image-artifacts`
  - `media-image`
  - `media-image-unreadable`

Current media image rows carry `parser`, `parser_version`, `source_path`, `source_format`, `hashes`, `width`, `height`, `channel_count`, `perceptual_hash`, `similarity_bucket`, `thumbnail_preview`, `gallery_review_mode`, `ocr_candidate`, `ocr_plan`, `translation_plan`, `ocr_queue_assessment`, `korean_ocr_translation_workflow`, `korean_ocr_translation_report_grade_validation_plan`, `classifier_validation`, `media_native_capabilities`, `media_report_grade_assessment`, and rule-based `visual_classification` under `details` when decoding succeeds. If OCR or translation sidecars are present, `ocr_sidecar` and `translation_sidecar` record source path/size/SHA256, bounded text, text SHA256, language hints or target language, Korean detection/quality metrics, and truncation status. Source-viewer `/source-ocr-translation` packages expose a side-by-side review array for original OCR text and translated text, each with sidecar path/name/hash, text hash, language hint, quality metrics, truncation state, and bounded optional text, plus a `source-ocr-translation-review-manifest-v1` with row hashes and source locator and a `source_ocr_translation_report_grade_validation_plan` with ready/blocking slots for report reliance.

Memory Volatility collector rows:

- `memory-volatility-artifacts`
  - `memory-process`
  - `memory-cmdline`
  - `memory-network`
  - `memory-malfind`
  - `memory-artifact`
  - `memory-dump-indicators`

Current memory rows carry `parser`, `parser_version`, `source_path`, `source_plugin`, `source_index`, `source_hashes`, normalized process fields, process/parent keys, command-line indicators, network endpoints, reconstruction/validation status, `risk_flags`, `risk_score`, and the original row under `raw`. Direct memory dump indicator rows carry `coverage_status`, `source_size`, `scan_limit_bytes`, `scan_ranges`, `scan_truncated`, `indicator_pivots`, redacted BitLocker candidate hashes with group-level checksum validation, process string candidates, and triage recommendations.

Cloud export collector rows:

- `cloud-export-artifacts`
  - `cloud-location`
  - `cloud-activity`
  - `cloud-account`
  - `cloud-mail`
  - `cloud-file`
  - `cloud-message`
  - `cloud-audit`
  - `cloud-export-archive`

Current cloud rows carry `parser`, `parser_version`, `source_path`, `source_format`, `source_index`, `source_hashes`, `commercial_grade_ready=false`, `service`, `event_type`, normalized timestamps, activity titles/products, account profile fields, location latitude/longitude/accuracy, mail subject/from/to/message/body-preview fields, file ID/name/mime/owner/URL fields, OneDrive/SharePoint permission/version/deleted-state review pivots, Teams/chat message IDs and text hashes, Teams reply/attachment/reaction review pivots, collaboration-SaaS workspace/channel/thread/message pivots plus attachment/reaction review profile fields, audit operation/actor/IP/user-agent fields, `validation_checks`, `commercial_grade_blockers`, `risk_flags`, and the original row under `raw`. ZIP archive mail rows additionally carry `archive_entry_name`, `archive_entry_index`, `archive_entry_crc32`, `archive_entry_size`, `archive_entry_modified_at`, and `archive_message_index`; ZIP JSON/CSV rows carry `archive_json_row_index` or `archive_csv_row_index`, and standalone CSV rows carry `csv_row_index` so the viewer/report flow can cite the exact member or row without extracting the archive.

### `cloud-collect`

- Output: `rapidtriage-cloud-collect.json` or `rapidtriage-cloud-collect.dry-run.json`
- Sidecar audit: `rapidtriage-cloud-collect.audit.json`
- Response folder: `responses/`

Current cloud API collection manifests carry `command`, `generated_at`, `manifest_path`, `manifest_sha256`, `output_dir`, `responses_dir`, `summary`, `credential_handling`, `requests`, `skipped`, and `import_guidance`. Request rows carry `name`, `service`, `method`, `url`, `url_sha256`, redacted request headers, per-request `credential_handling`, response status/content type/path/size/SHA256, truncation status, timestamps, and error fields when a request fails. Request rows also carry `cloud_api_response_parser_manifest`, whose `source_viewer_locator` points to the raw response sidecar while the report JSON keeps body bytes out of metadata; `row_citation` records citation ID, source SHA256, URL SHA256, status, path, and parser version. Case-level #40 output now carries `cloud_api_report_grade_validation_plan`, and `summary` records its hash plus ready/blocking slot counts so UI/report builders can show what is already reviewable and what still blocks provider-complete claims. Bearer tokens and API keys are redacted from saved metadata and `tokens_written_to_output=false` is recorded. Case-level `credential_handling` includes #41 `credential_strategy_profile`, `credential_authority_profile`, `credential_authority_manifest`, `credential_report_grade_validation_plan`, `credential_security_assessment`, `core_accuracy_gates`, and `commercial_uplift_evidence` so reviewers can distinguish usable redacted triage from unimplemented enterprise-vault/authority-audit handling. `credential_authority_manifest` records authority evidence as booleans and SHA256 fingerprints only; raw secret values, token values, raw consent IDs, raw vault IDs, and raw rotation IDs are not serialized into output metadata. `credential_report_grade_validation_plan` records the credential plan hash and ready/blocking slot counts for redaction, no-secret serialization, storage boundary, authority/scope/consent/legal evidence, vault and rotation records, reveal/RBAC enforcement, trusted authority diff, and independent review.

## Windows artifact collector rows

Execution rows in this sample include validation-gated row manifests for core Windows execution pivots. #9 `bam-entry` rows now carry `bam_dam_row_manifest` and `bam_dam_row_manifest_hash`, preserving ControlSet/source key/source offset, SID, device path, timestamp semantics, required trusted-diff field coverage, and `standalone_execution_proof=false` for GUI/report consumers.

The `manifest-windows-artifacts.json` sample also fixes the current artifact row shape for:

- `windows-browser-artifacts`
  - `browser-history-downloads`
  - `browser-history`
- `windows-recent-files`
  - `recent-shortcut`
  - `jumplist-automatic`
  - `jumplist-custom`
- `windows-os-account`
  - `windows-user-profile`
  - `windows-sam-account-candidate`
  - `windows-sam-group-candidate`
  - `windows-service-config`
  - `windows-mounted-device`
  - `windows-lsa-policy-location`
  - `windows-privilege-assignment`
  - `windows-group-membership`
  - `windows-account-lifecycle`
  - `windows-os-account-summary`
- `windows-execution`
  - `amcache-hive`
  - `amcache-entry`
  - `shimcache-entry`
  - `userassist-entry`
  - `bam-entry`
  - `powershell-history-command`
  - `srum-database-file`
  - `srum-database-pivot`
  - `srum-table-candidate`
  - `srum-row-candidate`
  - `srum-network-usage`
  - `srum-app-resource-usage`
- `windows-filesystem`
  - `mft-record`
  - `mft-file`
  - `usn-record`
  - `usn-journal-file`
- `windows-search-index`
  - `windows-search-index-entry`
  - `windows-search-edb-file`
  - `windows-search-edb-pivot`
  - `windows-search-edb-table-candidate`
  - `windows-search-index-summary`
- `windows-remote-access`
  - `rdp-config`
  - `rdp-cache-file`
  - `rdp-destination`
- `windows-eventlog`
  - `eventlog-event`
  - `eventlog-detection`
  - `eventlog-file`
  - `eventlog-chunk`
  - `eventlog-record-candidate`
  - `eventlog-summary`
- `windows-registry`
  - `registry-hive`
  - `registry-hive-cell`
  - `registry-key-tree-node`
  - `registry-key-recovery-candidate`
  - `registry-deleted-cell-candidate`
  - `registry-value-recovery-candidate`
  - `registry-hive-strings`
  - `registry-key`
  - `registry-run-key`
  - `registry-usb`
  - `registry-summary`
- `windows-shellbags`
  - `shellbag-key`
  - `shellbag-native-candidate`
- `windows-prefetch`
  - `prefetch-file`
  - `prefetch-reference`
- `windows-system`
  - `task-scheduler-task`
  - `defender-policy-artifact`
  - `defender-support-log`
  - `firewall-log`
  - `wer-report`
  - `wmi-repository-file`
  - `zone-identifier`
- `macos-system`
  - `macos-user-profile`
  - `macos-browser-history-downloads`
  - `macos-quarantine-event`
  - `macos-tcc-permission`
  - `macos-launch-agent`
  - `macos-unified-log-file`
  - `macos-spotlight-store`
  - `macos-fsevents-file`
  - `macos-apfs-snapshot-hint`
  - `ai-service-export-conversation`
- `linux-system`
  - `linux-user-profile`
  - `linux-shell-history`
  - `linux-ssh-authorized-key`
  - `linux-ssh-known-host`
  - `linux-auth-log-event`
  - `linux-auditd-event`
  - `linux-package`
  - `linux-package-event`
  - `linux-container-config`
  - `linux-cron-entry`
  - `linux-systemd-service`

Current browser rows carry `user`, `browser`, `profile`, `source_profile`, `history_count`, `download_count`, `history`, `downloads`, `internet_usage`, `ai_usage`, `browser_storage_inventory`, `browser_storage_review_profile`, `unified_timeline`, `browser_timeline_integrity_profile`, `browser_validation_checks`, `commercial_uplift_evidence`, `forensic_review`, `privacy_legal_warning`, `commercial_grade_ready=false`, and explicit `commercial_grade_blockers` under `details`. Chromium-family storage inventory rows (`browser-storage-inventory` and `macos-browser-storage-inventory`) use `coverage_status=inventory-candidate` and expose cache/session/extension/sync/cookie/credential/local-storage/IndexedDB presence, file counts, total bytes, bounded sample hashes, sensitivity flags, storage review priority/truncation/sample-hash counts, per-store `source_context_profile` metadata, `source_context_profile_hash`, #19 `commercial_uplift_evidence`, #19 `forensic_review`, #42 `browser_secret_authority_profile`, #42 `browser_secret_report_grade_validation_plan`, #42 `secret_handling_forensic_review`, #42 `secret_handling_commercial_uplift_evidence`, pass/fail legal-gate controls, and legal warnings without decrypting or extracting raw secrets. `source_context_profile` is metadata-first and redacted by default: it can expose extension manifest names/versions/permission counts, cache HTTP/content-type/URL signatures, SQLite header/schema inventories, session/LevelDB key samples, viewer guidance, and reportability blockers without serializing raw cookie/password/session values. `browser_secret_authority_profile` records sensitive store types/names, controlled-reveal default denial, no raw reveal, DPAPI/keychain and browser-version validation blockers, legal-authority requirement, reveal-audit requirement, and trusted secret-authority diff requirement. `browser_secret_report_grade_validation_plan` records ready/blocking slots and a stable hash for redaction, no raw serialization, authority manifest/source-viewer coverage, lawful authority, reveal audit, DPAPI/keychain corpus, browser-version corpus, RBAC reveal enforcement, trusted authority diff, and independent review. #19 browser storage trusted-diff helpers accept either flat trusted exports or generated rows with nested `details.storage_inventory`, then compare storage type/name, relative path, artifact hint, file count, total bytes, file/dir status, sensitivity, truncation, and sample hashes. #42 browser secret trusted-diff helpers now also accept generated storage-inventory rows with nested `details.storage_inventory`, expanding sensitive stores and propagating legal-authority/audit IDs before comparing against native-store, DPAPI/keychain, legal-authority, or audit-log references. #20 browser timeline trusted-diff helpers accept flat trusted exports or generated rows with nested `details.unified_timeline`, then compare visit/download row type, timestamp, URL, title/domain, transition, counts, download target/bytes/state, AI-service label, and source table/index. AI usage/conversation rows expose service-labeled usage and question/answer candidates, source storage kind, profile-relative path, source size/mtime/hash, offset candidates, transcript pair counts, completeness score, orphan counts, pair-level source citation, pairing confidence summary, source storage summary, validation checks, #21 `commercial_uplift_evidence`, #21 `forensic_review`, legal warnings, and `commercial_grade_ready=false`. Service-export ZIP rows use `source_format=zip-json-entry`, `coverage_status=service-export-zip-json-candidate`, `archive_entry_name`, `archive_entry_crc32`, `archive_entry_size`, `archive_entry_modified_at`, and `conversation_candidates[].source_path` in `archive.zip::entry` form so a reviewer can preserve archive-level hashes while inspecting the embedded JSON/JSONL source. #21 AI transcript trusted-diff helpers accept flat service exports, nested `details.transcript_pairs`, `details.transcript.pairs`, and nested `details.conversation_rows` that can be re-paired before comparing service, question, answer, source hashes, source paths/offsets, storage area, pair confidence, and pair IDs.

Current recent/jumplist rows carry `user`, `entry_name`, `entry_hint`, `size`, `modified_at`, and source SHA256 under `details`. Shortcut rows additionally expose `lnk_parse_status`, `target_path`, `embedded_paths`, `link_flag_names`, target timestamps, `working_dir`, `command_line_arguments`, `tracker_data`, bounded `property_store_blocks`, `lnk_target_context_citation_profile`, `lnk_target_context_citation_profile_hash`, and #17 `commercial_uplift_evidence` when the Shell Link header/string data can be decoded. The target-context citation profile binds ShellLinkHeader field offsets, StringData/LinkInfo target context, ExtraData block bounds, tracker identifiers, target identity hash, source viewer locator, and reportability blockers so GUI/report views can cite the exact shortcut source context without treating it as complete user-attribution proof. Jump List rows expose `jump_list_parse_status`, `container_hint`, `jumplist_kind`, `application_id_hash`, `ole_parse_status`, `ole_stream_count`, bounded `ole_streams` with stream SHA256, embedded path pivots, `destination_count`, `destination_stream_count`, and recoverable `destinations` with Shell Link offsets, stream provenance, stream hashes, target paths, working directories, target timestamps, command-line arguments, and link flag names. Automatic Jump List rows additionally expose bounded DestList metadata candidates when the `DestList` stream is recoverable: `destlist_parse_status`, `destlist_streams`, `destlist_header_candidates`, `destlist_entry_candidates`, `destlist_validation_checks`, parser confidence, validation guidance, and `commercial_grade_blockers` for OS-version-specific field validation, deleted-entry recovery, account attribution, and AppID mapping gaps.

Current Windows parser rows include source validation fields where available: `parser`, `parser_version`, `coverage_status`, `reportability`, `source_path`, `source_format`, `source_hashes`, `parser_confidence`, `evidence_strength`, and a bounded `raw_preview`. Validation-gated #6~#45 rows also use shared review objects with `gap_id`, `artifact_goal`, `review_status`, `report_grade_ready`, `validation_required`, `primary_evidence`, `blockers`, `caveats`, and `next_validation_step`. EventLog XML/JSON/JSONL/CSV imports emit normalized event/detection fields and native EVTX rows expose BinXML/message/recovery evidence plus `commercial_uplift_evidence` for #1~#3 source refs, validation matrix pass/fail IDs, commercial blockers, and large-data controls; EVTX trusted-diff helpers accept both flat trusted-tool exports and native RapidTriage artifact rows with nested `details`, so generated rows can be diffed directly without hand-flattening. OS/account rows emit account lifecycle rows with `account_security_context`, `forensic_review`, and #6 `commercial_uplift_evidence` for SAM/SECURITY/SYSTEM validation matrices, source refs, redaction state, and transaction-log blockers. Execution rows emit Amcache/ShimCache/BAM/SRUM evidence summaries plus `forensic_review` and #7~#10 `commercial_uplift_evidence` for schema/native-layout blockers, bounded Amcache/SRUM scans, row-level decode requirements, and pass/fail validation IDs; Amcache rows additionally include `amcache_schema_version_profile`, `amcache_row_manifest`, and `amcache_row_manifest_hash`; the execution trusted-diff helper accepts nested RapidTriage Amcache/ShimCache/BAM-DAM/SRUM rows and flat AmcacheParser/AppCompatCacheParser/SrumECmd/RECmd-style rows, comparing normalized path, device path, timestamp, timestamp source, SHA1, program name, publisher, file description, product name, source format, source key, source offset, cache order, OS build, user/user SID, SRUM table family, URL, network profile, interface LUID, individual SRUM counters, counter hash, and caveat text, while blocking Amcache commercial evidence if required path/SHA1/caveat field coverage is missing. Windows Search, NTFS, JumpList, and ShellBags rows expose #11~#15 `commercial_uplift_evidence`: Windows.edb candidates carry page-map bounds and row/deleted-state blockers; filesystem rows emit native `$MFT`/`$J` rows with `mft_record_evidence`, bounded nonresident runlist preview evidence, resident data hashes, `usn_record_evidence`, v2/v3 cursor evidence, v4 extent preview evidence, scan-limit evidence, `forensic_review`, and `ntfs_report_citation_manifest` for stable source hash, offset/cursor, viewer entrypoints, pass/fail validation IDs, and reportability blockers; `$LogFile` rows expose `ntfs_logfile_page_profile`, `transaction_operation_hints`, `transaction_operation_profile`, and `timeline_join_hints` for bounded transaction-page/path-operation review while keeping full redo/undo decode blocked; JumpList rows carry DestList/OLE/LNK stream provenance, unlinked/deleted review-only entry candidates, and deleted-entry/AppID blockers; ShellBags rows carry cell/hbin offsets, allocation state, bounded hive scan limits, and shell-item/transaction-log blockers. Prefetch, LNK, Windows system, and browser rows now expose #16~#20 `commercial_uplift_evidence` with Prefetch scan/version limits, LNK target-context citation profile controls plus property-store blockers and review candidates, Task temporal/correlation blockers, Defender/Firewall/WER/WMI source-citation line/offset profiles plus correlation blockers, and browser storage/timeline/secret-redaction bounds. Current-file search responses now include SQLite continuation fields (`sqlite_resume_token`, `sqlite_resume_token_hash`, `sqlite_resume_requested`, and row-level source viewer offsets) when result limits or row caps require continuing from the next table row, plus large non-document file continuation fields (`file_resume_token`, `file_resume_token_hash`, `file_scan_start_offset`, `file_scan_end_offset`, and `file_resume_requested`) when a log/binary byte-window search must continue from the next byte offset. AI transcript and evidence-image/container rows now expose #21~#25 `commercial_uplift_evidence` with AI pairing/source-storage quality, pair-level source citation, E01 segment-set profiles, RAW split-set profiles, VM chain-risk profiles, container export-first profiles, validation matrix IDs, source/hash refs, bounded hash/tool/command counts, and explicit native-parser or external-corpus blockers. #22 E01 trusted-diff helpers accept generated result dictionaries with nested `source_integrity`, `partition_selection`, `segment_set_profile`, and `recovered_root_manifest.files`, then compare source hash, selected sector, workflow, EWF segment count/order/contiguity, recovered file counts, and recovered file hashes against ewfverify/Sleuth Kit/vendor manifests. Mobile export, iOS backup/keychain, Android app-data, and APK rows now expose #26~#30 `commercial_uplift_evidence` with vendor/source hash refs, mobile/Android validation matrix IDs, row/file/string-scan caps, protected-data redaction, and known-answer corpus blockers. Chat app rows now expose #31~#35 `chat_app_commercial_uplift_evidence` with service profile/source refs, issue-matrix pass/fail IDs, table/sample caps, encrypted-store/deleted-record blockers, and service-specific known-answer requirements; KakaoTalk rows additionally emit `kakaotalk_report_grade_validation_plan` (`kakaotalk-report-grade-validation-plan-v1`) with source export/native DB integrity, source row citation, message pivot normalization, DB inventory boundary, BigBang compatibility, hash-only text policy, source viewer locator, trusted-diff, post-BigBang corpus, schema map, encrypted-store authority, deleted/read-state, attachment-byte, and independent-review evidence slots. Email/cloud rows expose #36~#40 `commercial_uplift_evidence`, and cloud credential, browser secret, and mobile correlation/schema rows now expose #41~#45 uplift evidence with credential redaction controls, legal-gated secret inventory controls, mobile actor/schema pass/fail IDs, row caps, and device-wide/known-answer blockers. Windows Search index rows inventory `Windows.edb` with ESE pivots, page candidates, table candidates, correlated row candidates, `edb_analysis_method`, `ese_page_map`, and `forensic_review`; `windows-search-edb-page-candidate` rows preserve page index, byte offset, page SHA256, page-local table marker hits, and page-local path/URL/content strings, and #11 trusted-diff helpers accept nested RapidTriage EDB row candidates plus WinSearchDBAnalyzer/libesedb-style rows while comparing path, URL, content, deleted state, table family, page index, page offset, page hash, and source format. #12 MFT trusted-diff helpers accept nested RapidTriage `mft-record` rows plus MFTECmd/analyzeMFT/TSK-style rows while comparing record number, sequence number, parent reference, path, deleted state, timestamps, attribute type sets, record offsets, resident data hashes, and nonresident runlist decode status. `kakaotalk-windows` rows now include `kakaotalk-windows-app-database` inventory rows for application `.edb`/`.db`/`.dat` stores with `database_role`, `profile_id_hint`, `sqlite_access`, header signature, DB/WAL/SHM/copy companion hashes, `kakaotalk_decryption_readiness`, validation warnings, and #31 `forensic_review`; `kakaotalk-windows-crypto-material-candidate` rows preserve redacted registry `DeviceInfo` context with field hashes and explicit no-decryption/no-proprietary-key status; `kakaotalk-windows-user-id-candidate` rows preserve redacted user/account identifier candidates with field names, value hashes, value shapes, offsets when available, and auto-decrypt eligibility; source-candidate rows correlate KakaoTalk evidence across `Windows.edb`, Registry, and memory dumps with `source_family`, `source_subtype`, `source_offset`, page metadata where available, `candidate_kind`, matched terms, privacy warnings, and #31 `forensic_review`. Recent-file rows expose LNK #17 target-context citation profiles and JumpList #14 `forensic_review`, Windows-system rows expose `system_source_citation_profile`, while ShellBags rows expose `shellbag_evidence` and `forensic_review`. Registry `.reg` exports and native hive rows emit key/value/activity/recovery candidates with validation and legal/reportability guidance, and native key/recovery rows include `commercial_uplift_evidence` for #4~#5 bounded scan controls, recovery profile version, validation matrix pass/fail IDs, and transaction-log/deleted-cell blockers; Registry trusted-diff helpers also accept generated artifact rows with nested `details`, so key-tree and deleted-cell candidates can be compared directly against Registry Explorer/RECmd/hand-labeled fixtures. Email, cloud export, and cloud API rows expose explicit commercial-readiness blockers plus review fields.

ShimCache #8 rows now also include `shimcache_layout_profile`, `shimcache_row_manifest`, and `shimcache_row_manifest_hash`. These schema objects bind cache-order/source-offset/path identity, source-viewer locator, layout-validation state, not-proof-of-execution semantics, and required trusted-diff coverage; missing path, cache-order/source-offset, or caveat coverage keeps ShimCache commercial evidence blocked even when row keys otherwise match.

SRUM rows for #10 now additionally expose `srum_report_citation_manifest` and `srum_report_citation_manifest_hash`. These fields preserve source format, source key/offset, table/app identity, timestamp source, individual counter values, network profile/interface LUID, row-cluster/source-offset locators, stable row hashes, required trusted-diff coverage including `source_format` and `semantics_warning`, and explicit `standalone_execution_proof=false` so GUI/report consumers can separate source-tool SRUM rows, native SRUDB inventory, and bounded string-cluster candidates.

Windows.edb rows for #11 now additionally expose `windows_edb_report_citation_manifest_hash`, `edb_semantics_warning`, and `windows_edb_trusted_diff_contract` where applicable. The manifest row identity carries path/URL/content, source format, timestamp/deleted-state semantics, page locator, field-presence profile, page table markers, and a warning that native candidates are not decoded ESE rows; the trusted-diff helper compares path, URL, content, deleted state, deleted-state semantics, timestamp semantics, table family, page index, page offset, page hash, source format, and warning text against WinSearchDBAnalyzer/libesedb-style rows.

Activities/Notifications row candidates (`activities-cache-row-candidate`, `notification-row-candidate`) are promoted from read-only SQLite timeline samples. They expose `timeline_type`, `normalized_time`, `time_parse_status`, `app_preview`, `text_preview`, `decoded_text_hint`, `profile_attribution`, `uwp_package_correlation`, `row_hash`, `source_locator`, `activity_row_review_profile`, row-level risk flags, and commercial blockers. These rows are safe for GUI search/review but remain validation-required until Windows build-specific schema semantics and deleted-state handling are proven.

`kakaotalk-decrypt` command outputs are intentionally separate from general artifact rows because they require authorization-gated key material. The payload includes `authorization_model`, redacted `auth_material`, summary counts for chat DBs/decrypt successes/SQLite opens/message rows/previews, and one entry per `chatLogs_*.edb` with source hashes, chat ID, source header, decrypt/SQLite status, decrypted SHA256 when attempted, table inventory, message-table candidates, row counts, and optional bounded `message_previews`. `auth_material` records which route was used (`direct-key-iv`, `pragma-user-id`, `pragma-user-id-candidates`, `deviceinfo-pragma-key-user-id-variants`, or `deviceinfo-pragma-key-user-id-candidates`), which DeviceInfo fields were found, redacted userId candidate hashes, whether a single userId was auto-selected, and how many candidate-derived keys were tried. Entry rows can include the non-secret matched pragma variant that produced a valid SQLite header. Secrets are never written to JSON or audit logs.

`kakaotalk-macos-report` command outputs include `summary.context_row_limit`, `summary.context_limit_reached`, `summary.context_truncated_table_count`, `summary.room_context_row_estimate`, and top-level `context_row_coverage[]` rows. Each coverage row records source database path/hash, source table, total row count, exported row count, row limit, truncation flag, and status. The HTML viewer mirrors the same warning so large Mac KakaoTalk room/user context tables cannot be silently capped.

#23 RAW/split trusted-diff helpers accept generated result dictionaries with nested `source_integrity[]`, `split_set_profile`, and `recovered_root_manifest.files`, then compare selected source-part hash, selected sector, workflow, split part count/order/contiguity, recovered file counts, and recovered file hashes against Sleuth Kit/vendor manifest exports.
- `raw-split-report-grade-validation-plan-v1` appears in RAW/split `evidence` output as `raw_split_validation_plan` and in extraction metadata as `report_grade_validation_plan`. It records executable validation commands for per-part hashing, preflight, `mmls`, `fsstat`, `tsk_recover`, and `image-workflow-validate --item-number 23`, plus evidence slots for split inventory, per-part hashes, gap/order review, partition/fsstat selection, recovery provenance, trusted recovered-file diff, damaged/gapped corpus, and encrypted-volume handoff.

#24 virtual-disk trusted-diff helpers accept generated result dictionaries with nested `raw_extraction.recovered_root_manifest.files`, `converted_raw_integrity`, `virtual_disk_chain_profile`, and `qemu_img_info_profile`, then compare source hash, converted RAW hash, selected sector, workflow, virtual format, chain review status, snapshot/differencing flag, parent-chain status, recovered file counts, and recovered file hashes against qemu-img/Sleuth Kit/vendor manifest references.
- `virtual-disk-report-grade-validation-plan-v1` appears in virtual-disk `evidence` output as `virtual_disk_validation_plan` and in extraction metadata as `report_grade_validation_plan`. It records executable validation commands for source hashing, preflight, `qemu-img info`, `qemu-img convert -O raw`, converted-RAW hashing, nested `mmls`, `fsstat`, `tsk_recover`, and `image-workflow-validate --item-number 24`, plus evidence slots for source integrity, qemu metadata, chain review, conversion provenance, converted-RAW integrity, nested recovery, trusted diff, snapshot/differencing corpus, hypervisor metadata, and encrypted/compressed/corrupt VM disk validation.

#25 forensic-container evidence rows include optional sidecar `verified_export_manifest_profile` from `.export-manifest.json` or `.manifest.json` files next to AD1/L01/Lx01/AFF/AFF4 inputs. The profile records manifest SHA256, vendor tool/version, export root hash, source-container hash match status, file/hash counts, bounded file samples, missing required fields, and validation status. Rows also include `forensic_container_workflow_manifest` with `profile_version=forensic-container-export-workflow-manifest-v1`, item `#25`, source integrity, export-first guidance, verified-manifest stage status, derived-export scan readiness, large-data controls, reportability decision, commercial blockers, and stable `manifest_sha256`. `forensic_container_validation_plan` with `profile_version=forensic-container-report-grade-validation-plan-v1` records executable/analyst validation commands for source hashing, evidence preflight, vendor export, manifest authoring, derived export scanning, and `image-workflow-validate --item-number 25`; its evidence slots cover source integrity, export policy, vendor tool/version logs, verified export manifest, derived export root hash, exported file hash inventory, trusted vendor diff, metadata/deleted-entry corpus, encrypted/compressed corpus, and native-parser research blockers. #25 trusted-diff helpers accept nested `verified_export_manifest_profile` and compare source hash, container format, and vendor manifest hash against vendor/export references.

#13 USN trusted-diff helpers accept nested RapidTriage `usn-record` rows plus UsnJrnl2Csv/MFTECmd/TSK-style rows while comparing USN, FRN, parent FRN, filename, reason flags, source-info flags, file attributes, major version, record cursor, and v4 extent counts. #14 JumpList trusted-diff helpers accept nested RapidTriage `jumplist-automatic`/`jumplist-custom` rows and expand `details.destinations` plus `destlist_metadata.destlist_entry_candidates` before comparing JLECmd-style AppID, entry number, stream path, target path, timestamp, MRU/pin text, DestList entry offset, hostname, and candidate validation status. #15 ShellBags trusted-diff helpers accept nested RapidTriage `shellbag-native-candidate` rows and `shellbag_evidence` before comparing ShellBagsExplorer/SBECmd-style key path, folder path candidates, Bag/Node IDs, hive, section, last-write timestamp, cell/hbin offsets, allocation status, and transaction-log status. #16 Prefetch trusted-diff helpers accept nested RapidTriage `prefetch-file` rows before comparing PECmd/WinPrefetchView-style executable/hash, run count, first and historical last-run timestamps, version/layout, declared size, section-boundary status/hash, referenced paths, volume/device path pivots, file-reference candidates, and compression/decompression status. #17 LNK trusted-diff helpers accept nested RapidTriage `recent-shortcut` rows before comparing LECmd-style target path, working directory, arguments, target timestamps, StringData, LinkInfo local/common paths, link flags, file attributes, target size, hotkey/show command, tracker machine/Droid GUIDs, and property-store path/string candidates. #18 Windows system trusted-diff helpers accept nested Task Scheduler, Defender, Firewall, WER, and WMI RapidTriage rows before comparing Velociraptor/Chainsaw/Autoruns/native-export style task URI, command, principal, trigger, Defender threat/remediation, Firewall endpoint tuple, WER exception, and WMI persistence-pivot fields through `system_artifact_field_comparison`. #19/#20 browser helpers expand nested `storage_inventory` and `unified_timeline` rows before comparing Hindsight/BrowserHistoryView/DB Browser/Velociraptor-style storage type/name/path/count/size/sensitivity and visit/download URL, timestamp, transition, title, count, state, and source-table fields through `browser_storage_field_comparison` and `browser_timeline_field_comparison`.

Native SRUDB inventory now also exposes `native_srudb_validation`, table candidate source offsets, stable SRUM citation manifest hashes, and bounded `srum-row-candidate` string-cluster rows with app/user/timestamp/counter candidates. These native SRUM candidates remain validation-required and keep `commercial_grade_ready=false` until full ESE catalog/page/row decoding, page checksum verification beyond header/page-size checks, and known-answer validation are available.

For OS/account specifically, exported group-membership rows now include SID/name source typing, member count semantics, privileged-group flags, validation checks, and native SAM alias-member blockers. SECURITY/LSA secret metadata remains non-decrypting and may include value type, byte count, SHA256, entropy, and timestamp candidates. Native SAM group rows are bounded group/alias key candidates only, with `commercial_grade_ready=false` until native alias-member binary decoding and OS-version validation are complete. The #6 trusted-diff helper accepts nested RapidTriage account/group/privilege artifact rows plus flat RECmd/Registry Explorer-style rows, comparing account RID/name/admin/disabled hints, group member SID/name sets, privilege assignments, and LSA secret names before allowing any report-grade account-state wording.

Current macOS parser rows include user profile inventory, Safari/Chromium/Firefox history/download rows, Safari download rows correlated from LaunchServices `QuarantineEventsV2` with quarantine source table/row ID/source hash provenance, LaunchServices quarantine events, TCC privacy permissions with `service`, `client`, `allowed`, `last_modified_at`, `risk_flags`, and `risk_score`, and LaunchAgent plist fields such as `label`, `program`, `program_arguments`, and `run_at_load`. Native macOS inventory rows for Unified Logs, Spotlight stores, FSEvents, and APFS snapshot hints add source hashes, size, modified time, bounded string/path/URL pivots, risk flags, validation guidance, and reportability fields for search/review before dedicated macOS tooling validation.

Current Linux parser rows include user inventory, shell history commands, SSH authorized-key hashes and known-host pivots, auth log events, auditd events, dpkg package/status events, Docker container config rows, cron entries, and systemd units. High-value fields include `user`, `uid`, `shell`, `command`, `src_ip`, `schedule`, `unit_name`, `exec_start`, `package`, `version`, `container_id`, `image`, `source_hashes`, `risk_flags`, and `risk_score`.
