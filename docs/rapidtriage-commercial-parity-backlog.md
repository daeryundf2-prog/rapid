# RapidTriage Commercial Parity Backlog

This is the current 120-item commercial parity backlog for moving RapidTriage toward AXIOM/WISDOM-class usefulness. It is intentionally strict: an item is not `Done` unless it is implemented, fixture-backed where practical, documented, and safe to explain to a forensic user without overstating evidence strength.

Status legend: `Done`, `Partial+`, `Partial`, `Planned`, `External`.

Compatibility note: the earlier historical item 121, NTUSER.DAT and UsrClass.dat deep analysis, is now folded into items 4, 5, 6, 15, 18, 65, 90, and 91 so the active execution list follows the user's latest 120-item structure.

## Execution Rule

Work proceeds in number order unless a dependency makes a later item necessary first. Each commit should update this file when the status or acceptance evidence changes.

Reorganization note: #6~#45 now share review-summary output on the main validation-gated rows so analysts can compare gap ID, artifact goal, triage/commercial status, primary evidence, blockers, caveats, and next validation steps consistently across account, execution, SRUM, Windows.edb, NTFS, JumpList, ShellBags, Prefetch, LNK, system artifacts, browser/AI artifacts, evidence-image adapters, mobile exports, iOS backup/keychain, Android APK/app-data evidence, chat apps, email, cloud exports/API collection, browser secret handling, and mobile correlation/schema rows.

Accuracy reinforcement note: #1~#30 now have machine-readable validation/accuracy profiles generated by `rapidtriage validation` under `core_forensics_accuracy_profiles`. Each profile fixes the artifact surface, known-answer corpus requirement, independent oracle, required checks, minimum evidence, pass/fail rules, and default `validation-required` reportability gate so future parser work can be measured instead of described informally.

Batch validation note: #1~#5 and #6~#10 rows now emit `core_accuracy_gates` on the relevant EVTX, Registry, SAM/SECURITY/SYSTEM, Amcache, ShimCache, BAM/DAM, and SRUM artifacts. `docs/validation/rapidtriage-core-forensics-001-005-known-answer.json` and `docs/validation/rapidtriage-core-forensics-006-010-known-answer.json` provide internal fixture-backed known-answer manifests for the implemented claims. This satisfies the internal `validated` maturity gate when attached to `rapidtriage commercial-readiness`, but it does not remove external corpus, trusted-tool diff, broad Windows-version, or independent-review blockers for commercial-grade claims.

## A. Core Forensic Capability

1. Native EVTX BinXML full parsing. Status: Partial+. Acceptance: decode binary EVTX records into full Event/System/EventData/UserData fields without relying on external exports; current native rows expose record metadata, strings, integrity, explicit `evtx_binxml_status`, first-pass BinXML token scans for fragment headers/elements/attributes/value text/template headers, inline String/ANSI/integer/bool/float/GUID/SID/FILETIME/SYSTIME/binary scalar decoding, TemplateInstance IDs, TemplateInstance value spec/value decoding for common scalar types, substitution value fields, promoted `Event/System`, `EventData`/named `Data`, duplicate-preserving ordered EventData sequences, grouped EventData values by name, and `UserData` fields, normalized EventID/provider/channel/computer/level/timestamp/process/thread/user-SID/command pivots, rendered previews, decoded value-type counts, BinXML field maps, and `eventlog-chunk` structure rows for native chunk bounds/checksum observations. Remaining commercial gap: complete provider-specific BinXML grammar coverage and independent corpus validation.
2. EVTX event template/message rendering. Status: Partial+. Acceptance: render provider templates/messages where available and preserve unresolved template IDs with validation warnings; current rows preserve imported rendered messages from XML/JSON/CSV exports, expose `event_message` plus `message_rendering` provenance/confidence/limitations, render high-value built-in fallback templates for common Security/PowerShell/log-clear/account/process/service/task/Sysmon/Defender/WMI/Firewall events, record which extracted fields were substituted into built-in messages, summarize available native BinXML fields for unresolved/provider-resource review, retain native TemplateInstance IDs, and mark native fallback messages as validation-required until provider DLL/resource-table rendering is implemented.
3. EVTX deleted/corrupt record recovery validation. Status: Partial+. Acceptance: recover slack/deleted/corrupt candidate records with offset, integrity, confidence, and caution labels; current native scans emit top-level validation flags, `evtx_validation_reasons`, `evtx_validation_checks`, `evtx_recovery_context`, and `evtx_recovery_evidence` with offset bounds, chunk free-space comparison, integrity summary, confidence, and machine-readable evidence reasons on parseable rows, flag chunk slack/deleted candidates when chunk free-space metadata is present, emit `eventlog-record-candidate` rows for corrupt/out-of-bounds record headers, emit `eventlog-chunk` rows with structure plausibility/checksum observations, and summarize recovery/allocation/chunk integrity counts. Remaining commercial gap: large validation corpus coverage for real-world deleted EVTX slack and corrupt chunk edge cases.
4. Registry hive full key tree reconstruction. Status: Partial+. Acceptance: reconstruct native hive key/value tree with last-write timestamps, source offsets, cell allocation state, and parser confidence; current native hive scans now prefer bounded `hbin` cell walking, fall back to signature scanning, and emit `registry-key-tree-node` rows from `nk` parent/subkey/value-list metadata, preserving key paths where parent chains are recoverable plus offsets, path components, key depth, ancestry cell offsets, path evidence/cycle/missing-parent flags, subkey/value names, linked/missing subkey/value offsets, last-write hints, allocation state, confidence, validation flags, and validation guidance. Remaining commercial gap: transaction-log replay and broad corpus validation across malformed/large hives.
5. Registry deleted key/value recovery. Status: Partial+. Acceptance: distinguish allocated/free/deleted key/value candidates and report recovery confidence without overstating testimony; current scans emit generic `registry-deleted-cell-candidate` rows plus key-specific `registry-key-recovery-candidate` and value-specific `registry-value-recovery-candidate` rows for free `nk`/`vk` cells with offsets, `registry_recovery_evidence` allocator/signature/path/parent/data-preview reasons, cautious path/data previews, explicit parent-key confidence that prefers key value-list ownership over nearest-key fallback, caution labels, confidence, and validation-required guidance. Remaining commercial gap: deleted-key/value testimony validation against full hive allocator state and transaction logs.
6. SAM/SECURITY/SYSTEM account and permission deep parser. Status: Partial+ with explicit commercial gate. Acceptance: decode users, groups, privileges, current control set, LSA/policy-sensitive locations, services, mounted devices, and timezone/boot metadata; current rows include account/profile summaries, separate `windows-account-lifecycle` rows with merged RID/name lifecycle hints, UAC flag labels, exported SAM F/V binary presence and byte counts, first-pass SAM F timestamp/RID/UAC candidate decoding, SAM V UTF-16 string candidates, account-centered `account_security_context` that links exported group SID candidates to inherited high-risk privileges, validation checks, explicit `commercial_grade_ready=false` blockers, exported `windows-group-membership` rows with SID/name source typing, builtin group SID candidates, and privileged-group flags, native SAM account/RID candidates, native SAM group/alias key candidates with explicit membership-reconstruction blockers, service configuration rows, mounted-device rows, LSA sensitive-location rows with value names/type/byte-count/SHA256/entropy/timestamp-candidate metadata, privilege-assignment rows with assigned principal hints, current control set hints, boot/shutdown/timezone pivots, hashes, confidence, and validation guidance. Remaining commercial gap: full OS-version validated SAM F/V binary decoding, SECURITY secret decryption, group membership reconstruction from native binary attributes, and transaction-log validation.
7. Amcache parser. Status: Partial+ with explicit commercial gate. Acceptance: extract application execution/install records, hashes, paths, publisher metadata, and timestamps; current registry export rows normalize Amcache keys/values including program name, publisher, SHA1, file description, product name, timestamp source, validation checks, correlation targets, and `amcache_evidence` with normalized path, filename, hash candidates, metadata-field presence, timestamp semantics, and execution caveat, while native `Amcache.hve` rows emit bounded hive string-pivot `amcache-hive` and `amcache-entry` candidates with paths, SHA1 candidates, publisher strings, `amcache_hive_evidence`, confidence, hashes, validation checks, and validation guidance. Remaining commercial gap: full Amcache.hve schema/version decoding and report-grade install/execution timestamp extraction.
8. ShimCache/AppCompatCache parser. Status: Partial+ with explicit commercial gate. Acceptance: parse SYSTEM hive cache entries with OS-version handling, paths, timestamps, flags, and caveats; current export rows emit `shimcache-entry` with path/timestamp extraction, `shimcache_evidence` caveat/OS-layout-validation metadata, confidence, risk flags, validation checks, correlation targets, commercial blockers, and an explicit not-proof-of-execution caveat. Remaining commercial gap: native binary AppCompatCache layout decoding across Windows versions.
9. BAM/DAM execution parser. Status: Partial+ with explicit commercial gate. Acceptance: parse user SID, executable path, last execution timestamp, and confidence; current export rows emit `bam-entry` with user SID, device/executable path, BAM value FILETIME timestamp decoding when present, timestamp source, `bam_dam_evidence` timestamp-semantics/native-SYSTEM-validation metadata, confidence, validation checks, commercial blockers, risk flags, and correlation guidance. Remaining commercial gap: native SYSTEM hive binary value interpretation and broad version validation.
10. SRUM full ESE table parser. Status: Partial+ with explicit commercial gate. Acceptance: decode native SRUDB tables for app, network, resource, user, bytes, energy, and timestamps; current imports normalize app/network/resource rows with bytes total, interface/profile, energy/CPU, counter validation checks, and `srum_usage_evidence`; native SRUDB emits ESE header/string pivots, `srum_database_evidence`, native SRUDB validation metadata, `srum-table-candidate` rows with marker offsets/counts, `srum_pivot_evidence`, and bounded `srum-row-candidate` rows for native string clusters with app/user/timestamp/counter candidates plus `srum_row_evidence`. Native SRUDB-derived rows keep `commercial_grade_ready=false`, validation checks, and blocker fields. Remaining commercial gap: full native ESE catalog/table/page decoding, page checksum validation, and row-level timestamp/counter extraction.
11. Windows.edb full ESE parser. Status: Partial+ with explicit commercial gate. Acceptance: decode native Windows Search tables into paths, URLs, content snippets, metadata, deleted/index state, and timestamps; current native handling inventories ESE header/hash/string data, emits path/URL/content pivots, builds a bounded ESE page map, adds native validation metadata, surfaces `windows-search-edb-page-candidate` rows with page index, byte offset, page SHA256, page-local table markers, path/URL/content/risk strings, emits `windows-search-edb-table-candidate` rows for likely gather/property/content/deleted-state table families, and emits `windows-search-edb-row-candidate` rows that correlate native path/URL/content strings into reviewable search-result candidates with deleted-state marker awareness, candidate basis, risk flags, and validation guidance. Remaining commercial gap: full native ESE catalog/table/row decoding, authoritative row-level timestamp/property extraction, and deleted/index-state validation.
12. `$MFT` full attribute parser. Status: Partial+ with explicit commercial gate. Acceptance: decode FILE records, attributes, filenames, timestamps, resident/nonresident data hints, deleted/in-use status, and sequence validation; current native rows parse bounded FILE record headers, update-sequence-array validation metadata, common `$STANDARD_INFORMATION`/`$FILE_NAME`/`$DATA` attribute summaries, FILETIME timestamp source validation, resident-data hashes, nonresident runlist previews, deleted/in-use hints, parser confidence, validation checks, and `mft_record_evidence` for record identity, path/parent reference, state, attribute evidence, validation evidence, and report limitations. Remaining commercial gap: full attribute-list extension resolution, report-grade nonresident data-run decoding, path reconstruction across parent records, broad malformed/corpus validation, and full-volume timeline validation.
13. `$UsnJrnl` large-scale timeline parser. Status: Partial+ with explicit commercial gate. Acceptance: parse v2/v3 records at scale with FRN/parent FRN, reason flags, timestamps, and pagination-friendly output; current native rows expose bounded scan metadata, cursor/next-cursor offsets, v2 reference decoding, v3 FILE_ID_128 hex, large-record sizing, filename UTF-16 validation, unknown reason-bit checks, timestamp ranges, and `usn_record_evidence` for record identity, file-reference evidence, change evidence, validation evidence, and report limitations. Remaining commercial gap: full journal replay/correlation, cross-record path reconstruction, and broad known-answer large-corpus pagination validation.
14. JumpList DestList deep parser. Status: Partial+ with explicit commercial gate. Acceptance: decode DestList MRU/account metadata, embedded LNK streams, timestamps, and source stream provenance. Current: recoverable OLE streams include stream SHA256 provenance, embedded LNK destinations are linked back to stream metadata, DestList streams emit bounded header/entry metadata candidates, and `jumplist_evidence` summarizes container/application hash, stream names, DestList validation, destination targets, stream offsets, tracker presence, timestamp candidates, and review guidance. Blockers: OS-version-specific DestList field semantics, deleted-entry recovery, report-grade account attribution, and AppID hash-to-application mapping still require validation before commercial parity.
15. ShellBags native hive parser. Status: Partial+ with explicit commercial gate. Acceptance: decode ShellBag folder nodes, paths, timestamps, bag IDs, source key paths, and confidence from native user hives; current rows preserve `.reg` ShellBags keys and add native `shellbag-native-candidate` rows from bounded NTUSER/UsrClass hive key-tree/string pivots with source key paths, bag/node ID candidates, timestamp candidates, validation checks, parser confidence, and `shellbag_evidence` for key evidence, bag/node relationship candidates, activity timestamps/paths, and report limitations. Remaining commercial gap: full binary shell-item payload decoding, bag/node relationship validation, transaction-log replay, deleted/slack ShellBag testimony validation, and corpus validation against dedicated commercial parsers.
16. Prefetch full version parser. Status: Partial+ with explicit commercial gate. Acceptance: support common Prefetch versions with run count, last run times, volume info, file references, and hash metadata; current rows include version-specific common-layout metadata for versions 17/23/26/30/31, run-count/last-run validation checks, bounded volume/file-reference candidates, source hashes, explicit commercial blockers, and shared `forensic_review` summaries for `prefetch-file` and `prefetch-reference` rows. Remaining commercial gap: full file metrics, authoritative volume table, trace-chain/directory sections, compressed/malformed corpus validation, and report-grade MFT file-reference decoding.
17. LNK full metadata parser. Status: Partial+ with explicit commercial gate. Acceptance: parse target path, working dir, arguments, timestamps, drive/network metadata, tracker data, and shell item metadata; current LNK rows include parsed header flags, target timestamps, LinkInfo/StringData/ExtraData, tracker candidates, validation matrix, source hashes, blockers, and shared `forensic_review`. Remaining commercial gap: full shell-item property store semantics, drive/network provider validation, and broad Shell Link known-answer corpus coverage.
18. WER/Defender/Firewall/Task Scheduler/WMI deep parser. Status: Partial+ with explicit commercial gate. Acceptance: parse high-value logs/configs with normalized fields, risk flags, source hashes, and report-ready pivots; current Task Scheduler, Defender MPLog, Firewall W3C, WER, and WMI repository rows now expose validation matrices, report-grade blockers, native-capability disclosure, and shared `forensic_review` summaries. Remaining commercial gap: deeper Defender/Firewall event semantics, native WMI repository object decoding, TaskCache/security descriptor/history correlation, and report-grade cross-artifact validation.
19. Browser cache/session/extension/sync artifacts. Status: Partial+ with explicit legal gate. Acceptance: parse cache, sessions, extensions, sync metadata, cookies, and sensitive browser artifacts with strict legal warnings. Current: Chromium-family profiles emit bounded storage inventory rows for cache/session/extension/sync/cookie/credential/local-storage/IndexedDB files with file counts, byte totals, sample hashes, privacy/legal warnings, validation checks, secret-handling assessment, and shared `forensic_review`. Remaining commercial gap: full cache-entry decoding, cookie/session/password decryption with explicit opt-in authority, extension schema interpretation, sync-engine validation, and known-answer corpus validation.
20. Chrome/Edge/Firefox/Safari unified browser timeline. Status: Partial+ with explicit commercial gate. Acceptance: normalize cross-browser history, downloads, typed URLs, visits, and source profile metadata into one timeline. Current: Windows and macOS Chromium/Firefox history rows expose typed-count/query/transition/duration candidates, source-profile metadata, bounded `unified_timeline` rows, browser report-grade assessment, and shared `forensic_review`; Safari remains macOS-history-only and is not overclaimed as full cache/session parity. Remaining commercial gap: browser-version-specific visit-transition semantics, deleted-history recovery, Safari download/session/cache parity, and large multi-profile timeline validation.
21. AI service transcript parser for ChatGPT/Claude/Gemini/Perplexity. Status: Partial+ with explicit commercial gate. Acceptance: recover service-labeled question/answer candidates, pairing confidence, source storage, and validation status. Current: AI usage and conversation rows include source storage kind, profile-relative path, source size/mtime/hash, offsets, pairing confidence summaries, source-storage summaries, orphan counts, validation checks, privacy warnings, `commercial_grade_ready=false`, and shared `forensic_review`. Remaining commercial gap: service-side export validation, service schema version tracking, deleted-fragment recovery, and corpus-backed false-positive/false-negative testing.
22. E01/Ex01 fully integrated workflow. Status: Partial++ with explicit commercial gate. Acceptance: run libewf/Sleuth Kit workflow where installed, record tool versions, mount/extract safely, and show actionable fallback guidance; current evidence adapter and extraction metadata record source integrity, external tool path/version preflight, mmls partition table with selected offset, command history, warnings, read-only safety framing, report-grade blockers, and `forensic_review`. Remaining commercial gap: broad libewf/Sleuth Kit version matrix, encrypted/malformed/corrupt E01/Ex01 validation, and independent known-answer image corpus reports.
23. RAW/split image robust partition/filesystem handling. Status: Partial++ with explicit commercial gate. Acceptance: enumerate partitions/filesystems, recover supported content read-only, and preserve extraction audit metadata; current output records discovered split parts, per-part integrity, gap/start warnings, mmls partition metadata, selected offsets, tool preflight, recovery command history, report-grade blockers, and `forensic_review`. Remaining commercial gap: large damaged/gapped split-set validation, filesystem-specific known-answer recovery checks, path/timestamp validation, encrypted volume handling, and full partition edge-case coverage.
24. VHD/VHDX/VMDK/VDI/QCOW direct handling polish. Status: Partial++ with explicit commercial gate. Acceptance: route through qemu-img/Sleuth Kit safely with clear progress, errors, hashes, and warnings; current output records qemu-img/Sleuth Kit tool preflight, source and converted-raw integrity, conversion command history, nested raw recovery metadata, safety notes, report-grade blockers, XVA export-first guidance, and `forensic_review`. Remaining commercial gap: snapshot/differencing-chain support, encrypted/compressed/corrupt VM disk validation, qemu-img version matrix, hypervisor metadata preservation, and large known-answer corpora.
25. AD1/L01/Lx01/AFF/AFF4/XVA support. Status: Partial detection/import-guidance with explicit commercial gate. Acceptance: detect, route, import, or document export-first workflows with evidence hashes and limitation warnings; current `rapidtriage evidence --json` detects AD1/L01/Lx01/AFF/AFF4/AFF4-L and XVA, records bounded source integrity, warnings, limitations, export-first guidance, safety notes, report-grade blockers, and `forensic_review`. Remaining commercial gap: native AD1/L01/Lx01/AFF/AFF4/XVA parsing, metadata/deleted-entry validation, encrypted/compressed container handling, and vendor export known-answer tests.

## B. Mobile, Cloud, And Apps

26. Cellebrite/XRY/GrayKey/AXIOM export deep import. Status: Partial+ with explicit commercial gate. Acceptance: normalize vendor export messages, contacts, calls, files, apps, accounts, media, browser rows, source IDs, hashes, validation checks, and source-summary blockers. Current rows include mobile validation matrices, report-grade assessment, native-capability disclosure, legal warning, and shared `forensic_review`. Remaining: vendor-version-specific schemas, deleted-record semantics, original acquisition hash verification, and independent known-answer validation.
27. iOS backup parser. Status: Partial+ with explicit commercial gate. Acceptance: parse authorized iOS backup files, domains, manifest DBs, app data, messages, media, accounts, and timestamps. Current: `Manifest.db`, `Info.plist`, and `Status.plist` inventory candidates include hashes, validation checks, authorization warnings, report-grade blockers, and shared `forensic_review`. Remaining: encrypted backup handling, app DB parsing, deleted records, and known-answer validation.
28. iOS keychain/artifact parser. Status: Partial+ with explicit legal gate. Acceptance: inventory keychain/artifact records with strict authorization, redaction, and legal warnings. Current: keychain SQLite table/column/count inventory includes `secrets_extracted=false`, redaction checks, protected-data legal warning, report-grade blockers, and shared `forensic_review`. Remaining: validated protected-data workflow outside core and legal/authorization package.
29. Android backup/artifact parser. Status: Partial+ with explicit commercial gate. Acceptance: normalize Android backup/export app, file, SMS, call, contact, browser, and media artifacts. Current: vendor CSV/JSON rows cover SMS/message, call, contact, app, file, account, media, and mobile browser candidates; Android app data export files are inventoried by package/path/category with Android validation matrix and shared `forensic_review`. Remaining: app-specific DB schemas and deleted/encrypted stores.
30. Android app package/data parser. Status: Partial+ with explicit commercial gate. Acceptance: parse APK metadata, permissions, manifests, strings, native libraries, app data exports, and risk flags. Current: APK rows include hashes, entry hashes, permissions, components, native architectures, bounded string pivots, app-data export inventory, Android report-grade assessment, native-capability disclosure, and shared `forensic_review`. Remaining: binary manifest decoding, DEX control-flow analysis, signing trust, and malware validation.
31. KakaoTalk parser. Status: Partial+ with explicit commercial gate. Acceptance: parse authorized exports/backups for chats, participants, media references, timestamps, and schema versions. Current: authorized CSV/JSON/JSONL rows are normalized into service-labeled message pivots with conversation IDs/titles, participants, message IDs, reactions, media references, text hashes, validation checks, `chat_app_report_grade_assessment`, `chat_app_forensic_review`, and `kakaotalk_compatibility_assessment`; KakaoTalk SQLite/DB candidates are inventoried by table/column/count only. PC KakaoTalk triage now also has `kakaotalk-windows`, which inventories application `.edb`/`.db`/`.dat` stores, DB/WAL/SHM/copy companions, source hashes, SQLite accessibility, header signatures, likely roles for chat logs, chat lists, profile/contact/media/login/client-state/post-BigBang adjacent indexes, 4096-byte legacy encrypted-page readiness, and redacted registry `DeviceInfo` context candidates before correlating `Windows.edb`, Registry exports/native hives, and bounded memory dumps into source candidates with source hashes, source family, source offsets or ESE page metadata where available, matched KakaoTalk terms, path/URL/process/chat-store classification, privacy warnings, and #31 `forensic_review`. The separate `kakaotalk-decrypt` command now supports authorized external key/IV, pragma/userId, or DeviceInfo+pragma-key+userId material, auto-decodes DeviceInfo values from `NTUSER.DAT`, decrypts 4096-byte AES-CBC pages, validates SQLite headers, counts message rows, and emits bounded previews only when explicitly requested. KakaoTalk 25.7.2 / 2025-08-13 and later is explicitly marked `post-bigbang-legacy-method-not-applicable` because the public release notes include deletion-behavior changes and enhanced encryption. Remaining: post-2025-08 KakaoTalk known-answer corpus, schema-version-specific parser mapping, deleted records, independent validation, and proprietary-key-free operational guidance.
32. WhatsApp parser. Status: Partial+ with explicit commercial gate. Acceptance: parse authorized exports/backups for chats, contacts, media, calls, and timestamps. Current: WhatsApp exports normalize message/chat/participant/media fields and `msgstore.db`-style databases are inventoried read-only with message-table candidates, hashes, `chat_app_report_grade_assessment`, and `chat_app_forensic_review`. Remaining: encrypted `crypt*` backups, contacts/calls/media recovery, deleted records, and corpus validation.
33. Telegram parser. Status: Partial+ with explicit commercial gate. Acceptance: parse Telegram desktop/mobile exports with chats, media references, contacts, and account metadata. Current: Telegram CSV/JSON/JSONL export rows normalize chat title/ID, author, message ID, text/media/reaction pivots, and Telegram DB/cache candidates are inventoried safely with service-specific review metadata. Remaining: Telegram Desktop encrypted/local map decoding, cloud export schema coverage, account attribution, and deleted/cache recovery validation.
34. Signal parser. Status: Partial+ with explicit commercial gate. Acceptance: parse authorized Signal exports/backups where technically and legally feasible with strong limitations. Current: Signal export rows normalize thread/message/participant/media fields and Signal SQLite candidates are inventory-only with no decryption attempts, plus service-specific review metadata. Remaining: encrypted backup support, SQLCipher/key handling outside core, attachment recovery, deleted records, and legal authorization package.
35. WeChat/LINE/Discord/Instagram and extended messenger parser. Status: Partial+ with explicit commercial gate. Acceptance: normalize authorized exports for messages, users, media, reactions, and timestamps. Current: authorized exports for WeChat, LINE, Discord, Instagram, iMessage, Facebook Messenger, Viber, Skype, Slack, Microsoft Teams, Reddit, X/Twitter, TikTok, Snapchat, Matrix/Element, Wire, Threema, Session, and Wickr are service-labeled with conversation/message IDs, participants, text hashes, media references, reactions, validation checks, chat DB inventory candidates, `chat_app_scope_profile`, `chat_app_issue_matrix`, and service-specific review metadata. Remaining: app-specific encrypted stores, schema drift, media recovery, reactions/read receipts completeness, ephemeral/secret chat validation, and known-answer validation.
36. Email PST/OST full mailbox parser. Status: Partial+ with explicit commercial gate. Acceptance: parse mailbox folders, messages, headers, attachments, conversation threading, hashes, and search text. Current: `email` collector parses EML/EMLX/MBOX/Maildir headers, body previews, attachment metadata/hashes, message/thread IDs, mailbox source hashes, `email_format_profile`, `email_issue_matrix`, and #36 `forensic_review`; PST/OST/MSG receive bounded string/email/subject candidate inventory with legal warnings and `commercial_grade_ready=false`. Remaining: native PST/OST folder/message/deleted-item decoding, full MSG MAPI properties, DKIM/ARC/S/MIME/OpenPGP validation, threading validation, corrupt-store recovery, and mailbox corpus testing.
37. Gmail/Google Takeout deep parser. Status: Partial+ with explicit commercial gate. Acceptance: normalize mail, account activity, location, Drive metadata, photos, browser/account events, and timestamps. Current: Google Takeout location/activity/account rows remain normalized, and Gmail/Drive-style JSON rows emit `cloud-mail` and `cloud-file` pivots with hashes, timestamps, URLs, body previews, validation blockers, `cloud_provider_profile`, `cloud_issue_matrix`, and cloud `forensic_review`. Remaining: full Takeout product matrix, MBOX threading, Photos sidecar/EXIF merge, archive split/expiration handling, deleted/retention semantics, and provider schema-version tracking.
38. Apple iCloud export parser. Status: Partial+ with explicit commercial gate. Acceptance: normalize Apple account exports, device/account metadata, photos/files references, and timestamps. Current: Apple/iCloud account and file/photo-style JSON rows normalize account fields, file IDs/names/URLs/owners/timestamps, risk flags, validation checks, blockers, `cloud_provider_profile`, `cloud_issue_matrix`, and cloud `forensic_review`. Remaining: full Apple data export schemas, iCloud Photos albums/EXIF correlation, device inventory, shared album fidelity/comments/likes, third-party iCloud containers, Advanced Data Protection limits, and known-answer validation.
39. Microsoft 365/OneDrive/Teams export parser. Status: Partial+ with explicit commercial gate. Acceptance: parse M365 exports for mail, Teams chats, files, sharing, audit logs, and account metadata. Current: Microsoft/OneDrive/Teams/Audit JSON rows emit `cloud-mail`, `cloud-file`, `cloud-message`, and `cloud-audit` pivots with operation/actor/IP/user-agent, Teams chat IDs, OneDrive file URLs, hashes, validation checks, blockers, `cloud_provider_profile`, `cloud_issue_matrix`, and cloud `forensic_review`; collaboration SaaS rows such as Slack-style message exports are also classified. Remaining: Graph/eDiscovery export variants, Teams Cosmos DB vs Exchange compliance record reconciliation, retention/deleted-item semantics, Teams attachments/reactions, SharePoint permissions, and audit completeness validation.
40. Cloud API acquisition workflow. Status: Partial+ with explicit commercial gate. Acceptance: authorized request manifests, bounded API collection, redacted credentials, response hashes, and audit output. Current: `cloud-collect` validates request manifests, redacts Bearer/API credentials from metadata, hashes raw responses, writes audit output, supports dry-run/import guidance into `cloud-export`, and emits #40 `forensic_review`. Remaining: OAuth device flows, token vaulting/rotation, pagination, throttling/backoff, provider-specific collectors, and legal hold/export package validation.
41. Cloud token/credential secure handling. Status: Partial+ with explicit commercial gate. Acceptance: redact secrets by default, store tokens safely, log access, and support controlled reveal/export. Current: `cloud-collect` uses environment-variable token input, redacts Authorization/API-key headers, records `credential_handling`, confirms `tokens_written_to_output=false`, writes audit output, and emits #41 credential `forensic_review`. Remaining: token vaulting, controlled reveal workflow, rotation, RBAC, and enterprise secret-store integration.
42. Browser password/cookie/session artifact handling with strict legal warning. Status: Partial+ with explicit commercial gate. Acceptance: sensitive artifacts are opt-in, redacted by default, audited, and legally warned. Current: browser storage/cookie/session/credential-like files are inventoried with source hashes, legal warnings, sensitivity labels, no credential decryption or token reveal by default, #42 secret-handling assessment, and #42 `secret_handling_forensic_review`. Remaining: audited opt-in reveal workflow, DPAPI/keychain integration where lawful, and validation package.
43. Mobile app media/message timeline correlation. Status: Partial+ with explicit commercial gate. Acceptance: correlate chats, media, contacts, app events, and filesystem timestamps. Current: mobile export sources emit `mobile-correlation-summary` rows with message/media/contact/call counts, services, participants, schema versions, validation checks, candidate message-media links, and #43/#44/#45 `forensic_review`. Remaining: cross-source/device-wide timeline merge, timezone normalization, attachment recovery, and known-answer validation.
44. Contact/call/SMS unified mobile view. Status: Partial+ with explicit commercial gate. Acceptance: show normalized contacts, calls, SMS/messages, participants, timestamps, and source app/export. Current: mobile exports normalize contacts, calls, SMS/messages, app/service participants, source record IDs, hashes, source summaries, and correlation summary actor pivots with #43/#44/#45 `forensic_review`. Remaining: UI-specific grouped mobile view, dedupe/entity resolution, and full app schema validation.
45. App-specific known schema version management. Status: Partial+ with explicit commercial gate. Acceptance: track parser schema versions, app DB versions, compatibility notes, and migration tests. Current: mobile message rows preserve `schema_version`/`dbversion`/`exportversion`/`appversion` candidates, chat DB inventory records table/column candidates, correlation summaries list schema versions, and #43/#44/#45 `forensic_review`. Remaining: per-app compatibility matrix, migration tests, schema diff fixtures, and release-gated parser support policy.

## C. Search, Analysis, And UX

46. Large-result clustering. Status: Partial+ with UX gate. Acceptance: cluster similar search hits, files, media, entities, and repeated artifacts for review efficiency. Current: unified search now emits bounded source/kind/extension/folder/keyword clusters with representative match indices, review hints, and web filter chips. Remaining: persistent cluster review state, near-duplicate text/media clustering, and large-case validation.
47. Entity view: people, accounts, email, phone, IP, and domain. Status: Partial+ with validation gate. Acceptance: pivot by normalized entities with linked evidence, counts, first/last seen, and risk indicators. Current: unified search extracts email, phone, URL, domain, public IPv4, and hash pivots with source/kind/path counts and match links. Remaining: person/account resolution, false-positive tuning, first/last seen across full case index, and entity merge/split workflow.
48. Graph view: account-file-URL-time relationships. Status: Partial. Acceptance: visualize entities and evidence relationships with filters and source citations. Current: search analysis emits capped match-path-keyword-entity graph nodes/edges and web summary counts. Remaining: interactive graph canvas, time-edge semantics, graph filters, saved layouts, and scalable server-side graph paging.
49. Unified timeline correlation. Status: Partial+. Acceptance: correlate filesystem, event logs, browser, mobile, cloud, and review annotations into one scalable timeline. Current: search analysis extracts timestamp anchors from matched metadata, sorts them, emits date buckets, and surfaces top timeline anchors in the web search view. Remaining: full Case DB timeline join, timezone/skew validation, review annotation overlays, and cursor-paged timeline correlation for very large cases.
50. Incident hypothesis/workbook feature. Status: Partial. Acceptance: let analysts create hypotheses, attach evidence, track questions, and export workbook sections. Current: search analysis generates draft hypotheses, linked cluster IDs, review tasks, questions, and next actions for credential, web/AI, execution, and network/cloud patterns. Remaining: editable/persistent workbook, evidence attachment workflow, reviewer assignments, report export sections, and version history.
51. Reviewer assignment/status workflow. Status: Partial+. Acceptance: assign items, track status, priorities, reviewer notes, and report candidate state. Current: Case DB review marks now persist reviewer, assignee, normalized priority, due date, tags, verification status, notes, report-candidate state, audit events, API/CLI inputs, batch review updates, and web quick-action assignment defaults. Remaining: role-based assignment queues, SLA dashboards, notifications, and conflict-aware multi-user review.
52. A/B/C multi-evidence compare. Status: Partial+. Acceptance: compare more than two evidence items with diff, metadata, hash, and note panels. Current: `rapidtriage compare` preserves two-file A/B behavior and now accepts 3+ files with the first as baseline, per-input labels, pairwise result rows, hashes, text diffs, and summary status counts for A/B/C review. Remaining: web-side three-pane compare UI, binary/hex diff, timeline-aware compare, and persistent compare notes.
53. Raw/source hex viewer. Status: Partial+. Acceptance: safe bounded hex/bytes view with offsets, search, copy-safe snippets, and source hash. Current: source preview falls back to a read-only bounded hex view for binary/non-previewable files with offsets, hex bytes, ASCII, preview SHA256, offset range, truncation flags, validation reminders, and byte-offset keyword search citations. Remaining: interactive jump-to-offset, copy-safe byte selection UI, full-file hash display inline for very large files, and exported hex-range evidence citations.
54. SQLite/table specialized viewer. Status: Partial+. Acceptance: browse tables, schemas, rows, FTS hits, and source DB metadata without loading all rows. Current: source viewer opens SQLite read-only, reports database metadata, schema SQL, column type/null/default/PK details, indexes, bounded row samples, truncation flags, and text-column keyword hit citations. Remaining: table-specific pagination controls, WHERE-builder UI, virtual table/FTS ranking, deleted-row recovery, and export-selected-rows workflow.
55. Email conversation viewer. Status: Partial. Acceptance: render threaded mail conversations, attachments, headers, and citations. Current: EML/MBOX source preview now groups bounded messages into thread summaries with participants, message indices, date range, and attachment counts while preserving per-message headers/body previews. Remaining: full PST/OST/MSG native conversations, attachment extraction, message-ID graphing, and report citation export.
56. Image gallery review mode. Status: Partial+. Acceptance: review thumbnails, similarity buckets, perceptual hashes, tags, and report selections. Current: media-image collector and source viewer expose dimensions, hashes, perceptual hash, similarity bucket, thumbnail metadata, OCR sidecar status, tag suggestions, compare readiness, and report-selection hints. Remaining: dedicated multi-image grid, keyboard triage across thumbnails, persistent gallery tags, visual similarity clustering UI, and selected-image report export flow.
57. Video/audio preview and transcript. Status: Partial+. Acceptance: preview bounded media metadata, transcripts/sidecars, hashes, and review marks. Current: source preview recognizes audio/video MIME types, exposes bounded media metadata, parses WAV duration/channel/sample-rate where possible, captures source hashes for reasonably sized media, imports adjacent transcript sidecars, extracts SRT/VTT-style cue timestamps with text hashes, and renders review guidance without playback/transcoding. Remaining: video duration extraction, waveform/thumb previews, hardened playback sandboxing, persistent transcript-to-review citations, and report export of selected cue ranges.
58. OCR queue manager. Status: Partial++. Acceptance: queue files, show OCR state, import sidecars, preserve confidence/language metadata, and retry failures. Current: unified search imports adjacent OCR/transcript sidecars, and `ocr-queue` now builds a persistent per-image queue with `queued`, `sidecar-imported`, and `failed-retry-queued` states, source/sidecar hashes, sidecar text hashes, optional language/confidence/engine metadata, quality metrics, retry tracking, and audit output. Remaining: web queue management, actual OCR worker execution, engine-specific retry logs, and per-case OCR job persistence in Case DB.
59. Korean OCR/translation workflow hardening. Status: Partial++. Acceptance: support Korean OCR/translation sidecars, language hints, confidence, and unchanged originals. Current: UTF-8 OCR sidecar text is preserved/searchable without modifying the original image, media-image artifacts and `ocr-queue` preserve Korean/English language hints, confidence/engine metadata from sidecar JSON, translation sidecars such as `.translation.txt`/`.en.txt`, text hashes, quality metrics, and translation review status. Remaining: built-in Korean OCR engine execution, machine translation worker integration, confidence calibration by engine, and side-by-side original/translation review UI.
60. Search hit deduplication. Status: Partial+. Acceptance: deduplicate repeated hits by source hash, normalized text, entity, artifact ID, and preview. Current: unified search analysis now emits bounded duplicate groups with fingerprints, representative previews, match indices, paths, source counts, and duplicate summary counts. Remaining: UI controls to collapse duplicates, fuzzy near-duplicate text grouping, media perceptual duplicate grouping, and Case DB duplicate suppression.
61. Fuzzy search/stemming/regex proximity search. Status: Partial+. Acceptance: support analyst-grade query modes with performance warnings and tested syntax. Current: completed-run search supports `exact`, `fuzzy`, and `regex` modes, simple stemming/fuzzy matching, and bounded proximity annotations; Case DB FTS remains exact-first for scale.
62. Saved keyword pack library. Status: Partial+. Acceptance: reusable keyword packs, saved searches, recent searches, and case-scoped history. Current: built-in credential/execution/network/browser-AI/Windows-IR/exfiltration packs plus JSON keyword-pack files are supported in CLI/API search flows; full per-case pack editing UI remains pending.
63. IOC/TI enrichment plugin. Status: Partial+. Acceptance: optional offline-safe enrichment for IPs, domains, URLs, hashes, rule IDs, and source/version metadata. Current: `indicators --ti-feed` accepts local JSON/CSV/TXT feeds, supports JSON feed/plugin metadata, annotates matching IOCs with severity/classification/source/note, feed name/version/path, exact or URL-host-domain match mode, and never sends evidence to an external service. Remaining: signed feed packages, STIX/TAXII import, configurable confidence decay, and web-side enrichment management.
64. Report citation manager. Status: Partial+. Acceptance: manage citations, source paths, hashes, parser versions, offsets, reviewer state, and exhibit numbers. Current: Case DB report exports include a citation index linking review decisions to source records and source references; exhibit numbering/editor workflow remains pending.
65. Evidence selection/version history. Status: Partial+. Acceptance: preserve selection changes, notes, review state changes, and report inclusion history. Current: review mark changes are versioned in `review_mark_history` and exported with report candidates; immutable/tamper-evident review history remains later validation work.

## D. Performance And Large Scale

66. 100k/1M/10M record benchmark. Status: Partial+. Acceptance: benchmark index/search/report behavior at these scales with documented latency/resource targets. Current: benchmark outputs now record records/sec, scale target labels, output sizes, p50/p95 search latency, peak memory, and stress guidance; published 1M/10M hardware runs remain required.
67. 1TB-10TB evidence stress test. Status: Partial+. Acceptance: publish repeatable stress runs, bottlenecks, resource caps, and known failure thresholds. Current: benchmark/stress guidance and resume/checkpoint artifacts are generated for repeatable large runs, and `stress-plan` now writes 1TB/5TB/10TB runbook scenarios with wall-clock estimates, output reserve, checkpoint interval, parser batch hints, resource caps, stop thresholds, and required validation evidence. Remaining: actual published 1TB-10TB hardware runs, bottleneck traces, and independent reproduction logs.
68. Incremental indexing. Status: Partial+. Acceptance: reuse completed outputs, detect changed sources, and avoid duplicate Case DB imports. Current: run resume now compares a bounded input fingerprint and disables reuse when source metadata changes; Case DB imports already avoid duplicate default workflow imports.
69. Background job queue. Status: Partial+. Acceptance: run jobs asynchronously with progress, state, warnings, cancellation, and retry. Current: job state persists steps, cancellation requests, queued-job cancellation, and failed/canceled retry; cooperative parser-level cancellation remains pending.
70. Stage checkpoint/resume hardening. Status: Partial+. Acceptance: checkpoint each stage, safely resume skipped/failed/capped work, and preserve previous outputs. Current: every run writes `rapidtriage-run-checkpoints.json` and `rapidtriage-run-fingerprint.json` with stage output/reuse status and resume-effective decisions.
71. Parser crash isolation. Status: Partial+. Acceptance: isolate parser failures so one bad artifact cannot crash a whole case run. Current: artifact collectors are isolated into per-kind outputs with `parser_errors`, `parser_error_count`, and `collection_status=failed-isolated` instead of crashing the entire run.
72. Memory cap enforcement. Status: Partial+. Acceptance: enforce bounded reads/previews/extractions and record cap warnings. Current: run summaries now expose `resource_caps`, previews/search/extracts are bounded, and fingerprint/checkpoint files record truncation/cap context; hard process RSS limits remain pending.
73. Preview sandboxing. Status: Partial+. Acceptance: render previews safely without executing active content or exposing host paths unnecessarily. Current: source previews include `viewer_sandbox` metadata stating read-only bounded mode, no content execution, active-content blocking, and preview byte/char caps.
74. Large SQLite/FTS optimization. Status: Partial+. Acceptance: tune indexes, pagination, inserts, FTS queries, and vacuum/maintenance for large cases. Current: Case DB connections set temp/cache optimization pragmas, run `PRAGMA optimize`, and add review-history target indexes alongside existing FTS/index tables.
75. Parallel parser scheduler. Status: Partial+. Acceptance: schedule independent parsers with CPU/I/O limits, progress, and deterministic output ordering. Current: run artifact collectors execute through a bounded thread scheduler while outputs are written in deterministic profile order; per-parser CPU/I/O quotas remain pending.
76. File hash cache. Status: Partial+. Acceptance: cache hashes by path/size/mtime/inode where safe and invalidate on change. Current: MD5/SHA1/SHA256 helpers now use a process-local path/size/mtime cache reused by Case DB imports and submission hashing.
77. Duplicate file/content detection. Status: Partial+. Acceptance: group duplicate hashes, similar names, near-duplicate media/text, and source occurrences. Current: file scans now hash bounded same-size candidates and emit `duplicate_content_groups`, duplicate group counts, and duplicate file counts.
78. Artifact pagination/cursor API. Status: Partial+. Acceptance: return cursor-paged artifacts/search results for massive datasets. Current: run files/docs/timeline/indicators/artifacts APIs support offset-compatible cursor tokens with `next_cursor` and `previous_cursor`.
79. UI virtualization for massive result tables. Status: Partial+. Acceptance: display large result sets without loading all rows in browser memory. Current: APIs expose cursor pagination, web evidence tabs page result sets, and search/Case DB tables render a bounded visible row window with a responsiveness notice instead of dumping every match into the DOM. Remaining: scroll-position based DOM windowing, keyboard navigation across virtual windows, and persisted row-window state.
80. Long-running job cancellation/retry. Status: Partial+. Acceptance: cancel/retry jobs safely and record partial outputs/warnings. Current: job store/API support cancellation requests, queued-job cancellation, failed/canceled retry, persisted cancellation state, and step messages.

## E. Validation And Legal Defensibility

81. NIST CFReDS/CFTT based known-answer tests. Status: Partial+. Acceptance: run known-answer validation against public corpora and document results. Current: `rapidtriage validation --known-answer-manifest` ingests NIST CFReDS/CFTT-style dataset manifests, records expected answers, evidence paths, status counts, pass/open state, and release-gate wording. Remaining: publish real public-corpus run outputs for each report-grade parser.
82. Parser-specific fixture corpus. Status: Partial+. Acceptance: each parser has fixtures, expected counts, edge cases, and regression tests. Current: validation output now inventories parser fixture areas, fixture/test counts, expected edge cases, and fixture-backed release gates across Windows, browser, mobile, cloud, email, memory, and media/OCR parsers.
83. Parser-specific false positive/false negative documentation. Status: Partial+. Acceptance: document known misses, noise, unsupported structures, and validation needs. Current: validation output includes parser-family FP/FN notes and validation-required guidance for EVTX, Registry/SAM/NTUSER, MFT/USN/Prefetch, Browser/AI, and Mobile/Cloud/Email/Media.
84. Independent validation report. Status: Partial+. Acceptance: produce third-party or independent validation evidence per release. Current: `rapidtriage validation --independent-report` attaches a reviewer report path, SHA256, size, required signoffs, and minimum sections; actual third-party execution remains operator-owned.
85. Tool validation package automation hardening. Status: Partial+. Acceptance: generate validation JSON/Markdown with commands, checks, docs, limitations, and artifacts. Current: validation package writes JSON, Markdown, and `rapidtriage-validation-artifacts.json` containing SHA256/size for generated validation outputs, plus known-answer, fixture corpus, FP/FN, and independent-report sections.
86. Chain-of-custody full workflow. Status: Partial+. Acceptance: evidence IDs, acquisition/source hashes, custody events, tool versions, and report links. Current: Case DB report exports now include `custody_workflow` with evidence source citations, paths, status, source hashes, and custody/audit events linked to report candidates. Remaining: dedicated acquisition-device/write-blocker fields and signed custody handoff forms.
87. Evidence acquisition hash workflow. Status: Partial+. Acceptance: hash source images/exports, extracted files, reports, bundles, and acquisition metadata. Current: Case DB report exports include `acquisition_hash_workflow` summarizing evidence-source and hash-record values, and reviewer bundles/validation packages include output hash manifests. Remaining: whole-device acquisition hash UI and write-blocker/operator metadata capture.
88. Analyst action immutable audit log. Status: Partial+. Acceptance: append-only audit for review, tagging, export, report, and settings changes. Current: review actions already append `audit_event` rows and report export now emits an export-time tamper-evident hash chain with previous/event hashes and head hash. Remaining: database-level append-only enforcement and external notarization/signing.
89. Report reproducibility: same input, same output. Status: Partial+. Acceptance: deterministic ordering, timestamps normalization, schema versions, and reproducibility tests. Current: report export now emits a deterministic `reproducibility` manifest with stable payload SHA256, deterministic sort description, item/citation counts, and volatile-field disclosure. Remaining: end-to-end same-input artifact byte-for-byte report tests across platforms.
90. Report item source provenance completeness. Status: Partial+. Acceptance: every report item cites source path, hash, parser, offset/index where available, and review status. Current: report items now include `provenance` with target/review citations, source path, hashes/record hashes, parser/version/confidence, offsets/index where available, review/verification status, reportability, and evidence strength.
91. Parser confidence scoring. Status: Partial+. Acceptance: every normalized artifact has evidence strength, parser confidence, reportability, and validation warnings. Current: Case DB report items now include `validation_assessment` with parser confidence, reportability, coverage status, validation-required flag, and warning list derived from source metadata/provenance.
92. Validation-required warning UX. Status: Partial+. Acceptance: UI and reports clearly show partial/native/unsupported/parser-required states. Current: report export summaries count validation warnings and each item carries actionable guidance when source hashes, parser confidence, reportability, or commercial-grade readiness need review. Remaining: stronger web visual badges across every table.
93. Legal limitation statement per artifact. Status: Partial+. Acceptance: artifact-specific limitations appear in reports and reviewer views. Current: report items now include `legal_limitations`, preferring parser-provided limitations/blockers and falling back to source-specific legal caution wording.
94. Court exhibit export package. Status: Partial+. Acceptance: export selected evidence, reports, hashes, audit, and provenance in a court-ready bundle. Current: reviewer bundle now writes `rapidtriage-court-exhibit-index.json`, includes it in the ZIP, records generated-output hashes, exhibit IDs, source references, selected item hashes, and verification steps. Remaining: jurisdiction-specific forms and signed/notarized exhibit manifests.
95. External tool version capture. Status: Partial+. Acceptance: capture versions and command evidence for ewfmount, Sleuth Kit, qemu-img, OCR, and parser imports. Current: validation package captures availability/path/version output for Python, ewfmount, Sleuth Kit tools, qemu-img, Tesseract, and Node for release evidence. Remaining: per-run capture for every external parser/import execution.
96. Write-blocker/acquisition metadata recording. Status: Partial+. Acceptance: record acquisition device, write-blocker, operator, time, source, and notes. Current: Case DB stores acquisition metadata with citation IDs, optional evidence-source links, operator, acquisition timestamps, source identifier, write-blocker, tool/version, whole-source SHA256, notes, audit event, CLI record/list commands, and report export missing-field checks. Remaining: web form, write-blocker device integration, signed handoff forms, and acquisition-time hash verification against original imaging logs.
97. Timezone normalization validation. Status: Partial+. Acceptance: preserve original timestamps, normalized UTC, source timezone, and parser assumptions. Current: report exports include `timezone_validation` with event counts, missing timezone counts, timezone distribution, and timestamp samples for reviewer checks.
98. Clock skew analysis. Status: Partial+. Acceptance: detect host/device time skew and annotate timelines/reports. Current: report exports include `clock_skew_analysis` with parsed timestamp range and warnings for obviously impossible pre-1980/future timestamps. Remaining: host-specific baseline comparison and multi-device skew model.
99. Evidence contamination warning. Status: Partial+. Acceptance: warn on writable sources, changed mtimes, generated files inside evidence, and unsafe output paths. Current: report exports include `contamination_warnings` for obvious RapidTriage output under evidence roots, staged output under evidence roots, zero-byte sources, and stat failures. Remaining: persistent acquisition-time mtime comparison and write-blocker integration.
100. Tamper-evident audit bundle. Status: Partial+. Acceptance: hash-chain or manifest-sign audit/report/export packages for tamper evidence. Current: reviewer bundle now writes `rapidtriage-tamper-evident-audit-bundle.json` with generated-output hashes, previous-entry hashes, head hash, and verification steps; Case DB report export also emits an audit event hash chain. Remaining: external signing/notarization.

## F. Deployment, Operations, And Commercialization

101. Windows signed installer. Status: External+ with repo evidence. Acceptance: signed installer, verification evidence, reproducible build steps, and fresh-machine smoke test. Current: release manifest and generated `packaging-plan.json/md` record Windows MSI/EXE target outputs, Authenticode/timestamp requirements, signing steps, required evidence, and Windows smoke scripts are packaged; actual Authenticode signing remains external.
102. macOS notarized package. Status: External+ with repo evidence. Acceptance: notarized package, Gatekeeper validation, reproducible build steps, and smoke test. Current: release manifest and generated `packaging-plan.json/md` record pkg/dmg targets, codesign/notarization/Gatekeeper steps, required evidence, and macOS/Linux smoke script is packaged; actual Apple signing/notarization remains external.
103. Linux package/deb/rpm/AppImage. Status: Partial+. Acceptance: installable Linux packages with dependency handling and smoke tests. Current: portable ZIP, wheel/sdist, Linux/macOS launcher/smoke scripts, dependency inventory, release manifest, and generated packaging plan now define deb/rpm/AppImage targets, clean-container build steps, dependency logs, install/uninstall smoke requirements, and evidence gates. Remaining: actual deb/rpm/AppImage artifact builds and distro smoke logs.
104. Auto-update channel. Status: Partial++. Acceptance: controlled update channel with signed artifacts, release notes, rollback, and enterprise disable option. Current: `scripts/build-release.py` writes `update-manifest.json` with artifact hashes, manual channel, enterprise disable flag, rollback guidance, signature policy, and the generated packaging plan marks public auto-update as blocked until hosting/signing/rollback tests exist. Remaining: hosted signed update service and client updater.
105. Crash reporting. Status: Partial+. Acceptance: local-first crash logs with opt-in export and no evidence exfiltration. Current: web/API unhandled exceptions write local JSON crash reports under `RAPIDTRIAGE_CRASH_LOG_DIR` or `~/.rapidtriage/crash-reports`, redact sensitive context keys, and never upload reports. Remaining: UI export workflow and crash trend dashboard.
106. Telemetry-free/local-only enterprise mode. Status: Partial+. Acceptance: local-only default, explicit remote bind/auth, and no hidden telemetry. Current: `enterprise-policy` CLI/API and validation package report telemetry disabled, evidence/crash uploads disabled, localhost default, and remote auth-token requirement.
107. License/activation system, if needed. Status: Partial+. Acceptance: license flow does not touch evidence and supports offline enterprise workflows. Current: enterprise policy reports license not required by default, records operator-provided offline license file path/status/SHA256/size when configured, confirms `evidence_touch=false` and `network_activation=false`, and documents local-only license notes. Remaining: paid activation workflow if the project needs commercialization.
108. Role-based access control. Status: Partial+. Acceptance: roles, permissions, case access, export controls, and audit logs. Current: enterprise policy documents local analyst/viewer/admin roles, active role support, active permissions, permission matrix, export-control notes, and remote API token gate. Remaining: per-action RBAC enforcement in a real multi-user server.
109. Multi-user case server. Status: Planned+ with explicit guard. Acceptance: shared case server with auth, locking/conflict handling, and scalable search/review. Current: enterprise policy explicitly reports multi-user server `not-enabled`, records network guardrails, and lists required enablement work: identity provider, per-action RBAC, case locking/conflict handling, append-only audit, concurrency migrations, and independent security review. Remaining: implementation of that server.
110. Collaboration audit trail. Status: Partial++. Acceptance: immutable user/action/time/source audit for collaboration workflows. Current: enterprise policy links collaboration audit to Case DB `audit_event` rows, enumerates recorded audit fields, documents single-actor identity limits, and report/reviewer exports include export-time hash-chained audit events. Remaining: multi-user identities, conflict handling, and database-enforced append-only audit.
111. Backup/restore/migration. Status: Partial++. Acceptance: versioned backups, restore validation, schema migrations, and integrity checks. Current: `case-backup` copies Case DB/WAL/SHM files with hashes and manifest, records schema version/table inventory/migration readiness/restore-rehearsal steps, and `case-restore` restores, verifies SHA256, and reports restored schema readiness. Remaining: multi-version migration test corpus and scheduled backup automation.
112. Release notes/changelog discipline. Status: Partial+. Acceptance: every release has changes, known limits, validation state, migration notes, and checksums. Current: release notes template, release checklist, validation package, release manifest, update manifest, and SHA256SUMS are generated/packaged. Remaining: enforced changelog gate in CI.
113. LTS branch/hotfix policy. Status: Partial+. Acceptance: documented support windows, patch policy, branch rules, and backport criteria. Current: `docs/rapidtriage-lts-hotfix-policy.md` documents branch, hotfix, validation, and emergency-fix gates.
114. Support SLA documentation. Status: External+ with documented template. Acceptance: staffed response targets, severity levels, escalation, and patch delivery expectations. Current: `docs/rapidtriage-support-sla.md` documents Sev1-Sev4 targets, update cadence, patch targets, required intake, escalation, secure evidence handling, and release attachment checklist; validation package references the document and release ZIP includes it. Remaining: actual staffed support desk and contractual SLA execution remain operator-owned.
115. Training curriculum. Status: Partial+. Acceptance: structured analyst/admin training, labs, examples, and validation exercises. Current: `docs/rapidtriage-training-curriculum.md` adds analyst quickstart, admin lab, and validation exercise.
116. Analyst quickstart lab. Status: Partial+. Acceptance: guided sample case from ingest to search, review, report, and export. Current: training curriculum and existing sample/smoke workflow guide users through sample run, search, review, report, and reviewer bundle hash verification.
117. Admin deployment guide. Status: Partial+. Acceptance: enterprise install, update, backup, auth, network, logging, and policy guidance. Current: `docs/rapidtriage-admin-deployment-guide.md` covers release verification, smoke tests, auth, crash logs, backup/restore, and hardening.
118. Security hardening review. Status: Partial+. Acceptance: periodic security review for auth, path handling, export rendering, dependencies, and parser safety. Current: admin guide, security policy, enterprise policy, sandbox preview metadata, crash redaction, and dependency monitoring script cover the current hardening baseline. Remaining: independent AppSec review.
119. Malicious evidence sandboxing. Status: Partial+. Acceptance: isolate previews/parsers for hostile files and active content. Current: source previews remain bounded/read-only with active content blocked, reviewer/report exports use CSP/escaping, parser crashes are isolated, and admin guide documents hostile evidence handling. Remaining: OS-level process sandbox for parsers.
120. Dependency vulnerability monitoring. Status: Partial+. Acceptance: scan dependencies, track advisories, patch cadence, and release-blocking severity policy. Current: `scripts/check-dependencies.py` writes a local dependency baseline and optional `pip-audit` result with release-blocking policy; release package includes the script. Remaining: CI scheduled advisory monitoring.
