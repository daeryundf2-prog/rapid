# RapidTriage Parser Coverage Matrix

| Area | Status | Notes |
| --- | --- | --- |
| File metadata | Implemented | Names, paths, extensions, sizes, modified time, categories. |
| KAPE-style collect planning/export | Implemented baseline | `collect-plan` profiles preview present/missing Windows/macOS collection targets before heavy scanning or copying. `collect-export` can build a bounded source-relative export package with SHA256 copy logs. |
| Document text | Implemented | Text/log/config/data, EML/MBOX email, bounded Outlook MSG/PST/OST strings, PDF, Office OpenXML, and OpenDocument where dependencies support extraction. |
| Browser history/downloads | Implemented baseline+ | Chrome, Edge, Brave, Firefox local profiles; history/download fixture coverage; internet usage summaries, top-domain/category pivots, source hashes, source-profile metadata, typed-count/query/transition/duration candidates where available, #20 `core_accuracy_gates`, and bounded unified visit/download timeline rows are normalized for Windows and macOS with browser validation matrix/report-grade assessment metadata. Safari history is macOS-only and should not be treated as cache/session parity. |
| Browser cache/session/extension/sync inventory | Partial++ | Chromium-family profiles now emit bounded browser storage inventory rows for cache, session storage, sessions, extensions, extension state, sync data, Web Data, cookie stores, credential stores, Local Storage, and IndexedDB with counts, byte totals, sample hashes, privacy/legal warnings, strict password/cookie/session legal warnings, #19/#42 `core_accuracy_gates`, #42 secret-handling assessment, #42 `secret_handling_commercial_uplift_evidence`, validation checks, native-capability disclosure, report-grade assessment, and `commercial_grade_ready=false`. Secret values are not decrypted or extracted. |
| Recent files | Implemented baseline+ | Recent shortcuts parse Shell Link header/string fields for target path, working directory, arguments, timestamps, flags, hashes, tracker candidates, #17 `core_accuracy_gates`, and embedded path pivots; Jump Lists traverse recoverable OLE/CFB streams, promote Shell Link destination records with stream provenance, parse bounded DestList header/entry metadata candidates with validation checks, add `jumplist_evidence` and #14 `core_accuracy_gates` for container/DestList/destination review, and fall back to embedded path extraction. |
| Timeline merge | Implemented baseline | Files, docs, artifacts, and normalized timeline export. |
| Case DB FTS | Implemented baseline+ | Documents and artifact metadata are indexed with SQLite FTS5; file metadata, timeline rows, review status filters, verification filters, promoted artifact metadata filters, #51 reviewer assignment/status gates, #60 duplicate-hit analysis gates, #61 advanced search metadata on completed-run search, #62 keyword-pack search inputs, #64 report citation index exports, and #65 versioned evidence-selection history are supported. |
| OCR | Partial+ | OCR queue manifests (#58) now track image candidates, retry state, sidecar hashes/text hashes, metadata sidecars, Korean/English language hints, translation sidecars, quality metrics, item-level and queue-level `core_accuracy_gates`, and report-grade blockers. Native OCR execution remains external and must be validated before report reliance. |
| Image/media triage | Partial+ | Image gallery review metadata (#56), audio/video sidecar transcript review (#57), Korean OCR/translation workflow metadata (#59), dimensions, file hashes, NumPy-aggregate-free perceptual average hash, similarity bucket, bounded inline thumbnail previews, rule-based visual classification hints, source-viewer `core_accuracy_gates`, and OCR/translation validation metadata are implemented for common image/media files. |
| Windows OS/account summary | Partial+ | User profile inventory plus `.reg` export hints for computer name, timezone, ProfileList SIDs, admin-group hints, current control set, last boot/shutdown timestamps, exported account lifecycle fields such as created, last logon, password-last-set, disabled/admin hints, UAC flags, SAM F/V binary presence/byte counts, first-pass SAM F timestamp/RID/UAC candidates, SAM V UTF-16 string candidates, account-centered security context linking group SID candidates to inherited privilege assignments, and `forensic_review` summaries for #6 review. Native SAM hive candidates expose bounded account-name/RID and group/alias key rows with offsets, hashes, last-write hints, validation matrices, native capability disclosure, guidance, explicit blockers, `core_accuracy_gates`, and `account_privilege_deep_parse_profile` metadata covering SAM F/V, SAM alias membership, SECURITY secret metadata, privilege mapping, legal handling, and required independent checks; full OS-version validated SAM F/V decoding, SECURITY secret decryption, and native binary group membership reconstruction remain planned. |
| #1-#120 accuracy profiles | Implemented validation layer | `rapidtriage validation` now emits `core_forensics_accuracy_profiles` for every item from native EVTX through dependency vulnerability monitoring. Each profile defines the parser/legal/operations surface, required known-answer corpus, independent oracle, required checks, minimum evidence, source/hash/offset/legal/resource controls, pass/fail rules, and default `validation-required` reportability. This improves validation discipline and accuracy review, but does not itself make the parsers commercial-grade without attached corpus/cross-tool/large-case/independent/legal/signing/operations evidence. |
| Windows Event Logs | Partial+ | XML/JSON/JSONL/CSV exports from EVTX-oriented tools parse into normalized event/detection rows with hashes, categories, event/channel families, typed parameters, imported rendered messages, parser confidence, risk flags, reportability status, expanded built-in detections for high-value Windows/Sysmon/Defender/WMI/RDP behaviors, high-risk summary pivots, and EventRecordID gap hints; binary EVTX is inventoried and recoverable native record headers emit partial rows with timestamps, record IDs, file-header/chunk context, extracted strings, parameter candidates, native message previews, explicit BinXML decode status, first-pass BinXML token scans for fragment headers/elements/attributes/value text/template headers, inline String/ANSI/integer/bool/float/GUID/SID/FILETIME/SYSTIME/binary scalar decoding, TemplateInstance IDs, TemplateInstance value spec/value decoding for common scalar types, substitution value fields, promoted `Event/System`, duplicate-preserving ordered `EventData` sequences, grouped EventData values by name, `UserData` fields, normalized EventID/provider/channel/computer/level/timestamp/process/thread/user-SID/command pivots, `event_message`/`message_rendering` provenance/confidence/limitations, optional curated JSON provider message catalogs, Windows Event Manifest XML (`.man`/`.xml`) catalog string-table resolution, high-value fallback message templates, rendered previews where possible, `eventlog-chunk` structure rows, `evtx_validation_checks`, `evtx_validation_matrix`, #1~#3 `core_accuracy_gates`, `evtx_report_grade_assessment`, chunk-boundary status counts, native capability disclosure, `evtx_recovery_context`, `evtx_recovery_validation_profile`, slack/deleted/corrupt candidate labels, separate `eventlog-record-candidate` rows for invalid record headers, and suspicious-term flags. Full provider DLL/resource-table message extraction and broad deleted-record corpus validation remain planned. |
| Registry hives | Partial+ | `.reg` exports parse into key/value rows including Run keys, suspicious persistence value hints, and USBSTOR device metadata. Native hive candidates such as `NTUSER.DAT`, `UsrClass.dat`, `SYSTEM`, and `SOFTWARE` are inventoried with `regf` header metadata, source hashes, sequence/dirty hints, last-write timestamp, bounded UTF-16 string pivots, hbin-aware bounded `nk`/`vk` cell rows, best-effort `registry-key-tree-node` rows from native parent/subkey/value-list metadata with linked/missing subkey/value offsets, root-cell reachability, parent/child backlink consistency, validation flags, `registry_key_tree_relationships`, `registry_validation_matrix`, #4 `core_accuracy_gates`, `registry_report_grade_assessment`, and native capability disclosure, separate `registry-deleted-cell-candidate` rows for positive-size free/deleted key/value candidates, key-specific `registry-key-recovery-candidate` rows, and value-specific `registry-value-recovery-candidate` rows with cautious parent-key/data previews, parent-key confidence, #5 `core_accuracy_gates`, `registry_recovery_evidence`, and `registry_recovery_validation_profile` fields that keep deleted/free cells non-reportable until second-parser, allocator, transaction-log, and known-answer checks are complete. User-hive activity pivots now emit `registry-user-activity` rows for UserAssist, TypedURLs/TypedPaths, RecentDocs, Run/RunOnce, Explorer/MRU, ShellBags, MountPoints2, Network, and ComDlg32/OpenSavePidlMRU keys from `.reg` exports and bounded native NTUSER/UsrClass string scans; binary value decoding, transaction-log replay, and deleted-value testimony still require validation with a dedicated hive parser. |
| ShellBags | Partial+ | `.reg` ShellBags keys are normalized as `shellbag-key` rows. Native NTUSER/UsrClass hives now emit validation-required `shellbag-native-candidate` rows from bounded key-tree and string pivots with source key paths, bag/node ID candidates, timestamp candidates, parser confidence, validation checks, #15 `core_accuracy_gates`, `shellbag_evidence`, shared `forensic_review`, and `commercial_grade_ready=false`; full binary shell-item folder decoding, bag/node relationship validation, transaction-log replay, and deleted/slack testimony remain planned. |
| MFT | Partial+ | MFT CSV/JSON/JSONL exports are normalized with record number, path, timestamps, and deleted hints; native `$MFT` files are inventoried with hashes, bounded FILE record-header samples, UTF-16 path pivots, update-sequence-array validation counts, timestamp validation counts, and attribute type counts. Separate native `mft-record` rows now expose FILE header fields, common attributes, parent-reference metadata, resident/nonresident hints, `mft_record_evidence`, shared `forensic_review`, validation checks, #12 `core_accuracy_gates`, parser confidence, and explicit commercial-readiness blockers. Full attribute-list extension resolution, report-grade nonresident data-run decoding, and broad corpus validation still require a dedicated parser. |
| Zone.Identifier ADS | Partial | Exported `:Zone.Identifier`/`.Zone.Identifier` sidecar files parse ZoneId, referrer URL, and host URL. |
| Defender/Firewall/WER/Task Scheduler/WMI | Partial+ with explicit commercial gate | Fixture-backed inventory for Defender MPLog, Windows Firewall W3C logs, WER reports, Task Scheduler XML, and WMI repository files; Task Scheduler rows now emit normalized action/trigger/principal metadata, executable names, command lines, timestamp/source hashes, validation checks, #18 `core_accuracy_gates`, validation matrix, native-capability disclosure, shared `forensic_review`, and commercial-readiness blockers for missing TaskCache/security-descriptor/history correlation. Defender, Firewall, WER, and WMI rows also expose `system_report_grade_assessment`, #18 `forensic_review`, #18 `core_accuracy_gates`, and native-capability disclosure. WER rows normalize application/module paths, exception codes, report IDs, FILETIME/ISO event times, problem signatures, source hashes, validation checks, and blockers for missing dump/cab/ReportQueue validation. WMI repository files include bounded string pivots for permanent event consumer/filter terms, commands, paths, URLs, and risk flags. |
| Prefetch / execution pivots | Partial+ | `.pf` inventory with executable hints, SCCA header detection, version-specific common-layout metadata for versions 17/23/26/30/31, header executable-name extraction, best-effort run count, last run timestamps, validation checks, bounded referenced path pivots, volume/file-reference candidates, separate validation-required `prefetch-reference` rows, #16 `core_accuracy_gates`, and shared `forensic_review` summaries for #16. Execution-related registry export and PowerShell history imports are available through `windows-execution`; Amcache, ShimCache/AppCompatCache, BAM/DAM, SRUM imports, and native SRUDB candidates now expose validation matrices, report-grade assessments, evidence summaries, native capability disclosure, grouped summary counts, shared `forensic_review` summaries, item-specific `core_accuracy_gates`, and artifact-specific profiles (`amcache_schema_profile`, `shimcache_execution_caveat_profile`, `bam_dam_decode_profile`, `srum_ese_validation_profile`) for #7~#10. |
| SRUM | Partial+ | SRUM CSV/JSON/JSONL/NDJSON exports can be normalized into app resource and network usage rows with bytes totals, interface/profile, energy/CPU, user, timestamp, source hashes, counter validation checks, `execution_validation_matrix`, and `execution_report_grade_assessment`; direct `SRUDB.dat` is inventoried with ESE header metadata, native SRUDB validation metadata, hashes, bounded strings, path/URL pivots, suspicious-term flags, separate `srum-database-pivot` rows, `srum-table-candidate` rows with marker offsets/counts, and bounded `srum-row-candidate` rows for native string clusters with app/user/timestamp/counter candidates. Native SRUDB rows expose native capability disclosure and remain validation-required with explicit commercial-readiness blockers; full native SRUM table timestamp/counter decoding still requires a dedicated parser. |
| Windows Search EDB | Partial+ | `Windows.edb` is inventoried with ESE header metadata, hashes, bounded strings, native validation fields, path/URL/content pivots, export-tool guidance, explicit commercial-readiness blockers, shared `forensic_review`, #11 `core_accuracy_gates`, and separate `windows-search-edb-pivot`, `windows-search-edb-page-candidate`, `windows-search-edb-table-candidate`, and correlated `windows-search-edb-row-candidate` rows for likely gather/property/content/deleted-state review. The page-candidate method preserves ESE page offset, page hash, page-local table markers, and page-local path/URL/content/risk strings so analysts can verify a hit at a stable page location instead of relying only on a global string list. CSV/JSON/JSONL exports are normalized into searchable index entries with path, title, URL, extension, timestamps, and content snippets. Full ESE catalog/table/row decoding, row-level timestamps, and deleted/index-state validation still require a dedicated Windows Search parser. |
| KakaoTalk Windows correlation | Triage+ | `kakaotalk-windows` inventories PC KakaoTalk application `.edb`/`.db`/`.dat` stores, DB/WAL/SHM/copy companions, hashes, SQLite accessibility, header signatures, likely roles such as chat log, chat list, profile, contact, media, login state, client state, and post-BigBang adjacent indexes, plus legacy decryption-readiness metadata such as 4096-byte page alignment and expected SQLite-header validation. It also records redacted `DeviceInfo` context and userId/account identifier candidates from registry exports/native hives, then correlates evidence across `Windows.edb`, Registry, and bounded memory dumps. It emits `kakaotalk-windows-app-database`, `kakaotalk-windows-crypto-material-candidate`, `kakaotalk-windows-user-id-candidate`, and `kakaotalk-windows-source-candidate` rows with source family, source hash, offset or ESE page hash where available, matched KakaoTalk terms, path/URL/chat-store/process classification, privacy warnings, and #31 `forensic_review`; a `kakaotalk-windows-correlation-summary` groups source families and reports whether evidence is single-source, moderate, or strong. This is not message DB decryption or report-grade chat testimony by itself; post-BigBang KakaoTalk behavior still requires known-answer validation. |
| KakaoTalk decrypt/key-store/report commands | Partial+ | `kakaotalk-decrypt` processes authorized Windows KakaoTalk `chatLogs_*.edb` files with externally supplied direct AES key/IV, pragma/userId, or DeviceInfo+pragma-key+userId material. It can decode `sys_uuid`, `hdd_model`, and `hdd_serial` from `NTUSER.DAT`, derive multiple research-backed pragma variants with an externally supplied pragma-key, auto-use one unambiguous redacted userId candidate, or try multiple candidate-derived keys until one validates the SQLite header. It decrypts 4096-byte AES-CBC pages through pycryptodome when installed or OpenSSL fallback, opens the DB read-only, identifies message-table candidates, counts rows, emits source/decrypted hashes, and includes bounded message previews only when explicitly requested. `scripts/kakaotalk_zip_to_report.py` accepts ZIP, extracted folder, and single evidence inputs, rejects unsafe ZIP traversal/symlink/size-ratio patterns, and writes summary, rooms/messages/media CSVs, and per-database count/status CSVs so raw recovered row counts are not confused with bounded viewer-visible rows. `kakaotalk-key-store-inspect` now parses post-BigBang `appstate.dat`/backup CBOR key stores, maps `wrapped_dek_map` entries to EDB/chatLogs files, records salt/wrapped-DEK lengths and hashes, and checks memory residency without exporting raw key bytes. It does not yet recover the entropy-bound KEK/IKM or unwrap DEKs, so post-patch direct message DB opening remains validation-gated. |
| Windows remote access | Implemented baseline+ | RDP files, Terminal Server Client cache files, and exported Terminal Server Client registry destinations are normalized for remote-access triage; cache files include bounded PNG/JPEG/BMP/DIB thumbnail signature pivots while full vendor-specific cache decoding remains planned. |
| Jump Lists/LNK | Partial+ | LNK header/string parsing, target timestamps, LinkInfo/StringData/ExtraData, tracker candidates, embedded path extraction, validation checks, and shared `forensic_review` for #17 are implemented; Jump Lists are inventoried with OLE/custom hints, recover OLE/CFB stream listings, promote embedded Shell Link destination records with stream provenance when present, and expose DestList header/entry metadata candidates including stream-link candidates, timestamps, host/account-adjacent fields, validation checks, and explicit commercial blockers. Full OS-version-specific DestList field testimony, deleted entry recovery, and account attribution remain planned. |
| USN Journal | Partial+ | USN CSV/JSON/JSONL exports are normalized with FRN/parent/reason/timestamp fields; native `$J`/USN journal files are inventoried and recoverable v2/v3 records are emitted as triage `usn-record` rows with cursor/next-cursor offsets, bounded scan metadata, large-record sizing, layout/name/timestamp validation, reason/source/file-attribute flags, delete/rename hints, #13 `core_accuracy_gates`, `usn_record_evidence`, shared `forensic_review`, parser confidence, and validation warnings. Full replay/correlation and report-grade large-corpus pagination validation remain planned. |
| macOS user/browser/quarantine/TCC | Implemented baseline+ | User home inventory, Safari/Chromium/Firefox history imports with web/AI usage pivots, LaunchServices quarantine events, TCC privacy permission rows with high-value permission risk flags, LaunchAgent plist inventory, and bounded native pivots for Unified Logs, Spotlight stores, FSEvents files, and APFS snapshot hints are available through `macos-system`. |
| Linux system artifacts | Implemented baseline+ | User inventory, shell history, SSH authorized keys/known hosts, auth log events, auditd events, dpkg package/status events, Docker container configs, cron entries, and systemd service units are normalized through `linux-system` with source hashes and suspicious command/path risk flags. |
| A/B/C file compare | Implemented baseline+ | `compare` compares two or more individual files with MD5/SHA1/SHA256, baseline-labeled pairwise differences, bounded UTF-8 text diff previews, summary status counts, #52 `core_accuracy_gates`, case bookmark support, and case-report review pivots. |
| Volume Shadow Copy compare/extract | Implemented baseline+ | `vsc-compare` compares current vs one or more snapshot folders and emits deleted/added/modified candidates with optional SHA256 confirmation; `vsc-extract` copies selected deleted/modified snapshot-side files into an evidence package with source/destination SHA256 values. VSC deletion command hints can also be surfaced from EVTX/PowerShell history imports. |
| Deleted-file carving | Implemented bounded baseline | `carve` scans files or mounted/exported folders for JPEG, PNG, PDF, and ZIP signatures with byte offsets, SHA256, caps, status, and optional extraction under analyst control. |
| Linux XFS | Direct-extract when tools support it | Image partition selection recognizes Linux/ext/XFS partitions before Sleuth Kit recovery; actual file recovery depends on the installed Sleuth Kit build's filesystem support. |
| E01/Ex01 | Direct-extract when tools exist | UI/API adapter detection plus `ewfmount`, `mmls`, and `tsk_recover` recovery into the run output folder; source integrity, tool preflight, partition selection, command history, read-only safety notes, #22 `core_accuracy_gates`, and `commercial_grade_ready=false` metadata are emitted. |
| Forensic containers AD1/L01/Lx01/AFF/AFF4/AFF4-L | Detection + export-first audit guidance | UI/API adapter detection records source integrity when bounded, limitation warnings, export-first actions, #25 `core_accuracy_gates`, and `commercial_grade_ready=false`; direct parsing requires vendor/tool export first. |
| Raw/split images DD/RAW/IMG/001 | Direct-extract when tools exist | UI/API adapter detection plus `mmls`/`tsk_recover` recovery into the run output folder; split numeric segments are discovered, gap/start warnings are recorded, source-part integrity is captured when bounded, partition metadata/selected offsets, command history, report-grade blockers, #23 `core_accuracy_gates`, and `forensic_review` are emitted, and `commercial_grade_ready=false` remains until large known-answer validation exists. |
| Optical/archive images ISO/DMG/WIM/SWM | Direct-extract when tools exist | UI/API adapter detection plus `7zz`/`7z` extraction, with `bsdtar` fallback for ISO. |
| Virtual disks VHD/VHDX/VMDK/VDI/QCOW/QCOW2 | Direct-convert/extract when tools exist | UI/API adapter detection plus `qemu-img` conversion to raw and Sleuth Kit recovery; output records tool preflight, source/converted integrity, command history, nested raw recovery metadata, warnings, #24 `core_accuracy_gates`, and `commercial_grade_ready=false`. XVA remains vendor/export-first with source integrity, #24 `core_accuracy_gates`, and export guidance. |
| Memory dumps MEM/DMP/VMEM/VMSS/VMSN/HPAK | Detection + import+ | Memory dump detection exists; Volatility/Volatility3 JSON/JSONL outputs can be imported as process, network, cmdline, and malfind artifacts. Direct `.mem`, `.raw`, `.vmem`, `.vmss`, `.vmsn`, `.hpak`, `.dmp`, and memory-named `.bin` dumps receive bounded indicator scans for redacted BitLocker recovery-key candidates, suspicious strings, URLs, and IPs. |
| APK malware triage | Partial+ | Exported `.apk` files are inventoried with hashes, manifest metadata, permissions, components, dex/native-library counts, entry hashes, native architectures, bounded suspicious string pivots, URL/IP indicators, certificate/signature limitation evidence, validation checks, Android validation matrix, native-capability disclosure, #30 `core_accuracy_gates`, commercial blockers, shared `forensic_review`, and risk flags. Android app data export files under common `Android/data`, `Android/media`, and `data/data` paths are inventoried by inferred package/category with communication/browser/media store hints, schema profile metadata, #29/#30 `core_accuracy_gates`, and no secret extraction. |
| Memory forensics | Partial+ | Volatility-style output normalization is implemented; direct RAM dump scans now produce bounded BitLocker/string/network pivots, while full process reconstruction, key validation, and process-risk visualization remain long-term work. |
| Indicator and IOC summary | Implemented baseline+ | Completed run outputs can be summarized into URL, domain, IP, and hash pivots with source pointers, optional local `--rules` IOC matching, #63 offline JSON/CSV/TXT TI feed enrichment with feed provenance, `core_accuracy_gates`, and explicit local-only/no-external-call warnings; external TI API enrichment remains disabled/outside core. |
| Large-case performance workflow | Partial++ | `benchmark` emits #66 100k/1M/10M scale-target metadata, p50/p95 search latency, records/sec, output size, memory metrics, and #66 `commercial_uplift_evidence`. `stress-plan` emits #67 1TB/5TB/10TB runbook scenarios and plan/scenario #67 uplift evidence. Runs emit #68 bounded input fingerprints, #70 stage checkpoints, #68/#70 uplift evidence, #71 isolated parser-crash payloads, #72 optional RSS memory-cap enforcement, and #75 parallel parser scheduler assessment; web/API jobs expose #69 local background queue state, queue/step uplift evidence, and #80 cancellation/retry assessment. Source previews expose #73 read-only bounded sandbox metadata; SQLite/Case DB viewers expose #74 FTS/index optimization metadata; hash requests and duplicate scans expose #76 hash-cache and #77 duplicate-detection metadata; API pages expose #78 cursor metadata, and web tables expose #79 bounded-DOM virtualization notices. Published hardware-scale validation remains required. |
| Report/legal validation workflow | Partial+ | Validation packages expose #81 known-answer status, #82 fixture-corpus inventory, #83 FP/FN notes, #84 independent-report attachment metadata, #85 validation-package hashes, and #95 external tool version capture. Case DB report exports expose #86 custody, #87 acquisition hashes, #88 audit hash chains, #89 reproducibility, #90 provenance, #91 parser confidence, #92 validation warnings, #93 legal limitations, #96 acquisition/write-blocker metadata, #97 timezone validation, #98 clock-skew analysis, and #99 contamination warnings. Reviewer bundles expose #94 court exhibit indexes and #100 tamper-evident generated-output hash chains. External signing, notarization, and jurisdiction-specific exhibit forms remain operator-owned. |
| AI prompt artifacts | Partial+ | Browser history detects common AI service visits such as ChatGPT, Claude, Gemini, Perplexity, Copilot, Poe, Hugging Face Chat, Grok, You.com, Phind, Mistral, DeepSeek, Meta AI, Character.AI, and Notion AI. Browser Local Storage, Session Storage, IndexedDB, and Cache files are scanned for role/content, prompt, question, answer, response, and completion snippets as review-only conversation candidates. Recovered question/answer snippets are paired into transcript pivots with completeness scoring, orphan counts, source hashes, source storage provenance, offset candidates, pairing confidence summaries, validation checks, #21 `core_accuracy_gates`, privacy warnings, and `commercial_grade_ready=false`; full service-side transcript completeness still requires service-specific validation. |
| Cloud export import | Partial+ | Google Takeout-style location/activity/account rows, Gmail/Drive-style mail/file rows, Apple/iCloud account/file/photo rows, Microsoft/OneDrive/Teams/Audit rows, and collaboration-SaaS message/file-style rows are normalized with source hashes, provider profiles, issue matrices, validation matrices, native-capability disclosure, #37/#38/#39 `core_accuracy_gates`, #37/#38/#39 `forensic_review`, and explicit provider completeness blockers. |
| Mobile/cloud acquisition | Partial+ | Exported APK and cloud export imports exist. Cloud API collection now adds #40/#41 `core_accuracy_gates`, #40 acquisition review, #41 credential-security assessment, redacted request credential metadata, environment-variable-only token handling, and secure-vault/scope-capture blockers. Cellebrite/XRY/GrayKey/AXIOM-style CSV/JSON exports normalize messages, contacts, calls, apps, files, accounts, media, browser rows, source IDs, hashes, validation matrix, native-capability disclosure, #26 `core_accuracy_gates`, shared `forensic_review`, and validation blockers. KakaoTalk, WhatsApp, Telegram, Signal, WeChat, LINE, Discord, Instagram, iMessage, Facebook Messenger, Viber, Skype, Slack, Microsoft Teams, Reddit, X/Twitter, TikTok, Snapchat, Matrix/Element, Wire, Threema, Session, and Wickr authorized export rows are service-labeled with chat/message IDs, participants, media references, reactions, text hashes, service-specific #31~#35 gap mapping, #31~#35 `core_accuracy_gates`, `chat_app_forensic_review`, `chat_app_scope_profile`, `chat_app_issue_matrix`, native-capability disclosure, and validation checks. KakaoTalk rows include `kakaotalk_compatibility_assessment` and mark 25.7.2 / 2025-08-13+ as post-BigBang legacy-method-incompatible. App SQLite/DB candidates are inventory-only with table/column/count summaries and no decryption. Authorized iOS backups now surface `Manifest.db`, `Info.plist`, `Status.plist`, and keychain inventory candidates with #27/#28 `core_accuracy_gates`, controlled-reveal audit gates, and shared `forensic_review`, without decrypting or exposing secrets. Direct cloud/device acquisition remains deferred. |
| Mobile correlation/schema management | Partial++ | Mobile export sources emit contact/call/message/media/app normalized rows plus `mobile-correlation-summary` rows with counts, services, participants, message-media candidate links, contact/call/SMS actor view, app/schema version registry, #43~#45 `core_accuracy_gates`, #43~#45 `mobile_correlation_commercial_uplift_evidence`, report-grade blockers, row caps, pass/fail validation IDs, and correlation-readiness checks. This is source-export scoped and not yet a full device-wide timeline/entity-resolution system. |
| Email/mailbox | Partial+ | EML/EMLX/MBOX/Maildir parsing emits headers, message IDs, body previews/hashes, attachment metadata/hashes, source hashes, legal warnings, #36 gap mapping, #36 `core_accuracy_gates`, format profiles, issue matrices, validation matrix, native-capability disclosure, and validation blockers. PST/OST/MSG support is bounded string/email/subject candidate inventory only; native mailbox folder/message/deleted-item decoding requires dedicated tooling. |
| Cloud exports | Partial++ | Google Takeout location/activity/account rows, Gmail/Drive-style mail/file JSON rows, Apple/iCloud account/file rows, Microsoft 365/OneDrive/Teams/Audit JSON rows, and Slack/Dropbox/Box-style collaboration SaaS rows normalize timestamps, actors, accounts, URLs, files, Teams/chats, audit events, hashes, risk flags, provider profile, issue matrix, validation matrix, native-capability disclosure, #37/#38/#39 `core_accuracy_gates`, and #37/#38/#39 gap mapping. `cloud-collect` provides authorized bounded API collection with redacted credentials, response hashes, dry-run, #40/#41 `core_accuracy_gates`, #40/#41 `commercial_uplift_evidence`, validation metadata, and audit output; provider-native acquisition remains planned. |

Every new parser should publish:

- Stable parser ID and version.
- Input evidence type.
- Normalized output model.
- Fixture path and expected output.
- Known limitations.

Validation package coverage now exposes #81 known-answer test manifests, #82 parser fixture corpus inventory, #83 parser false-positive/false-negative notes, #84 independent validation report attachment/hashing, and #85 validation-package automation metadata. These fields document release evidence readiness; they do not by themselves make a parser court-ready without attached test output and reviewer sign-off.
