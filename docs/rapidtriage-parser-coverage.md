# RapidTriage Parser Coverage Matrix

| Area | Status | Notes |
| --- | --- | --- |
| File metadata | Implemented | Names, paths, extensions, sizes, modified time, categories. |
| KAPE-style collect planning/export | Implemented baseline | `collect-plan` profiles preview present/missing Windows/macOS collection targets before heavy scanning or copying. `collect-export` can build a bounded source-relative export package with SHA256 copy logs. |
| Document text | Implemented | Text/log/config/data, EML/MBOX email, bounded Outlook MSG/PST/OST strings, PDF, Office OpenXML, and OpenDocument where dependencies support extraction. |
| Browser history/downloads | Implemented baseline+ | Chrome, Edge, Brave, Firefox local profiles; history/download fixture coverage; internet usage summaries, top-domain/category pivots, source hashes, source-profile metadata, typed-count/query/transition/duration candidates where available, and bounded unified visit/download timeline rows are normalized for Windows and macOS with browser validation matrix/report-grade assessment metadata. Safari history is macOS-only and should not be treated as cache/session parity. |
| Browser cache/session/extension/sync inventory | Partial+ | Chromium-family profiles now emit bounded browser storage inventory rows for cache, session storage, sessions, extensions, extension state, sync data, Web Data, cookie stores, credential stores, Local Storage, and IndexedDB with counts, byte totals, sample hashes, privacy/legal warnings, strict password/cookie/session legal warnings, #42 secret-handling assessment, validation checks, native-capability disclosure, report-grade assessment, and `commercial_grade_ready=false`. Secret values are not decrypted or extracted. |
| Recent files | Implemented baseline+ | Recent shortcuts parse Shell Link header/string fields for target path, working directory, arguments, timestamps, flags, hashes, and embedded path pivots; Jump Lists traverse recoverable OLE/CFB streams, promote Shell Link destination records with stream provenance, parse bounded DestList header/entry metadata candidates with validation checks, and fall back to embedded path extraction. |
| Timeline merge | Implemented baseline | Files, docs, artifacts, and normalized timeline export. |
| Case DB FTS | Implemented baseline | Documents and artifact metadata are indexed with SQLite FTS5; file metadata, timeline rows, review status filters, verification filters, promoted artifact metadata filters, #64 report citation index exports, and #65 versioned evidence-selection history are supported. |
| OCR | Partial+ | OCR queue manifests (#58) now track image candidates, retry state, sidecar hashes/text hashes, metadata sidecars, Korean/English language hints, translation sidecars, quality metrics, and report-grade blockers. Native OCR execution remains external and must be validated before report reliance. |
| Image/media triage | Partial+ | Image gallery review metadata (#56), audio/video sidecar transcript review (#57), Korean OCR/translation workflow metadata (#59), dimensions, file hashes, NumPy-aggregate-free perceptual average hash, similarity bucket, bounded inline thumbnail previews, rule-based visual classification hints, and OCR/translation validation metadata are implemented for common image/media files. |
| Windows OS/account summary | Partial+ | User profile inventory plus `.reg` export hints for computer name, timezone, ProfileList SIDs, admin-group hints, current control set, last boot/shutdown timestamps, exported account lifecycle fields such as created, last logon, password-last-set, disabled/admin hints, UAC flags, SAM F/V binary presence/byte counts, first-pass SAM F timestamp/RID/UAC candidates, SAM V UTF-16 string candidates, separate account lifecycle rows, service configuration, mounted devices, SECURITY/LSA sensitive locations with value names/type/byte-count/SHA256/entropy/timestamp-candidate metadata, privilege assignments, and separate exported group membership hint rows with SID/name source typing. Native SAM hive candidates expose bounded account-name/RID and group/alias key rows with offsets, hashes, last-write hints, `os_account_validation_matrix`, `os_account_report_grade_assessment`, native capability disclosure, validation guidance, and explicit commercial-readiness blockers; full OS-version validated SAM F/V decoding, SECURITY secret decryption, and native binary group membership reconstruction remain planned. |
| Windows Event Logs | Partial+ | XML/JSON/JSONL/CSV exports from EVTX-oriented tools parse into normalized event/detection rows with hashes, categories, event/channel families, typed parameters, imported rendered messages, parser confidence, risk flags, reportability status, expanded built-in detections for high-value Windows/Sysmon/Defender/WMI/RDP behaviors, high-risk summary pivots, and EventRecordID gap hints; binary EVTX is inventoried and recoverable native record headers emit partial rows with timestamps, record IDs, file-header/chunk context, extracted strings, parameter candidates, native message previews, explicit BinXML decode status, first-pass BinXML token scans for fragment headers/elements/attributes/value text/template headers, inline String/ANSI/integer/bool/float/GUID/SID/FILETIME/SYSTIME/binary scalar decoding, TemplateInstance IDs, TemplateInstance value spec/value decoding for common scalar types, substitution value fields, promoted `Event/System`, `EventData`, and `UserData` fields, normalized EventID/provider/channel/computer/level/timestamp/process/thread/user-SID/command pivots, `event_message`/`message_rendering` provenance/confidence/limitations, high-value fallback message templates, rendered previews where possible, `eventlog-chunk` structure rows, `evtx_validation_checks`, `evtx_validation_matrix`, `evtx_report_grade_assessment`, chunk-boundary status counts, native capability disclosure, `evtx_recovery_context`, slack/deleted/corrupt candidate labels, separate `eventlog-record-candidate` rows for invalid record headers, and suspicious-term flags. Full provider DLL/resource-table message rendering and broad deleted-record corpus validation remain planned. |
| Registry hives | Partial+ | `.reg` exports parse into key/value rows including Run keys, suspicious persistence value hints, and USBSTOR device metadata. Native hive candidates such as `NTUSER.DAT`, `UsrClass.dat`, `SYSTEM`, and `SOFTWARE` are inventoried with `regf` header metadata, source hashes, sequence/dirty hints, last-write timestamp, bounded UTF-16 string pivots, hbin-aware bounded `nk`/`vk` cell rows, best-effort `registry-key-tree-node` rows from native parent/subkey/value-list metadata with linked/missing subkey/value offsets, validation flags, `registry_validation_matrix`, `registry_report_grade_assessment`, and native capability disclosure, separate `registry-deleted-cell-candidate` rows for positive-size free/deleted key/value candidates, key-specific `registry-key-recovery-candidate` rows, and value-specific `registry-value-recovery-candidate` rows with cautious parent-key/data previews and parent-key confidence when recoverable. User-hive activity pivots now emit `registry-user-activity` rows for UserAssist, TypedURLs/TypedPaths, RecentDocs, Run/RunOnce, Explorer/MRU, ShellBags, MountPoints2, Network, and ComDlg32/OpenSavePidlMRU keys from `.reg` exports and bounded native NTUSER/UsrClass string scans; binary value decoding, transaction-log replay, and deleted-value testimony still require validation with a dedicated hive parser. |
| ShellBags | Partial+ | `.reg` ShellBags keys are normalized as `shellbag-key` rows. Native NTUSER/UsrClass hives now emit validation-required `shellbag-native-candidate` rows from bounded key-tree and string pivots with source key paths, bag/node ID candidates, timestamp candidates, parser confidence, validation checks, and `commercial_grade_ready=false`; full binary shell-item folder decoding, bag/node relationship validation, transaction-log replay, and deleted/slack testimony remain planned. |
| MFT | Partial+ | MFT CSV/JSON/JSONL exports are normalized with record number, path, timestamps, and deleted hints; native `$MFT` files are inventoried with hashes, bounded FILE record-header samples, UTF-16 path pivots, update-sequence-array validation counts, timestamp validation counts, and attribute type counts. Separate native `mft-record` rows now expose FILE header fields, in-use/deleted hints, base references, common `$STANDARD_INFORMATION`/`$FILE_NAME`/`$DATA` attribute summaries, FILETIME timestamp sources, parent-reference metadata, resident-data hashes, nonresident runlist previews, validation checks, parser confidence, and explicit commercial-readiness blockers. Full attribute-list extension resolution, report-grade nonresident data-run decoding, and broad corpus validation still require a dedicated parser. |
| Zone.Identifier ADS | Partial | Exported `:Zone.Identifier`/`.Zone.Identifier` sidecar files parse ZoneId, referrer URL, and host URL. |
| Defender/Firewall/WER/Task Scheduler/WMI | Partial+ with explicit commercial gate | Fixture-backed inventory for Defender MPLog, Windows Firewall W3C logs, WER reports, Task Scheduler XML, and WMI repository files; Task Scheduler rows now emit normalized action/trigger/principal metadata, executable names, command lines, timestamp/source hashes, validation checks, validation matrix, native-capability disclosure, and commercial-readiness blockers for missing TaskCache/security-descriptor/history correlation. Defender, Firewall, WER, and WMI rows also expose `system_report_grade_assessment` with #18 gap mapping. WER rows normalize application/module paths, exception codes, report IDs, FILETIME/ISO event times, problem signatures, source hashes, validation checks, and blockers for missing dump/cab/ReportQueue validation. WMI repository files include bounded string pivots for permanent event consumer/filter terms, commands, paths, URLs, and risk flags. |
| Prefetch / execution pivots | Partial+ | `.pf` inventory with executable hints, SCCA header detection, version-specific common-layout metadata for versions 17/23/26/30/31, header executable-name extraction, best-effort run count, last run timestamps, run-count/last-run validation checks, bounded referenced path pivots, volume/file-reference candidates, and separate validation-required `prefetch-reference` rows for review/search. Rows explicitly expose `prefetch_validation_matrix`, `prefetch_report_grade_assessment`, native-capability disclosure, and commercial blockers for full file metrics, authoritative volume tables, trace-chain/directory sections, MFT file-reference decoding, and broad known-answer validation. Validate critical findings with PECmd or another dedicated parser. Execution-related registry export and PowerShell history imports are available through `windows-execution`; Amcache, ShimCache/AppCompatCache, BAM/DAM, and native Amcache string pivots now expose `execution_validation_matrix`, `execution_report_grade_assessment`, native capability disclosure, and grouped execution summary report-grade status counts. |
| SRUM | Partial+ | SRUM CSV/JSON/JSONL/NDJSON exports can be normalized into app resource and network usage rows with bytes totals, interface/profile, energy/CPU, user, timestamp, source hashes, counter validation checks, `execution_validation_matrix`, and `execution_report_grade_assessment`; direct `SRUDB.dat` is inventoried with ESE header metadata, native SRUDB validation metadata, hashes, bounded strings, path/URL pivots, suspicious-term flags, separate `srum-database-pivot` rows, `srum-table-candidate` rows with marker offsets/counts, and bounded `srum-row-candidate` rows for native string clusters with app/user/timestamp/counter candidates. Native SRUDB rows expose native capability disclosure and remain validation-required with explicit commercial-readiness blockers; full native SRUM table timestamp/counter decoding still requires a dedicated parser. |
| Windows Search EDB | Partial+ | `Windows.edb` is inventoried with ESE header metadata, hashes, bounded strings, native validation fields, path/URL/content pivots, export-tool guidance, explicit commercial-readiness blockers, and separate `windows-search-edb-pivot` plus `windows-search-edb-table-candidate` rows for likely gather/property/content/deleted-state table families. CSV/JSON/JSONL exports are normalized into searchable index entries with path, title, URL, extension, timestamps, and content snippets. Full ESE catalog/table/page decoding, row-level timestamps, and deleted/index-state validation still require a dedicated Windows Search parser. |
| Windows remote access | Implemented baseline+ | RDP files, Terminal Server Client cache files, and exported Terminal Server Client registry destinations are normalized for remote-access triage; cache files include bounded PNG/JPEG/BMP/DIB thumbnail signature pivots while full vendor-specific cache decoding remains planned. |
| Jump Lists/LNK | Partial+ | LNK header/string parsing and embedded path extraction implemented; Jump Lists are inventoried with OLE/custom hints, recover OLE/CFB stream listings, promote embedded Shell Link destination records with stream provenance when present, and expose DestList header/entry metadata candidates including stream-link candidates, timestamps, host/account-adjacent fields, validation checks, and explicit commercial blockers. Full OS-version-specific DestList field testimony, deleted entry recovery, and account attribution remain planned. |
| USN Journal | Partial+ | USN CSV/JSON/JSONL exports are normalized with FRN/parent/reason/timestamp fields; native `$J`/USN journal files are inventoried and recoverable v2/v3 records are emitted as triage `usn-record` rows with cursor/next-cursor offsets, bounded scan metadata, large-record sizing, layout/name/timestamp validation, reason/source/file-attribute flags, delete/rename hints, parser confidence, and validation warnings. Full replay/correlation and report-grade large-corpus pagination validation remain planned. |
| macOS user/browser/quarantine/TCC | Implemented baseline+ | User home inventory, Safari/Chromium/Firefox history imports with web/AI usage pivots, LaunchServices quarantine events, TCC privacy permission rows with high-value permission risk flags, LaunchAgent plist inventory, and bounded native pivots for Unified Logs, Spotlight stores, FSEvents files, and APFS snapshot hints are available through `macos-system`. |
| Linux system artifacts | Implemented baseline+ | User inventory, shell history, SSH authorized keys/known hosts, auth log events, auditd events, dpkg package/status events, Docker container configs, cron entries, and systemd service units are normalized through `linux-system` with source hashes and suspicious command/path risk flags. |
| A/B file compare | Implemented baseline | `compare` compares two individual files with MD5/SHA1/SHA256, field differences, bounded UTF-8 text diff previews, case bookmark support, and case-report review pivots. |
| Volume Shadow Copy compare/extract | Implemented baseline+ | `vsc-compare` compares current vs one or more snapshot folders and emits deleted/added/modified candidates with optional SHA256 confirmation; `vsc-extract` copies selected deleted/modified snapshot-side files into an evidence package with source/destination SHA256 values. VSC deletion command hints can also be surfaced from EVTX/PowerShell history imports. |
| Deleted-file carving | Implemented bounded baseline | `carve` scans files or mounted/exported folders for JPEG, PNG, PDF, and ZIP signatures with byte offsets, SHA256, caps, status, and optional extraction under analyst control. |
| Linux XFS | Direct-extract when tools support it | Image partition selection recognizes Linux/ext/XFS partitions before Sleuth Kit recovery; actual file recovery depends on the installed Sleuth Kit build's filesystem support. |
| Forensic containers AD1/L01/Lx01/AFF/AFF4/AFF4-L | Detection + export-first audit guidance | UI/API adapter detection records source integrity when bounded, limitation warnings, export-first actions, and `commercial_grade_ready=false`; direct parsing requires vendor/tool export first. |
| Raw/split images DD/RAW/IMG/001 | Direct-extract when tools exist | UI/API adapter detection plus `mmls`/`tsk_recover` recovery into the run output folder; split numeric segments are discovered, gap/start warnings are recorded, source-part integrity is captured when bounded, partition metadata/selected offsets and command history are emitted, and `commercial_grade_ready=false` remains until large known-answer validation exists. |
| Optical/archive images ISO/DMG/WIM/SWM | Direct-extract when tools exist | UI/API adapter detection plus `7zz`/`7z` extraction, with `bsdtar` fallback for ISO. |
| Virtual disks VHD/VHDX/VMDK/VDI/QCOW/QCOW2 | Direct-convert/extract when tools exist | UI/API adapter detection plus `qemu-img` conversion to raw and Sleuth Kit recovery; output records tool preflight, source/converted integrity, command history, nested raw recovery metadata, warnings, and `commercial_grade_ready=false`. XVA remains vendor/export-first with source integrity and export guidance. |
| Memory dumps MEM/DMP/VMEM/VMSS/VMSN/HPAK | Detection + import+ | Memory dump detection exists; Volatility/Volatility3 JSON/JSONL outputs can be imported as process, network, cmdline, and malfind artifacts. Direct `.mem`, `.raw`, `.vmem`, `.vmss`, `.vmsn`, `.hpak`, `.dmp`, and memory-named `.bin` dumps receive bounded indicator scans for redacted BitLocker recovery-key candidates, suspicious strings, URLs, and IPs. |
| APK malware triage | Partial+ | Exported `.apk` files are inventoried with hashes, manifest metadata, permissions, components, dex/native-library counts, entry hashes, native architectures, bounded suspicious string pivots, URL/IP indicators, validation checks, Android validation matrix, native-capability disclosure, commercial blockers, and risk flags. Android app data export files under common `Android/data`, `Android/media`, and `data/data` paths are inventoried by inferred package/category without extracting secrets and now map #29/#30 report-grade gaps explicitly. |
| Memory forensics | Partial+ | Volatility-style output normalization is implemented; direct RAM dump scans now produce bounded BitLocker/string/network pivots, while full process reconstruction, key validation, and process-risk visualization remain long-term work. |
| Indicator and IOC summary | Implemented baseline | Completed run outputs can be summarized into URL, domain, IP, and hash pivots with source pointers, optional local `--rules` IOC matching, and #63 offline JSON/CSV/TXT TI feed enrichment with feed provenance; external TI API enrichment remains disabled/outside core. |
| AI prompt artifacts | Partial+ | Browser history detects common AI service visits such as ChatGPT, Claude, Gemini, Perplexity, Copilot, Poe, Hugging Face Chat, Grok, You.com, Phind, Mistral, DeepSeek, Meta AI, Character.AI, and Notion AI. Browser Local Storage, Session Storage, IndexedDB, and Cache files are scanned for role/content, prompt, question, answer, response, and completion snippets as review-only conversation candidates. Recovered question/answer snippets are paired into transcript pivots with completeness scoring, orphan counts, source hashes, source storage provenance, offset candidates, pairing confidence summaries, validation checks, privacy warnings, and `commercial_grade_ready=false`; full service-side transcript completeness still requires service-specific validation. |
| Cloud export import | Partial | Google Takeout-style location/activity JSON and Apple/general account JSON exports are normalized with source hashes. |
| Mobile/cloud acquisition | Partial+ | Exported APK and cloud export imports exist. Cloud API collection now adds #41 credential-security assessment, redacted request credential metadata, environment-variable-only token handling, and secure-vault/scope-capture blockers. Cellebrite/XRY/GrayKey/AXIOM-style CSV/JSON exports normalize messages, contacts, calls, apps, files, accounts, media, browser rows, source IDs, hashes, validation matrix, native-capability disclosure, #26 report-grade gaps, and validation blockers. KakaoTalk, WhatsApp, Telegram, Signal, WeChat, LINE, Discord, and Instagram authorized export rows are service-labeled with chat/message IDs, participants, media references, reactions, text hashes, service-specific #31~#35 gap mapping, native-capability disclosure, and validation checks; app SQLite/DB candidates are inventory-only with table/column/count summaries and no decryption. Authorized iOS backups now surface `Manifest.db`, `Info.plist`, `Status.plist`, and keychain inventory candidates with #27/#28 gap mapping, without decrypting or exposing secrets. Direct cloud/device acquisition remains deferred. |
| Mobile correlation/schema management | Partial+ | Mobile export sources emit contact/call/message/media/app normalized rows plus `mobile-correlation-summary` rows with counts, services, participants, message-media candidate links, contact/call/SMS actor view, app/schema version registry, #43~#45 report-grade blockers, and correlation-readiness checks. This is source-export scoped and not yet a full device-wide timeline/entity-resolution system. |
| Email/mailbox | Partial+ | EML/MBOX parsing emits headers, message IDs, body previews/hashes, attachment metadata/hashes, source hashes, legal warnings, #36 gap mapping, validation matrix, native-capability disclosure, and validation blockers. PST/OST/MSG support is bounded string/email/subject candidate inventory only; native mailbox folder/message/deleted-item decoding requires dedicated tooling. |
| Cloud exports | Partial+ | Google Takeout location/activity/account rows, Gmail/Drive-style mail/file JSON rows, Apple/iCloud account/file rows, and Microsoft 365/OneDrive/Teams/Audit JSON rows normalize timestamps, actors, accounts, URLs, files, Teams chats, audit events, hashes, risk flags, validation matrix, native-capability disclosure, and #37/#38/#39 gap mapping. `cloud-collect` provides authorized bounded API collection with redacted credentials, response hashes, dry-run, #40 validation metadata, and audit output; provider-native acquisition remains planned. |

Every new parser should publish:

- Stable parser ID and version.
- Input evidence type.
- Normalized output model.
- Fixture path and expected output.
- Known limitations.
