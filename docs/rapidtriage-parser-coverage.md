# RapidTriage Parser Coverage Matrix

| Area | Status | Notes |
| --- | --- | --- |
| File metadata | Implemented | Names, paths, extensions, sizes, modified time, categories. |
| KAPE-style collect planning/export | Implemented baseline | `collect-plan` profiles preview present/missing Windows/macOS collection targets before heavy scanning or copying. `collect-export` can build a bounded source-relative export package with SHA256 copy logs. |
| Document text | Implemented | Text/log/config/data, EML/MBOX email, bounded Outlook MSG/PST/OST strings, PDF, Office OpenXML, and OpenDocument where dependencies support extraction. OOXML/ODF files now also emit `document-metadata-risk` rows with author/timestamp candidates, `metadata_profile`, `macro_profile`, and external relationship candidates so macro/template-link leakage pivots are visible in GUI/search; legacy OLE and macro behavior remain validation-gated. Sticky Notes `plum.sqlite` now emits live note rows with `sticky_note_schema_profile` and `sticky_note_review_profile`, plus bounded `sticky-note-recovery-candidate` string fragments that are not present in live note rows; recovered fragments remain validation-gated until deleted-row fixtures/trusted diffs exist. |
| Browser history/downloads | Implemented baseline+ | Chrome, Edge, Brave, Firefox local profiles; history/download fixture coverage; internet usage summaries, top-domain/category pivots, source hashes, source-profile metadata, typed-count/query/transition/duration candidates where available, #20 `core_accuracy_gates`, and bounded unified visit/download timeline rows are normalized for Windows and macOS with browser validation matrix/report-grade assessment metadata. Rows now include a browser timeline integrity profile and `browser_analyst_review_profile` that record sort order, source-index completeness, visit/download counts, missing timestamp counts, storage/sensitive-store review priority, correlation targets, and reportability warnings. #46 now adds row-level SQLite source table/row-ID preservation plus `browser-history-download-citation-manifest-v1` with source-viewer locators, row hashes, timeline row hashes, source SHA-256, and review workflow hints so GUI/report flows can jump from a browser finding back to the original SQLite row before citation. #20 trusted-diff helpers accept nested generated `unified_timeline` rows and compare visit/download type, timestamp, URL, title/domain, transition, counts, download target/bytes/state, AI label, and source table/index against BrowserHistoryView/Hindsight/native-query style exports. Safari history is macOS-only, and macOS Safari download chronology now correlates LaunchServices `QuarantineEventsV2` rows into the unified timeline with quarantine source table/row/source-hash provenance; Safari cache/session/deleted-state parity remains validation-gated. |
| Browser cache/session/extension/sync inventory | Partial++ | Chromium-family profiles now emit bounded browser storage inventory rows for cache, session storage, sessions, extensions, extension state, sync data, Web Data, cookie stores, credential stores, Local Storage, and IndexedDB with counts, byte totals, sample hashes, privacy/legal warnings, strict password/cookie/session legal warnings, #19/#42 `core_accuracy_gates`, #42 secret-handling assessment, #42 `browser_secret_authority_profile`, #42 `secret_handling_commercial_uplift_evidence`, validation checks, native-capability disclosure, report-grade assessment, a storage review profile for review priority/sample-hash/truncation controls, `browser_analyst_review_profile`, and `commercial_grade_ready=false`. Each storage row now carries a bounded `browser-storage-source-context-profile-v1` with redacted source-viewer guidance, context hashes, extension manifest candidates, cache HTTP/content-type/URL signatures, SQLite header/schema inventories, and session/LevelDB structured key hints so GUI/report review can open the right source without exposing cookie/password/session values. WebCacheV01.dat rows now add ESE header metadata, bounded URL/domain/path/string pivots, `ese_page_map` page-local marker candidates, and `webcachev01_review_profile` so WinINet/WebView evidence can be reviewed from source page context; full WebCache ESE row/container/deleted-state decoding remains blocked. OneDrive/Google Drive desktop sync DBs now emit `desktop-cloud-sync-db` schema inventory plus bounded `desktop-cloud-sync-row-candidate` rows for file/sync/delete/share/account fields with `cloud_sync_row_review_profile`; provider-specific row semantics and upload/delete/share conclusions still require validation. #47 adds `browser-storage-citation-manifest-v1` with storage row hashes, source context profile hashes, source file locators, sample file SHA-256 citations, sensitive-store counts, metadata-collapsed review hints, and redaction-by-default proof. #19 trusted-diff helpers accept nested generated `storage_inventory` rows and compare storage type/name, relative path, artifact hint, file count, byte total, sensitivity, file/dir status, truncation, and sample hashes against Hindsight/Velociraptor/native-query style references. #42 trusted-diff helpers now accept generated inventory rows too, expanding sensitive nested stores and comparing legal-authority/audit IDs against native-store, DPAPI/keychain, legal-authority, or audit-log references. Secret values are not decrypted or extracted. |
| Recent files | Implemented baseline+ | Recent shortcuts parse Shell Link header/string fields for target path, working directory, arguments, timestamps, flags, hashes, tracker candidates, PropertyStoreDataBlock review-only candidates, #17 `core_accuracy_gates`, `lnk_target_context_citation_profile` source-viewer/field-offset evidence, and embedded path pivots; Jump Lists traverse recoverable OLE/CFB streams, promote Shell Link destination records with stream provenance, parse bounded DestList header/entry metadata candidates with validation checks, layout-selection profiles, selected fixed-header size, stream-link evidence, and stable entry citation hashes, preserve unlinked DestList entries as deleted/unlinked review-only candidates, add `jumplist_evidence`, `jumplist_analyst_review_profile`, and #14 `core_accuracy_gates` for container/DestList/destination review, and fall back to embedded path extraction. |
| Timeline merge | Implemented baseline | Files, docs, artifacts, and normalized timeline export. |
| Case DB FTS | Implemented baseline+ | Documents and artifact metadata are indexed with SQLite FTS5; file metadata, timeline rows, review status filters, verification filters, promoted artifact metadata filters, #51 reviewer assignment/status gates, structured source-read citation packages on review marks, parser-native source-viewer locators/row citation hashes/parser manifest hashes for imported cloud, email, and AI artifact rows, #60 duplicate-hit analysis gates, #61 advanced search metadata on completed-run search, #62 keyword-pack search inputs, #64 report citation index exports with source-viewer package hashes/handoff metadata, and #65 versioned evidence-selection history are supported. |
| OCR | Partial+ | OCR queue manifests (#58) now track image candidates, retry state, sidecar hashes/text hashes, metadata sidecars, Korean/English language hints, translation sidecars, quality metrics, item-level and queue-level `core_accuracy_gates`, and report-grade blockers. Native OCR execution remains external and must be validated before report reliance. |
| Image/media triage | Partial+ | Image gallery review metadata (#56), audio/video sidecar transcript review (#57), Korean OCR/translation workflow metadata (#59), dimensions, file hashes, NumPy-aggregate-free perceptual average hash, similarity bucket, bounded inline thumbnail previews, rule-based visual classification hints, source-viewer `core_accuracy_gates`, and OCR/translation validation metadata are implemented for common image/media files. Audio rows are now surfaced as GUI-visible partial capability rows with `media-audio`, `waveform_preview`, `transcript_sidecars`, and cue citation manifest search terms instead of being hidden as inventory-only. Image rows also include `exif_gps_profile` and `exif_map_review_profile` for EXIF GPS latitude/longitude/altitude/datetime map pivots, `steganography_suspicion_profile` for bounded trailing-data/embedded-payload-signature/entropy triage, and `media_authenticity_profile` for AI-generation/editing metadata hints while keeping deepfake detection as `not-run` until validated model/corpus evidence exists. EXIF GPS remains metadata triage until EXIFTool/native-tool diff, device clock/timezone validation, and tamper review are attached. |
| Windows OS/account summary | Partial+ | User profile inventory plus `.reg` export hints for computer name, timezone, ProfileList SIDs, admin-group hints, current control set, last boot/shutdown timestamps, exported account lifecycle fields such as created, last logon, password-last-set, disabled/admin hints, UAC flags, SAM F/V binary presence/byte counts, first-pass SAM F timestamp/RID/UAC candidates, SAM V UTF-16 string candidates, SAM V offset/length layout string candidates for user name/full name/comment/home/profile paths, account-centered security context linking group SID candidates to inherited privilege assignments, and `forensic_review` summaries for #6 review. Account, group, privilege, LSA secret-metadata, and native SAM candidate rows now emit `sam_security_system_row_manifest` with stable row identity hashes, source hashes, required trusted-diff fields, validation summaries, redaction policy, and reportability blockers; `build_os_account_trusted_diff` blocks commercial evidence when required account/group/privilege/secret fields are missing from RapidTriage or trusted rows. Native SAM hive candidates expose bounded account-name/RID and group/alias key rows with offsets, hashes, last-write hints, validation matrices, native capability disclosure, guidance, explicit blockers, `core_accuracy_gates`, and `account_privilege_deep_parse_profile` metadata covering SAM F/V, SAM alias membership, SECURITY secret metadata, privilege mapping, legal handling, and required independent checks; full OS-version validated SAM F/V decoding, SECURITY secret decryption, and native binary group membership reconstruction remain planned. |
| #1-#120 accuracy profiles | Implemented validation layer | `rapidtriage validation` now emits `core_forensics_accuracy_profiles` for every item from native EVTX through dependency vulnerability monitoring. Each profile defines the parser/legal/operations surface, required known-answer corpus, independent oracle, required checks, minimum evidence, source/hash/offset/legal/resource controls, pass/fail rules, and default `validation-required` reportability. This improves validation discipline and accuracy review, but does not itself make the parsers commercial-grade without attached corpus/cross-tool/large-case/independent/legal/signing/operations evidence. |
| Windows Event Logs | Partial+ | XML/JSON/JSONL/CSV exports from EVTX-oriented tools parse into normalized event/detection rows with hashes, categories, event/channel families, typed parameters, imported rendered messages, parser confidence, risk flags, reportability status, expanded built-in detections for high-value Windows/Sysmon/Defender/WMI/RDP behaviors plus USB/PnP, WLAN AutoConfig, PrintService, and BITS Client provider pivots. These provider pivots expose normalized `device_instance_id`, `ssid`/`interface_guid`, `document_name`/`printer_name`, and BITS `job_id`/`remote_name`/`local_file` fields, provider-aware message templates, and analyst-facing `event_semantics_profile` correlation targets for USBSTOR/MountedDevices/SetupAPI, WLAN profiles/NetworkList, spooler SPL/SHD, and qmgr stores. Event rows also include severity/questions/pivots/source values/validation requirements, high-risk summary pivots, and EventRecordID gap hints; binary EVTX is inventoried and recoverable native record headers emit partial rows with timestamps, record IDs, file-header/chunk context, extracted strings, parameter candidates, native message previews, explicit BinXML decode status, first-pass BinXML token scans for fragment headers/elements/attributes/value text/template headers, inline String/ANSI/integer/bool/float/GUID/SID/FILETIME/SYSTIME/binary scalar decoding, bounded nested `BinXmlType` payload profiles with hashes/previews/field maps/token counts, TemplateInstance IDs, TemplateInstance value spec/value decoding for common scalar types, substitution value fields, promoted `Event/System`, duplicate-preserving ordered `EventData` sequences, grouped EventData values by name, `UserData` fields, normalized EventID/provider/channel/computer/level/timestamp/process/thread/user-SID/command pivots, `event_message`/`message_rendering` provenance/confidence/limitations, bounded case-local auto-discovery for provider manifest/message catalogs, optional curated JSON provider message catalogs, Windows Event Manifest XML (`.man`/`.xml`) catalog string-table resolution with `%1`/`%2` template insertion mapping, manifest `valueMap`/`bitMap` display mapping with raw-value provenance, high-value fallback message templates, rendered previews where possible, `eventlog-chunk` structure rows, `evtx_validation_checks`, `evtx_validation_matrix`, #1~#3 `core_accuracy_gates`, `evtx_report_grade_assessment`, chunk-boundary status counts, native capability disclosure, `evtx_recovery_context`, `evtx_recovery_validation_profile`, slack/deleted/corrupt candidate labels, separate `eventlog-record-candidate` rows for invalid record headers, recovery citation manifests for parseable slack/deleted rows and corrupt candidates, nested recovery-evidence-aware corpus diff normalization, and suspicious-term flags. Full provider DLL/resource-table message extraction and broad deleted-record corpus validation remain planned. |
| Registry hives | Partial+ | `.reg` exports parse into key/value rows including Run keys, suspicious persistence value hints, and USBSTOR device metadata. Native hive candidates such as `NTUSER.DAT`, `UsrClass.dat`, `SYSTEM`, and `SOFTWARE` are inventoried with `regf` header metadata, source hashes, sequence/dirty hints, last-write timestamp, bounded UTF-16 string pivots, hbin-aware bounded `nk`/`vk` cell rows, best-effort `registry-key-tree-node` rows from native parent/subkey/value-list metadata with linked/missing subkey/value offsets, root-cell reachability, parent/child backlink consistency, validation flags, `registry_key_tree_relationships`, `registry_validation_matrix`, #4 `core_accuracy_gates`, `registry_report_grade_assessment`, `registry_report_citation_manifest_hash`, analyst-facing `registry_analyst_review_profile`, and native capability disclosure. Registry key-tree trusted-diff normalization now accepts nested generated manifest rows and compares cell offset, parent cell offset, subkey names, value names, linked child/value counts, last-write time, root reachability, and backlink consistency against RECmd/Registry Explorer-style exports. Separate `registry-deleted-cell-candidate` rows for positive-size free/deleted key/value candidates, key-specific `registry-key-recovery-candidate` rows, and value-specific `registry-value-recovery-candidate` rows include cautious parent-key/data previews, parent-key confidence, #5 `core_accuracy_gates`, `registry_recovery_evidence`, `registry_recovery_validation_profile`, `registry_recovery_identity_profile`, allocator/recovery identity hashes, `registry_report_citation_manifest_hash`, and `registry_analyst_review_profile` fields that keep deleted/free cells non-reportable until second-parser, allocator, transaction-log, and known-answer checks are complete. Deleted-cell trusted-diff normalization now accepts nested generated rows and requires oracle coverage for cell signature, size, hbin offset, allocation status, name, and related value/parent/hash fields before `commercial_grade_evidence` can become true. Adjacent `LOG1`/`LOG2` files are hashed, header-inspected for recognized transaction-log signatures such as `HvLE`/`HvLG`, and summarized as replay input readiness; actual transaction-log replay is still blocked pending validated replay implementation and trusted parser diffs. User-hive activity pivots now emit `registry-user-activity` rows for UserAssist, TypedURLs/TypedPaths, RecentDocs, Run/RunOnce, Explorer/MRU, ShellBags, MountPoints2, Network, and ComDlg32/OpenSavePidlMRU keys from `.reg` exports and bounded native NTUSER/UsrClass string scans; user-activity review profiles expose severity, pivots, populated decoded values, correlation targets, and validation requirements for GUI/review workflows. Binary value decoding, transaction-log replay, and deleted-value testimony still require validation with a dedicated hive parser. |
| ShellBags | Partial+ | `.reg` ShellBags keys are normalized as `shellbag-key` rows. Native NTUSER/UsrClass hives now emit validation-required `shellbag-native-candidate` rows from bounded key-tree and string pivots with source key paths, bag/node ID candidates, timestamp candidates, adjacent LOG1/LOG2 transaction-log context, parser confidence, validation checks, #15 `core_accuracy_gates`, `shellbag_evidence`, `shellbag_relationship_profile` with stable key-lineage hashes, `shellbag_depth_manifest`, `shellbag_analyst_review_profile`, shared `forensic_review`, and `commercial_grade_ready=false`; full binary shell-item folder decoding, dedicated-parser relationship validation, transaction-log replay, and deleted/slack testimony remain planned. |
| MFT | Partial+ | MFT CSV/JSON/JSONL exports are normalized with record number, path, timestamps, and deleted hints; native `$MFT` files are inventoried with hashes, bounded FILE record-header samples, UTF-16 path pivots, update-sequence-array validation counts, USA sector-trailer restoration status, timestamp validation counts, and attribute type counts. Separate native `mft-record` rows now expose FILE header fields, common attributes decoded after USA fixup application, parent-reference metadata, resident data hashes, bounded nonresident `$DATA` runlist preview decoding with sparse-run and LCN-delta evidence, `mft_record_evidence`, `ntfs_analyst_review_profile`, shared `forensic_review`, validation checks, #12 `core_accuracy_gates`, parser confidence, and explicit commercial-readiness blockers. Full attribute-list extension resolution, report-grade physical-cluster/data-run validation, full parent path cache reconstruction, and broad corpus validation still require a dedicated parser. |
| `$LogFile` | Partial+ | Native `$LogFile` files are exposed as `ntfs-logfile-transaction-candidate` rows with source hashes, bounded scan bytes, `RSTR`/`RCRD`/`CHKD` signature counts/offsets, `ntfs_logfile_page_profile` page candidates, path-centered `transaction_operation_hints` for create/rename/delete/write/security context strings, and `timeline_join_hints` for MFT/USN/Recycle Bin/LNK/ShellBags correlation. These fields are GUI/search-visible triage aids only; full redo/undo record decoding, transaction sequence validation, MFT/USN replay correlation, and trusted LogFileParser/MFTECmd diffs remain required before report-grade conclusions. |
| ADS / Zone.Identifier | Partial+ | Exported `:Zone.Identifier`/`.Zone.Identifier` sidecar files parse ZoneId, referrer URL, and host URL. `windows-filesystem` now also emits `ads-stream-candidate` rows for arbitrary `name:stream` and `name:stream:$DATA` exports with host-file correlation, stream suffix/family, bounded preview, source locator, embedded signature detection, script/executable risk flags, `ads_review_profile`, and explicit blockers for native NTFS ADS enumeration, MFT/USN correlation, and trusted extraction-log diff. |
| USB/Wi-Fi system pivots | Partial+ | SetupAPI USB install rows now include `usb_device_review_profile` with parsed USB family, USBSTOR vendor/product/revision, serial-number candidate/hash, first/last install-context timestamp hints, storage-device count, and correlation targets for USBSTOR/Enum USB/MountedDevices/MFT/USN. WLAN profile XML rows now include `wifi_profile_review_profile` with interface GUID, security level, hidden-network/auto-connect/MAC-randomization flags, and redacted credential-material presence/length. EventLog rows now add USB/PnP and WLAN AutoConfig provider pivots so configuration evidence can be cross-checked against observed provider activity; these remain triage-grade until registry/filesystem/trusted-parser validation confirms actual use and connection times. |
| Windows Timeline and notifications | Partial+ | `ActivitiesCache.db`, `wpndatabase.db`, and `notifications.db` are opened read-only when SQLite headers are present. Database rows still include schema/row-count inventories and normalized timeline samples, and those samples are now promoted into `activities-cache-row-candidate` and `notification-row-candidate` rows with normalized time, decoded text hint, app hint, profile attribution, UWP package correlation, row hash, SQLite source locator, review profile, and validation blockers. These rows are GUI/search/timeline-visible triage candidates; Windows build-specific table semantics, deleted-row recovery, and trusted parser diffs remain required before report-grade user-activity claims. |
| Defender/Firewall/WER/Task Scheduler/WMI | Partial+ with explicit commercial gate | Fixture-backed inventory for Defender MPLog, Defender policy/export tamper pivots, Windows Firewall W3C logs, WER reports, Task Scheduler XML, and WMI repository files; Task Scheduler rows now emit normalized action/trigger/principal metadata, executable names, command lines, temporal provenance profile, timestamp/source hashes, validation checks, #18 `core_accuracy_gates`, validation matrix, native-capability disclosure, shared `forensic_review`, `system_analyst_review_profile`, `system_source_citation_profile`, and commercial-readiness blockers for missing TaskCache/security-descriptor/history correlation. Defender MPLog rows highlight threat/remediation/exclusion terms, while `defender-policy-artifact` rows normalize registry/MpPreference-style `defender_policy_profile` entries for exclusions, disabled protection controls, TamperProtection values, risk flags, hashes, and explicit EVTX 5007/registry-transaction/admin attribution blockers. Defender, Firewall, WER, and WMI rows also expose `system_report_grade_assessment`, #18 `forensic_review`, #18 `core_accuracy_gates`, source citation profiles with line references or WMI binary string offsets, `system_analyst_review_profile`, and native-capability disclosure. WER rows normalize application/module paths, exception codes, report IDs, FILETIME/ISO event times, problem signatures, source hashes, validation checks, and blockers for missing dump/cab/ReportQueue validation. WMI repository files include bounded string pivots for permanent event consumer/filter terms, commands, paths, URLs, byte-offset references, and risk flags. BITS `qmgr` artifacts now expose bounded string pivots plus SQLite-like `bits-qmgr-sqlite-job-candidate` rows with table/rowid source locators, URL/path/owner/state/job candidates, and row hashes; Print Spooler rows now expose `print_spooler_companion_profile` to show SHD/SPL pair completeness. #18 trusted-diff helpers now accept nested generated rows and compare family-specific task, Defender, Firewall, WER, and WMI fields against Velociraptor/Chainsaw/Hayabusa/native-export style references. |
| Prefetch / execution pivots | Partial+ | `.pf` inventory with executable hints, SCCA header detection, MAM compressed-Prefetch detection/disclosure, version-specific common-layout metadata for versions 17/23/26/30/31, header executable-name extraction, `prefetch_section_bounds_profile` hashes for declared file-metrics/trace-chain/filename-string/volume locators, best-effort run count, last run timestamps, validation checks, bounded referenced path pivots, volume/file-reference candidates, separate validation-required `prefetch-reference` rows, #16 `core_accuracy_gates`, shared `forensic_review` summaries, and `prefetch_analyst_review_profile` for #16. Execution-related registry export and PowerShell history imports are available through `windows-execution`; Amcache, ShimCache/AppCompatCache, BAM/DAM, SRUM imports, and native SRUDB candidates now expose validation matrices, report-grade assessments, evidence summaries, native capability disclosure, grouped summary counts, shared `forensic_review` summaries, item-specific `core_accuracy_gates`, analyst-facing `execution_analyst_review_profile`, and artifact-specific profiles (`amcache_schema_profile`, `amcache_schema_version_profile`, `shimcache_execution_caveat_profile`, `shimcache_layout_profile`, `bam_dam_decode_profile`, `srum_ese_validation_profile`) for #7~#10. Native Amcache.hve scans also emit bounded nearby-string row clusters with source offsets, SHA1 candidates, timestamp candidates, metadata candidates, stable `amcache_row_manifest` hashes, row-cluster provenance gates, and explicit warning that this is still not decoded InventoryApplicationFile row semantics; Amcache trusted diffs now fail commercial evidence when required path/SHA1/caveat coverage is missing. Native SYSTEM hive ShimCache/AppCompatCache scans now emit bounded path clusters with source offsets, cache order, nearby timestamp candidates, `shimcache_row_manifest` hashes, source-viewer locators, required trusted-diff path/cache-order-or-source-offset/caveat field coverage, and mandatory “not proof of execution” caveats; OS-build-specific binary layout decoding is still a blocker for commercial claims. Native SYSTEM hive BAM/DAM scans now emit bounded path/SID/timestamp clusters with source offsets, stable `bam_dam_row_manifest` hashes, ControlSet/source-key/source-offset locators, required trusted-diff path/SID/timestamp/source/caveat coverage, and correlation-required warnings; binary FILETIME row decoding and broad Windows build validation remain commercial blockers. |
| SRUM | Partial+ | SRUM CSV/JSON/JSONL/NDJSON exports can be normalized into app resource and network usage rows with bytes totals, interface/profile, energy/CPU, user, timestamp, source hashes, counter validation checks, `execution_validation_matrix`, `execution_report_grade_assessment`, and `execution_analyst_review_profile`; these rows now also emit `srum_report_citation_manifest` plus `srum_report_citation_manifest_hash` with source format, source key/offset, table/app identity, timestamp source, individual counter values, network profile/interface LUID, semantics warning, and required trusted-diff fields for SrumECmd/libesedb-style comparison. Direct `SRUDB.dat` is inventoried with ESE header/page-size validation metadata, native SRUDB validation metadata, hashes, bounded strings, path/URL pivots, suspicious-term flags, separate `srum-database-pivot` rows, `srum-table-candidate` rows with marker offsets/counts, and bounded `srum-row-candidate` rows for native string clusters with app/user/timestamp/counter candidates and validation-required citation manifests. Row candidates now merge nearby split strings inside a bounded window and expose cluster window, nearby offsets, nearby string count, row-cluster string samples, field-presence profile, manifest hash, and analyst review profile so reviewers can see which app/user/time/counter pieces were actually observed and what must be corroborated. Native SRUDB rows expose native capability disclosure and remain validation-required with explicit commercial-readiness blockers; full native SRUM table timestamp/counter decoding, page checksum verification, and tagged-column row decoding still require a dedicated parser. |
| Windows Search EDB | Partial+ | `Windows.edb` is inventoried with ESE header metadata, hashes, bounded strings, native validation fields, path/URL/content pivots, export-tool guidance, explicit commercial-readiness blockers, shared `forensic_review`, #11 `core_accuracy_gates`, `windows_search_analyst_review_profile`, stable `windows_edb_report_citation_manifest_hash`, `edb_semantics_warning`, and separate `windows-search-edb-pivot`, `windows-search-edb-page-candidate`, `windows-search-edb-table-candidate`, and correlated `windows-search-edb-row-candidate` rows for likely gather/property/content/deleted-state review. The page-candidate method preserves ESE page offset, page hash, page-local table markers, and page-local path/URL/content/risk strings so analysts can verify a hit at a stable page location instead of relying only on a global string list. Row candidates now prefer page-local path/URL/content correlation when available and expose `page_offset`, `page_sha256`, `page_table_marker_hits`, `field_presence_profile`, citation-manifest row identity, and analyst review profile so reviewers can see which fields were actually observed together and what must be externally validated. The trusted-diff contract compares path, URL, content, deleted-state semantics, timestamp semantics, table/page/source-format fields, and warning text against WinSearchDBAnalyzer/libesedb-style exports. The GUI visible capability now maps to the concrete `windows-search-edb-*` artifact types instead of inventory-only wording. CSV/JSON/JSONL exports are normalized into searchable index entries with path, title, URL, extension, timestamps, and content snippets. Full ESE catalog/table/row decoding, row-level timestamps, and deleted/index-state validation still require a dedicated Windows Search parser. |
| KakaoTalk Windows correlation | Triage+ | `kakaotalk-windows` inventories PC KakaoTalk application `.edb`/`.db`/`.dat` stores, DB/WAL/SHM/copy companions, hashes, SQLite accessibility, header signatures, likely roles such as chat log, chat list, profile, contact, media, login state, client state, and post-BigBang adjacent indexes, plus legacy decryption-readiness metadata such as 4096-byte page alignment and expected SQLite-header validation. It also records redacted `DeviceInfo` context and userId/account identifier candidates from registry exports/native hives, then correlates evidence across `Windows.edb`, Registry, and bounded memory dumps. It emits `kakaotalk-windows-app-database`, `kakaotalk-windows-crypto-material-candidate`, `kakaotalk-windows-user-id-candidate`, and `kakaotalk-windows-source-candidate` rows with source family, source hash, offset or ESE page hash where available, matched KakaoTalk terms, path/URL/chat-store/process classification, privacy warnings, and #31 `forensic_review`; a `kakaotalk-windows-correlation-summary` groups source families and reports whether evidence is single-source, moderate, or strong. This is not message DB decryption or report-grade chat testimony by itself; post-BigBang KakaoTalk behavior still requires known-answer validation. |
| KakaoTalk macOS database inventory/report | Triage+ | `kakaotalk-macos` scans Mac KakaoTalk sandbox and support roots, including `com.kakao.KakaoTalkMac` containers, for `.db`/`.sqlite`/`.sqlite3`/`.edb`/`.dat` candidates plus extensionless 78-character hash-named SQLCipher store files. It emits `kakaotalk-macos-database` rows with source hashes, DB size, WAL/SHM companions, SQLite header/open status, table names/counts, schema samples, message-table candidates, estimated message-row counts, parser confidence, #31 `forensic_review`, and explicit privacy/legal warnings. For encrypted stores it derives candidate DB names and SQLCipher keys from macOS `IOPlatformUUID` plus plist/user-directory UserID evidence using the public PBKDF2/SHA-derived macOS KakaoTalk workflow, probes `sqlcipher -readonly -ifexists` compatibility 3/4 without exporting raw UserID or key material, and records only SHA-256 fingerprints of matched sensitive values. User-directory SHA512-slice brute-force recovery is explicit and bounded behind `RAPIDTRIAGE_KAKAO_MAC_SHA512_BRUTE_MAX`; analysts can instead provide `RAPIDTRIAGE_KAKAO_MAC_USER_ID(S)` and source `RAPIDTRIAGE_KAKAO_MAC_UUID` for mounted evidence. `kakaotalk-macos-report` is the opt-in review/export path: it reopens plain SQLite or SQLCipher stores read-only, exports messages/rooms/media CSVs, writes report/summary/audit JSON, and renders a static no-script HTML viewer. Message text is hash/length-only by default and appears only with `--include-message-text`. Room/user context export is bounded by `--max-context-rows`; report JSON, summary JSON, CLI output, and the HTML viewer now expose `context_row_coverage` and truncation warnings instead of silently cutting large context tables. Known-answer Mac KakaoTalk corpora and version-specific schema validation remain blockers before report-grade message completeness claims. |
| KakaoTalk decrypt/key-store/report commands | Partial+ | `kakaotalk-decrypt` processes authorized Windows KakaoTalk `chatLogs_*.edb` files with externally supplied direct AES key/IV, pragma/userId, or DeviceInfo+pragma-key+userId material. It can decode `sys_uuid`, `hdd_model`, and `hdd_serial` from `NTUSER.DAT`, derive multiple research-backed pragma variants with an externally supplied pragma-key, auto-use one unambiguous redacted userId candidate, or try multiple candidate-derived keys until one validates the SQLite header. It decrypts 4096-byte AES-CBC pages through pycryptodome when installed or OpenSSL fallback, opens the DB read-only, identifies message-table candidates, counts rows, emits source/decrypted hashes, and includes bounded message previews only when explicitly requested. `scripts/kakaotalk_zip_to_report.py` accepts ZIP, extracted folder, and single evidence inputs, rejects unsafe ZIP traversal/symlink/size-ratio patterns, and writes summary, rooms/messages/media CSVs, and per-database count/status CSVs so raw recovered row counts are not confused with bounded viewer-visible rows. `kakaotalk-key-store-inspect` now parses post-BigBang `appstate.dat`/backup CBOR key stores, maps `wrapped_dek_map` entries to EDB/chatLogs files, records salt/wrapped-DEK lengths and hashes, and checks memory residency without exporting raw key bytes. The standalone algorithm reference script defaults to redaction and now emits a `kakaotalk-raw-key-disclosure-gate-v1` manifest when raw-key output is requested, requiring `RAPIDTRIAGE_KAKAO_ALLOW_RAW_KEYS=I_UNDERSTAND_RAW_KEY_DISCLOSURE` before raw key bytes can be printed. It does not yet recover the entropy-bound KEK/IKM or unwrap DEKs, so post-patch direct message DB opening remains validation-gated. |
| KakaoTalk Windows split strategy | Partial+ | #51 KakaoTalk command outputs now embed `kakaotalk-windows-split-strategy-manifest-v1` with stable hash, per-mode statuses for legacy EDB decrypt, userdir brute force, post-BigBang key-store inventory, SQLCipher probe, memory/registry/Windows.edb correlation, and authorized Windows collection. The manifest records evidence counts, source JSON pointers, large-case caps, raw-secret redaction defaults, and commercial blockers so GUI/report flows can explain which path produced each conclusion. Before/after-BigBang known-answer ZIPs, trusted extractor diffs, Windows 11 smoke evidence, and post-BigBang unwrap/deleted-store validation remain blockers. |
| Windows remote access | Implemented baseline+ | RDP files, Terminal Server Client cache files, exported Terminal Server Client registry destinations, and third-party remote-control traces for AnyDesk, TeamViewer, RustDesk, and Chrome Remote Desktop are normalized for remote-access triage; cache files include bounded PNG/JPEG/BMP/DIB thumbnail signature pivots, while third-party rows include product tags, URL/IP pivots, `remote_control_session_profile`, session candidates, remote ID candidates, file-transfer indicators, hashes, risk flags, and explicit product-specific state validation blockers. Full vendor-specific cache and session decoding remains planned. |
| Jump Lists/LNK | Partial+ | LNK header/string parsing, target timestamps, LinkInfo/StringData/ExtraData, tracker candidates, PropertyStoreDataBlock review candidates, embedded path extraction, validation checks, shared `forensic_review`, and `lnk_analyst_review_profile` for #17 are implemented; Jump Lists are inventoried with OLE/custom hints, recover OLE/CFB stream listings, promote embedded Shell Link destination records with stream provenance when present, and expose DestList header/entry metadata candidates including layout-selection profiles, stream-link candidates, stable entry citation hashes, unlinked/deleted review-only candidates, timestamps, host/account-adjacent fields, validation checks, `jumplist_analyst_review_profile`, and explicit commercial blockers. Full Shell Item/PropertyStore semantics, OS-version-specific DestList field testimony, report-grade deleted entry recovery, and account attribution remain planned. |
| USN Journal | Partial+ | USN CSV/JSON/JSONL exports are normalized with FRN/parent/reason/timestamp fields; native `$J`/USN journal files are inventoried and recoverable v2/v3 records are emitted as triage `usn-record` rows with cursor/next-cursor offsets, bounded scan metadata, resume_start_offset, cursor-window profile hashes, large-record sizing, layout/name/timestamp validation, reason/source/file-attribute flags, delete/rename hints, #13 `core_accuracy_gates`, `usn_record_evidence`, `ntfs_analyst_review_profile`, shared `forensic_review`, parser confidence, and validation warnings. Native v4 extent records are now recognized with 128-bit FRN/parent references, USN/reason/source fields, bounded extent offset/length previews, cursor evidence, and explicit no-filename-by-design validation. Full replay/correlation and report-grade large-corpus pagination validation remain planned. |
| macOS user/browser/quarantine/TCC | Implemented baseline+ | User home inventory, Safari/Chromium/Firefox history imports with web/AI usage pivots, Safari download timeline correlation from LaunchServices `QuarantineEventsV2`, direct and ZIP-contained ChatGPT/Claude/Gemini/Perplexity-style JSON/JSONL service-export conversation rows, LaunchServices quarantine events, TCC privacy permission rows with high-value permission risk flags, LaunchAgent plist inventory, and bounded native pivots for Unified Logs, Spotlight stores, FSEvents files, and APFS snapshot hints are available through `macos-system`. |
| Linux system artifacts | Implemented baseline+ | User inventory, shell history, SSH authorized keys/known hosts, auth log events, auditd events, dpkg package/status events, Docker container configs, cron entries, and systemd service units are normalized through `linux-system` with source hashes and suspicious command/path risk flags. |
| A/B/C file compare | Implemented baseline+ | `compare` compares two or more individual files with MD5/SHA1/SHA256, baseline-labeled pairwise differences, bounded UTF-8 text diff previews, summary status counts, #52 `core_accuracy_gates`, case bookmark support, and case-report review pivots. |
| Volume Shadow Copy compare/extract | Implemented baseline+ | `vsc-compare` compares current vs one or more snapshot folders and emits deleted/added/modified candidates with optional SHA256 confirmation; `vsc-extract` copies selected deleted/modified snapshot-side files into an evidence package with source/destination SHA256 values. VSC deletion command hints can also be surfaced from EVTX/PowerShell history imports. |
| Deleted-file carving | Implemented bounded baseline | `carve` scans files or mounted/exported folders for JPEG, PNG, PDF, and ZIP signatures with byte offsets, SHA256, caps, status, and optional extraction under analyst control. Evidence preflight now exposes `unallocated_carving_workflow` so GUI users see the bounded carving command, supported signatures, caps, and full-unallocated-map blockers before starting a deep job. |
| Linux XFS | Direct-extract when tools support it | Image partition selection recognizes Linux/ext/XFS partitions before Sleuth Kit recovery; actual file recovery depends on the installed Sleuth Kit build's filesystem support. |
| E01/Ex01 | Direct-extract when tools exist | UI/API adapter detection plus `ewfmount`, `mmls`, and `tsk_recover` recovery into the run output folder; source integrity, EWF split segment-set profile, gap/selected-first warnings, tool preflight, partition selection, command history, read-only safety notes, #22 `core_accuracy_gates`, `image_analyst_review_profile`, `recovery_unlock_profile`, and `commercial_grade_ready=false` metadata are emitted. The recovery/unlock profile exposes snapshot handoff, FDE indicator/required-material guidance, `fde_operator_runbook`, and bounded carving workflow before extraction. #22 trusted-diff helpers now accept generated E01 result dictionaries with nested `source_integrity`, `partition_selection`, `segment_set_profile`, and `recovered_root_manifest.files`, then compare source hash, selected sector, workflow, EWF segment count/order/contiguity, recovered file counts, and recovered file hashes against ewfverify/Sleuth Kit/vendor manifest style references. |
| Forensic containers AD1/L01/Lx01/AFF/AFF4/AFF4-L | Detection + export-first audit guidance | UI/API adapter detection records source integrity when bounded, limitation warnings, export-first actions, `container_export_profile` required-artifact checklist, optional sidecar `verified_export_manifest_profile` from `.export-manifest.json`/`.manifest.json` with manifest hash, vendor tool/version, source-hash match status, export root hash, bounded file-hash samples, #25 `core_accuracy_gates`, `image_analyst_review_profile`, and `commercial_grade_ready=false`; direct parsing requires vendor/tool export first. #25 trusted-diff helpers accept generated container result dictionaries with nested `verified_export_manifest_profile`, then compare source hash, detected format/container type, and vendor export manifest hash against vendor manifest references. |
| Raw/split images DD/RAW/IMG/001 | Direct-extract when tools exist | UI/API adapter detection plus `mmls`/`tsk_recover` recovery into the run output folder; split numeric segments are discovered, `split_set_profile` records part count, segment numbers, missing segments, selected-first status, total size, and warnings, source-part integrity is captured when bounded, partition metadata/selected offsets, command history, recovered-root file manifest with bounded SHA-256 hashes, report-grade blockers, #23 `core_accuracy_gates`, `forensic_review`, and `image_analyst_review_profile` are emitted. #23 trusted-diff helpers accept generated RAW result dictionaries with nested `source_integrity[]`, `split_set_profile`, and `recovered_root_manifest.files`, then compare source hash, selected sector, workflow, split part count/order/contiguity, recovered file counts, and recovered file hashes against Sleuth Kit/vendor manifest style references. `commercial_grade_ready=false` remains until large known-answer validation exists. |
| Optical/archive images ISO/DMG/WIM/SWM | Direct-extract when tools exist | UI/API adapter detection plus `7zz`/`7z` extraction, with `bsdtar` fallback for ISO. |
| ZIP/RAR/7z archive inventory | Partial | `generic-documents` emits `archive-file-inventory` rows for ZIP/RAR/7z files. ZIP central directory metadata is parsed without extraction to expose entry count, encrypted entries, path traversal entries, executable/script entries, nested archive entries, compression ratio, `archive_review_profile`, and sandbox/password workflow blockers; RAR/7z are metadata-only until a dedicated sandboxed parser is attached. |
| Virtual disks VHD/VHDX/VMDK/VDI/QCOW/QCOW2 | Direct-convert/extract when tools exist | UI/API adapter detection plus `qemu-img info`, `qemu-img` conversion to raw, and Sleuth Kit recovery; output records tool preflight, source/converted integrity, parsed `qemu_img_info_profile`, `virtual_disk_chain_profile`, snapshot/differencing warnings, command history, nested raw recovery metadata including recovered file manifest/hashes, warnings, #24 `core_accuracy_gates`, `image_analyst_review_profile`, and `commercial_grade_ready=false`. #24 trusted-diff helpers accept generated virtual-disk result dictionaries with nested `raw_extraction.recovered_root_manifest.files`, `converted_raw_integrity`, and chain profiles, then compare source hash, converted RAW hash, selected sector, workflow, virtual format, chain status, snapshot flag, parent-chain status, recovered file counts, and recovered file hashes against qemu-img/Sleuth Kit/vendor manifest references. XVA remains vendor/export-first with source integrity, chain/export profile, #24 `core_accuracy_gates`, `image_analyst_review_profile`, and export guidance. |
| Memory dumps MEM/DMP/VMEM/VMSS/VMSN/HPAK | Detection + import+ | Memory dump detection exists; Volatility/Volatility3 JSON/JSONL outputs can be imported as process, network, cmdline, and malfind artifacts. Direct `.mem`, `.raw`, `.vmem`, `.vmss`, `.vmsn`, `.hpak`, `.dmp`, and memory-named `.bin` dumps receive bounded indicator scans for redacted BitLocker recovery-key candidates, suspicious strings, URLs, and IPs. |
| APK malware triage | Partial+ | Exported `.apk` files are inventoried with hashes, manifest metadata, permissions, components, dex/native-library counts, entry hashes, native architectures, bounded suspicious string pivots, URL/IP indicators, `apk_analysis_profile`, `apk_signing_inventory` for META-INF signature-block/signature-file/JAR-manifest hashes, certificate/signature limitation evidence, validation checks, Android validation matrix, native-capability disclosure, #30 `core_accuracy_gates`, commercial blockers, shared `forensic_review`, `android_analyst_review_profile`, `android_apk_deep_analysis_manifest`, `android_apk_report_grade_validation_plan`, and risk flags. Android app data export files under common `Android/data`, `Android/media`, and `data/data` paths are inventoried by inferred package/category with communication/browser/media store hints, `android_app_data_profile`, read-only SQLite schema/row-count inventory, Android source layout profile, artifact-family matrix for SMS/call/contact/browser/media/app DB candidates, schema profile metadata, `android_app_data_report_grade_validation_plan`, #29/#30 `core_accuracy_gates`, `android_analyst_review_profile`, and no secret/value extraction. |
| Android parser manifest | Partial+ | #54 Android APK/app-data rows now emit `android-backup-app-data-parser-manifest-v1` with stable hash, source-viewer locator, package/source hash, APK permission/component/DEX/native/signing inventory, app-data SQLite table counts, artifact family matrix, source-layout classification, secret/schema boundary, and large-data caps. #29 app-data rows also emit `android-app-data-report-grade-validation-plan-v1` with source hashing, package/path attribution, read-only SQLite inventory, artifact-family matrix, redaction proof, source locator, acquisition-manifest attribution, trusted Android export diff, app-schema fixture, encrypted/deleted-store corpus, and independent review slots. #30 APK rows emit `android-apk-report-grade-validation-plan-v1` with source APK integrity, ZIP entry inventory, manifest/package context, permission/component normalization, DEX/native pivot boundaries, signing-entry inventory, source locator, trusted APK tool diff, binary-manifest validation, signature-chain/lineage validation, malware-behavior known-answer corpus, and independent review slots. #29/#30 gates and #54 profiles require the manifest/locator/validation plan before GUI/report citation. Native backup payload decoding, binary manifest/signature chain validation, app-specific schema fixtures, deleted-row validation, and trusted Android export/tool diffs remain blockers. |
| Memory forensics | Partial+ | Volatility-style output normalization is implemented; direct RAM dump scans now produce bounded BitLocker/string/network pivots, while full process reconstruction, key validation, and process-risk visualization remain long-term work. |
| WebShell and web server logs | Partial+ | `windows-system` emits `webshell-source-candidate` rows for ASP/ASPX/PHP/JSP-like source files with `webshell_semantic_profile`, evidence spans, optional external rule sidecars, IIS/site correlation, web-log correlation, timeline relation, and `webshell_report_citation_package`. IIS/Apache/Nginx-style logs emit `web-server-log` rows with parsed URI/status/IP/time pivots and suspicious request scoring. The GUI visible capability now maps directly to those artifact types, but court-grade attribution still requires trusted rule-pack diff, full log corpus coverage, MFT/USN timeline corroboration, and manual malware review. |
| Indicator and IOC summary | Implemented baseline+ | Completed run outputs can be summarized into URL, domain, IP, and hash pivots with source pointers, optional local `--rules` IOC matching, YARA-lite `.yar/.yara` literal string IOC import, normalized `ioc_scanner_hits` for rule/hash/domain/URL/keyword hits with source-viewer locators and scanner manifest, #63 offline JSON/CSV/TXT TI feed enrichment with feed provenance, `core_accuracy_gates`, and explicit local-only/no-external-call warnings; full native YARA grammar, signed feeds, STIX/TAXII, and external TI API enrichment remain disabled/outside core. |
| Large-case performance workflow | Partial++ | `benchmark` emits #66 100k/1M/10M scale-target metadata, p50/p95 search latency, records/sec, output size, memory metrics, and #66 `commercial_uplift_evidence`. `stress-plan` emits #67 1TB/5TB/10TB runbook scenarios and plan/scenario #67 uplift evidence. Runs emit #68 bounded input fingerprints, #70 stage checkpoints, #68/#70 uplift evidence, #71 isolated parser-crash payloads, #72 optional RSS memory-cap enforcement, #75 parallel parser scheduler assessment, and a consolidated #71-#75 `runtime_defensibility_profiles` block; web/API jobs expose #69 local background queue state, queue/step uplift evidence, and #80 cancellation/retry assessment with retry lineage, partial-output policy, and `cancellation-retry-manifest-v1` hashes. Source previews expose #73 read-only bounded sandbox metadata; SQLite/Case DB viewers expose #74 FTS/index optimization metadata; hash requests and duplicate scans expose #76 hash-cache and #77 duplicate-content manifest metadata; file scans and full run workflows now accept local MD5/SHA1/SHA256 known-good feeds including analyst-supplied NSRL RDS CSV headers (`SHA-1`, `MD5`, `FileName`, `ProductCode`, etc.), mark matches by default, preserve source row/feed traceability in `known_good_match.source_detail`, optionally suppress them with `--hide-known-good` / GUI "Hide known-good rows", preserve `known_good_suppression_profile` / `known_good_suppressed_candidates`, and surface bounded extension/magic-header mismatches via `file_signature_profile` / `signature_mismatch_candidates`; the web Files tab exposes these controls and review queues instead of leaving them as hidden JSON fields. `large-case-readiness` emits Case DB cursor diagnostics with deterministic page-window hashes, next-offset hashes, and pagination-proven table counts for #78/#79 Mac-local evidence. API pages expose #78 cursor and pagination-window manifest metadata, and web tables expose #79 bounded-DOM row-window manifest metadata. Published hardware-scale validation, bundled NSRL updater/full database management, and second-parser file-type validation remain required. |
| Report/legal validation workflow | Partial+ | Validation packages expose #81 known-answer status, #82 fixture-corpus inventory, #83 FP/FN notes, #84 independent-report attachment metadata, #85 validation-package hashes, and #95 external tool version capture. Case DB report exports expose #86 custody, #87 acquisition hashes, #88 audit hash chains, #89 reproducibility, #90 provenance, #91 parser confidence, #92 validation warnings, #93 legal limitations, #96 acquisition/write-blocker metadata, #97 timezone validation, #98 clock-skew analysis, and #99 contamination warnings. Reviewer bundles expose #94 court exhibit indexes and #100 tamper-evident generated-output hash chains. External signing, notarization, and jurisdiction-specific exhibit forms remain operator-owned. |
| AI prompt artifacts | Partial+ | Browser history detects common AI service visits such as ChatGPT, Claude, Gemini, Perplexity, Copilot, Poe, Hugging Face Chat, Grok, You.com, Phind, Mistral, DeepSeek, Meta AI, Character.AI, and Notion AI. Browser Local Storage, Session Storage, IndexedDB, and Cache files are scanned for role/content, prompt, question, answer, response, and completion snippets as review-only conversation candidates. Official/exported JSON and JSONL files, plus bounded JSON/JSONL entries inside service export ZIP archives, whose path or archive entry indicates ChatGPT/OpenAI, Claude/Anthropic, Gemini/Bard, Perplexity, Copilot, Takeout, or conversation export data now emit GUI-visible `ai-service-export-conversation` rows with `ai_service_export_parser_manifest`, source hash, source offset, JSON Pointer locator, row citation hash, text hash, service export schema profile, archive entry context where applicable, Q/A pairing, chat-like review hints, and provider-scope blockers. Local LLM stores for Ollama, LM Studio, and GPT4All now emit `local-llm-artifact` inventory rows plus bounded `local-llm-prompt-candidate` rows from text/log/config and SQLite prompt-like tables with `local_llm_review_profile`. Desktop AI app stores for ChatGPT/OpenAI, Copilot, Claude, Gemini, and Perplexity are inventoried as `desktop-ai-app-artifact` rows; SQLite candidates are opened read-only for bounded table, row-count, and message-table candidate metadata, and message-like tables now emit bounded `desktop-ai-conversation-candidate` rows with role/direction, timestamp, conversation-id hints, content hashes, and `desktop_ai_conversation_review_profile` while staying service-export-diff gated. Windows Recall/CoreAIPlatform candidates are inventoried as `windows-recall-database` and `windows-recall-snapshot-file` rows; DB candidates are opened read-only for bounded schema/row-count/semantic table-role metadata, snapshot candidates are hashed/signature-profiled, and all Recall rows keep privacy/legal warnings plus `commercial_grade_ready=false` until real Windows 11 Recall corpus, protected-store authority, and snapshot-to-DB linkage validation exist. Recovered question/answer snippets are paired into transcript pivots with completeness scoring, orphan counts, source hashes, source storage provenance, pair-level source/offset/JSON-Pointer citation, pairing confidence summaries, validation checks, #21 `core_accuracy_gates`, `ai_transcript_analyst_review_profile`, privacy warnings, and `commercial_grade_ready=false`. #48 now adds `ai-transcript-candidate-manifest-v1` with candidate text hashes/previews, source storage paths, offsets, JSON Pointers, source SHA-256s, pair citations, source-viewer locators, chat-like review workflow hints, and large-data caps so GUI/report flows can inspect each candidate before using it as evidence. #21 trusted-diff helpers accept flat service exports, nested generated `transcript_pairs`, and nested `conversation_rows` that can be re-paired before comparing service, question, answer, source hashes, source paths/offsets, storage area, pair confidence, and pair IDs against ChatGPT/Claude/Gemini/Perplexity export-style references. Full service-side transcript completeness still requires provider-specific schema validation and trusted export diffs. |
| Cloud export import | Partial+ | Google Takeout-style location/activity/account rows, Gmail/Drive-style mail/file rows, standalone CSV audit rows, bounded Gmail Takeout MBOX-in-ZIP mail rows, bounded JSON/CSV-in-ZIP cloud rows, bounded ChatGPT/Claude/Gemini/Perplexity-style service export JSON/JSONL rows including JSON/JSONL entries inside service export ZIP archives, Apple/iCloud account/file/photo rows, Microsoft/OneDrive/Teams/Audit rows, and collaboration-SaaS message/file-style rows are normalized with source hashes, provider profiles, issue matrices, validation matrices, native-capability disclosure, #37/#38/#39 `core_accuracy_gates`, #37/#38/#39 `forensic_review`, `cloud_analyst_review_profile`, and explicit provider completeness blockers. ZIP provider archives are inventoried as `cloud-export-archive` rows with original archive SHA256, bounded entry manifests, JSON/CSV entry counts, provider/product/suffix counts, `cloud_archive_manifest`, archive viewer locators, and split/scope/provider-diff blockers before any archive-scope claim; small Gmail `.mbox` entries, bounded JSON/CSV entries, and AI service export JSON/JSONL entries are read in-place without extracting the ZIP and emit archive entry name, CRC32, entry indexes, row/message indexes, Q/A pairing where applicable, and bounded content hashes on searchable rows. Gmail Takeout MBOX rows additionally emit `gmail_thread_profile`, `thread_root_id`, `thread_parent_id`, and `normalized_thread_subject` from `Message-ID`/`In-Reply-To`/`References`, and those values are carried into Google row citations and analyst review pivots for conversation review. Google rows now add `google_takeout_review_profile` for Gmail/Drive/Photos/My Activity/Location/account product-family classification, expected/present pivots, sidecar merge status, original archive hash status, timezone/deleted-retention validation status, and provider-native diff requirements. Apple/iCloud rows now add `icloud_export_review_profile` for account/iCloud Drive/iCloud Photos/iCloud Mail/device-association product-family classification, ADP status, shared-album semantics, photo sidecar/EXIF merge status, third-party container visibility, and provider-native diff requirements; they also emit `icloud_export_report_grade_validation_plan` for source hash, row citation, product pivot, source viewer, account/file/mail pivots, Photos sidecar/EXIF/album/share, export scope, ADP/container scope, provider diff, device association, schema, and independent-review slots. Microsoft 365 rows now add `m365_export_review_profile` for Exchange/Teams/OneDrive/SharePoint/audit workload classification, Purview/eDiscovery/Graph source hints, Teams compliance-record status, retention/hold policy status, deleted/version-history status, and provider-native diff requirements. Microsoft Teams message rows additionally emit `teams_message_review_profile`, `teams_attachment_review_profile`, `teams_reaction_review_profile`, `reply_to_message_id`, attachment counts/names/URL hashes, reaction counts/types/actor hashes, and edited/deleted hints as review pivots while still requiring compliance-record/provider diffs. Collaboration SaaS message rows now emit `collaboration_message_review_profile` with workspace/channel/thread/message pivots, attachment counts/names/URL hashes, reaction counts/types/actor hashes, and edited/deleted hints for Slack/Discord/Dropbox/Box/Zoom/Notion/Atlassian/GitHub-style exports while still requiring workspace-scope and provider-native diffs. OneDrive/SharePoint file rows additionally emit `m365_file_permission_review_profile` and `m365_file_state_review_profile` with permission counts, role pivots, principal/link hashes, version IDs/counts, retention labels, and deleted-state hints while still requiring provider permission graph and retention/version diffs. iCloud and M365/Teams/OneDrive are now GUI-visible partial capabilities mapped to cloud account/file/message/audit rows. #55 rows also emit `cloud-export-import-manifest-v1` with stable hash, source-viewer locator, provider strategy, product/workload family, row pivots, original source hash linkage, redaction defaults, and provider-scope/known-answer/deleted-state/native-diff blockers for GUI/report citation. |
| Mobile/cloud acquisition | Partial+ | Exported APK and cloud export imports exist. Cloud API collection now adds #40/#41 `core_accuracy_gates`, #40 acquisition review, #40 `cloud_api_collection_strategy_profile`, #41 credential-security assessment, #41 `credential_strategy_profile`, #41 `credential_authority_profile`, redacted request credential metadata, environment-variable-only token handling, controlled-reveal default denial, provider/scope/legal-authority linkage, and secure-vault/scope/rotation-audit blockers. Cloud export rows for Google Takeout/Gmail/Drive/Activity/Location, iCloud account/file/photo-style exports, and Microsoft 365/Teams/OneDrive/SharePoint/audit rows include `cloud_provider_strategy_profile` with provider product matrix, expected row pivots, source-scope blockers, and trusted provider diff requirements. Cellebrite/XRY/GrayKey/AXIOM-style CSV/JSON exports normalize messages, contacts, calls, apps, files, accounts, media, browser, location, health/fitness, and screen-time rows, source IDs, hashes, validation matrix, native-capability disclosure, #26 `core_accuracy_gates`, shared `forensic_review`, `mobile_analyst_review_profile`, source-level `mobile_export_source_profile` with input/emitted/unclassified row counts, common key samples, row-cap status, vendor schema registry profile, optional `vendor_export_manifest_profile` sidecar metadata (`.export-metadata.json`, `.manifest.json`, or `export_manifest.json`) with vendor tool/version, parser/schema version, source export hash match, original acquisition hash presence, export settings presence, and validation blockers. KakaoTalk, WhatsApp, Telegram, Signal, WeChat, LINE, Discord, Instagram, iMessage, Facebook Messenger, Viber, Skype, Slack, Microsoft Teams, Reddit, X/Twitter, TikTok, Snapchat, Matrix/Element, Wire, Threema, Session, and Wickr authorized export rows are service-labeled with chat/message IDs, participants, media references, reactions, text hashes, service-specific #31~#35 gap mapping, #31~#35 `core_accuracy_gates`, `chat_app_forensic_review`, `chat_app_scope_profile`, `chat_app_strategy_profile`, `chat_app_issue_matrix`, native-capability disclosure, and validation checks. KakaoTalk rows include `kakaotalk_compatibility_assessment`, `kakaotalk_message_review_profile` for chat-bubble review/attachment/read-deleted metadata, `kakaotalk_report_grade_validation_plan` with source export/native DB integrity, row citation, message pivot normalization, DB inventory boundary, BigBang compatibility, hash-only text policy, source-viewer locator, trusted diff, post-BigBang corpus, schema map, encrypted-store authority, deleted/read-state, attachment-byte, and independent-review evidence slots, and post-BigBang strategy tracking; 25.7.2 / 2025-08-13+ remains post-BigBang legacy-method-incompatible until independently validated. KakaoTalk SQLite candidates can expose `kakaotalk_database_review_profile` with message/media/participant table candidates while remaining inventory-only. WhatsApp rows now expose `whatsapp_message_review_profile` for JID attribution, media class, crypt-key authority, quoted/read-deleted-state metadata, msgstore/export boundaries, and `whatsapp_report_grade_validation_plan` with source export/msgstore integrity, row citation, message/JID/media normalization, msgstore DB inventory boundary, crypt/export strategy classification, hash-only text policy, source-viewer locator, trusted WhatsApp diff, crypt authority, schema-version, deleted-row, contact/call/media, timezone/ack/read-state, attachment-byte, and independent-review evidence slots; WhatsApp SQLite candidates can expose `whatsapp_database_review_profile` with message, JID/contact, media, crypt-key, and deleted-row validation blockers. Telegram rows now expose `telegram_message_review_profile` for account/dialog attribution, media/cache class, edited/deleted/secret-chat hints, local-store decryption status, and cache/export source-track boundaries; Telegram SQLite/cache candidates can expose `telegram_database_review_profile` with message, account/peer, media-cache, local-store, and secret-chat validation blockers. Signal/extended messenger rows expose service-specific tracks such as SQLCipher authority-gated inventory and export-schema/ephemeral validation. App SQLite/DB candidates are inventory-only with table/column/count summaries and no decryption. Authorized iOS backups now surface `Manifest.db`, `Info.plist`, `Status.plist`, `ios_backup_file_profile`, `ios_backup_scope_profile`, `ios_backup_root_profile` with required-file presence/hashes, device/product version, backup status, full/encrypted hints, top domains/categories, `mobile_analyst_review_profile`, and keychain file presence, plus keychain inventory candidates with `ios_keychain_scope_profile` table classes/sensitive columns/redaction policy, `ios_keychain_authority_gate`, #27/#28 `core_accuracy_gates`, controlled-reveal audit gates, shared `forensic_review`, and `mobile_analyst_review_profile`, without decrypting or exposing secrets. Direct cloud/device acquisition remains deferred. |
| Mobile vendor import manifest | Partial+ | #52 mobile vendor rows now emit `mobile-vendor-import-manifest-v1` with stable hash, source row locator, source record ID, known vendor family, schema registry expectations, sidecar manifest status, source hash linkage, row-cap controls, hash-only/redaction defaults, and explicit per-vendor fixture/trusted-diff blockers. #26 source rows also emit `mobile-vendor-export-report-grade-validation-plan-v1` with source hash, vendor sidecar, tool version/settings, original acquisition hash, schema mapper/row identity, trusted diff, per-version fixture, deleted-record corpus, and independent signoff slots plus rerunnable manifest/import/diff commands. #26 gates and #52 functional profiles use these manifests to prove row-level import provenance before GUI/report citation. Per-vendor version fixtures and trusted Cellebrite/XRY/GrayKey/AXIOM export diffs remain blockers. |
| iOS backup parser manifest | Partial+ | #53 iOS backup, plist metadata, source-summary, and keychain inventory rows now emit `ios-backup-parser-manifest-v1` with stable hash, source-viewer locator, Manifest.db fileID/domain/path citation, backup scope/root metadata, Info/Status/keychain presence, keychain table/redaction/authority state, lawful-key workflow flags, row caps, and explicit known-answer/protected-data blockers. #27 source-summary rows emit `ios-backup-report-grade-validation-plan-v1` with report-required evidence slots and commands for backup-root hashing, import regeneration, encrypted-backup authority review, and trusted iOS parser diff. #28 keychain rows emit `ios-keychain-report-grade-validation-plan-v1` with keychain DB hash, read-only inventory, redaction, table-class/sensitive-column map, source locator, controlled-reveal audit boundary, trusted keychain diff, protected-data/keybag corpus, and independent-review slots. #27/#28 gates and #53 profiles require these manifests before GUI/report citation. Encrypted-backup unlock evidence, protected-data class validation, application DB payload parsing, and trusted iOS corpus diffs remain blockers. |
| Telegram report-grade validation plan | Partial+ | Telegram rows now emit `telegram_report_grade_validation_plan` (`telegram-report-grade-validation-plan-v1`) with source export/cache/database integrity, source row citation, message/account/dialog/media normalization, Telegram DB/cache inventory boundary, export/cache strategy classification, hash-only text policy, source-viewer locator, trusted Telegram export/native DB diff, local-store decryption authority workflow, export schema-version corpus, account/dialog/peer attribution corpus, secret/edited/deleted semantics corpus, cache media locality validation, and independent-review evidence slots. Core gates, commercial uplift evidence, and the functional profile carry the plan hash plus ready/blocking slot counts so GUI/report flows can use Telegram rows as export/cache triage without claiming encrypted local-store or deleted-cache completeness. |
| Signal report-grade validation plan | Partial+ | Signal rows now emit `signal_report_grade_validation_plan` (`signal-report-grade-validation-plan-v1`) with source export/SQLCipher integrity, source row citation, thread/recipient/message/attachment normalization, Signal database/SQLCipher inventory boundary, SQLCipher strategy classification, hash-only text policy, source-viewer locator, trusted Signal export/native DB diff, SQLCipher key authority workflow, recipient/thread schema corpus, attachment locality validation, deleted/disappearing message semantics, delivery/read-state semantics, and independent-review evidence slots. Core gates, commercial uplift evidence, and the functional profile carry the plan hash plus ready/blocking slot counts so GUI/report flows can use Signal rows as authorized export/SQLCipher-inventory triage without claiming SQLCipher content, attachment byte locality, deleted/disappearing semantics, or delivery/read-state completeness. |
| Extended messenger report-grade validation plan | Partial+ | WeChat, LINE, Discord, Instagram, and broader #35 service-family rows now emit `extended_messenger_report_grade_validation_plan` (`extended-messenger-report-grade-validation-plan-v1`) with source export/native row integrity, service row citation, message/media/reaction normalization, database schema inventory boundary, service source-track classification, hash-only text policy, source-viewer locator, trusted extended messenger export/native DB diff, service schema-version corpus, encrypted/ephemeral authority workflow, media locality validation, reaction/read/edit/delete semantics, service coverage matrix, and independent-review evidence slots. Core gates, commercial uplift evidence, and the functional profile carry the plan hash plus ready/blocking slot counts so GUI/report flows can use extended messenger rows as service export/native-inventory triage without claiming per-service schema completeness, media recovery, or state semantics. |
| Email report-grade validation plan | Partial+ | EML/EMLX/MBOX/Maildir/PST/OST/MSG rows now emit `email_report_grade_validation_plan` (`email-report-grade-validation-plan-v1`) with source mailbox hash integrity, message/mailbox row citation, header/body/attachment inventory, thread/participant profile, bounded MAPI inventory boundary, source-viewer locator, hash-only body policy, attachment source locator, trusted mailbox export/native diff, native MAPI decoding or trusted export, deleted-item corpus, thread/dedup/timezone corpus, attachment-byte validation, auth/signature/crypto validation, privilege/scope review, and independent-review evidence slots. Core gates, commercial uplift evidence, and the functional profile carry the plan hash plus ready/blocking slot counts so GUI/report flows can treat mailbox rows as review pivots without claiming native PST/OST/MSG object completeness or deleted-item recovery. |
| Mobile correlation/schema management | Partial++ | Mobile export sources emit contact/call/message/media/app normalized rows plus `mobile-correlation-summary` rows with counts, services, participants, message-media candidate links, `mobile_timeline_correlation_profile`, contact/call/SMS actor view, `mobile_actor_review_profile`, `mobile_schema_compatibility_profile`, app/schema version registry, #43~#45 `core_accuracy_gates`, #43~#45 `mobile_correlation_commercial_uplift_evidence`, report-grade blockers, row caps, pass/fail validation IDs, and correlation-readiness checks. The timeline profile provides a bounded message/media/call event list, event cap/truncation state, missing timestamp count, resolved/unresolved media-link counts, and timezone/device-wide known-answer blockers for review. The actor profile provides a review queue with message/call/contact counts, phone/email/name counts, merge-split review requirements, and multi-identifier actor counts. The schema compatibility profile records app/schema versions, release-gate entries, unvalidated counts, fixture requirements, and schema migration blockers. Search-analysis workbench output also emits `analysis_analyst_review_profile` as a top-level routing card for #46~#50/#60 cluster/entity/graph/timeline/workbook/dedup review entrypoints. This is source-export/search-result scoped and not yet a full device-wide timeline/entity-resolution system. |
| Signal messenger review | Partial+ | Signal authorized export rows now expose `signal_message_review_profile` for thread/recipient attribution, attachment class, SQLCipher key authority status, disappearing/deleted-state boundaries, read/delivery hints, and chat-bubble review guidance. Signal SQLite candidates can expose `signal_database_review_profile` with message, recipient/thread, attachment table candidates, SQLCipher/deleted-row blockers, and no secret/value extraction. Report-grade claims still require lawful SQLCipher/key authority, trusted parser diff, and Signal known-answer fixtures. |
| Extended messenger review | Partial+ | WeChat, LINE, Discord, and Instagram authorized export rows now expose `extended_messenger_message_review_profile` for service-specific source track, thread/channel/room attribution, actor/recipient pivots, attachment class, reaction/read/edit/delete/ephemeral hints, and metadata-collapsed chat review guidance. Matching SQLite/cache candidates can expose `extended_messenger_database_review_profile` with message, actor/thread, attachment, and state-semantics table candidates. Native protected-store decryption, deleted recovery, and trusted parser diff remain blockers. |
| Messenger export framework | Partial+ | #50 now emits `messenger-export-framework-manifest-v1` on mobile message and chat-database records. The manifest carries #50 plus legacy #31~#35 mapping, stable manifest hash, row citation, source-viewer locator, hash-only text handling, redaction defaults, large-data caps, and bounded SQLite table citations for chat database candidates. Core gates and uplift evidence require the manifest/source locator before treating a row as framework-normalized. Per-service native schema/decryption/deleted recovery and known-answer corpora remain blockers. |
| Email/mailbox | Partial++ | EML/EMLX/MBOX/Maildir parsing emits headers, message IDs, body previews/hashes, attachment metadata/hashes, source hashes, legal warnings, #36 gap mapping, #36 `core_accuracy_gates`, format profiles, `email_mailbox_strategy_profile`, issue matrices, validation matrix, native-capability disclosure, and validation blockers. #49 now adds `email-expansion-citation-manifest-v1` with message citations, attachment citations, bounded PST/OST/MSG candidate citations, source-viewer locators, row hashes, source SHA-256, metadata-collapsed review workflow hints, and large-data caps. PST/OST/MSG support is bounded string/email/subject candidate inventory only, now with `mapi_container_review_profile` for scan-window bounds, folder/message-class/attachment/deleted-item hints, and native decode/deleted/threading limitations. `email-external-parse` now records Mac-first external parser evidence with tool availability, selected command/path, command/stdout/stderr hashes, export inventory hashes, validation checks, `email-external-parser-evidence-manifest-v1`, and `email-external-export-review-profile-v1` for exported EML/MBOX message candidates, attachment counts, folder candidates, source-viewer locators, row hashes, metadata-collapsed review tabs, and trusted-diff blockers; its JSON can be attached to `commercial-readiness --mac-first-evidence` for #36/#55/#81/#85/#90/#95 preparatory tracking. Strategy profiles still require libpff/readpst/Outlook/Purview-style trusted export diff evidence before any native mailbox, attachment, threading, or deleted-item claim. |
| Cloud exports | Partial++ | Google Takeout location/activity/account rows, Gmail/Drive-style mail/file JSON rows, Apple/iCloud account/file rows, Microsoft 365/OneDrive/Teams/Audit JSON rows, and Slack/Discord/Dropbox/Box-style collaboration SaaS rows normalize timestamps, actors, accounts, URLs, files, Teams/chats, audit events, collaboration channels/threads/attachments/reactions, hashes, risk flags, provider profile, issue matrix, validation matrix, native-capability disclosure, #37/#38/#39 `core_accuracy_gates`, and #37/#38/#39 gap mapping. `cloud-collect` provides authorized bounded API collection with redacted credentials, response hashes, dry-run, #40/#41 `core_accuracy_gates`, #40/#41 `commercial_uplift_evidence`, validation metadata, audit output, `cloud_api_provider_scope_profile`, per-request `request_acquisition_profile` with retry/backoff and pagination/delta declarations, #56 `cloud-api-acquisition-manifest-v1` with stable hash, source manifest hash, output/response directories, request locators, response hashes, provider scope profile, credential boundary, large-response caps, and #41 `credential_authority_profile` with controlled-reveal default denial, sensitive header inventory, provider/scope/legal-authority linkage, vault status, and token rotation audit blockers; provider-native OAuth/device-flow, pagination execution, legal hold, vault integration, and known-answer API validation remain planned. |
| Google Takeout report-grade validation plan | Partial++ | Google Takeout/Gmail rows emit `google_takeout_report_grade_validation_plan` with stable hash, source hash integrity, row citation, product pivot, source-viewer locator, Gmail thread/body policy, Drive/Photos sidecar, Location/Activity timezone/source, selected-products scope, original archive completeness, provider-native row diff, duplicate/thread/timezone corpus, deleted/retention/sharing semantics, schema-version tracking, independent-review evidence slots, and ready/blocking counts. The plan is a reportability gate, not a commercial-grade claim. |
| iCloud export report-grade validation plan | Partial++ | Apple/iCloud account, Drive/Photos, and Mail rows emit `icloud_export_report_grade_validation_plan` with stable hash, source hash integrity, row citation, product pivot, source-viewer locator, account identity, file/photo reference, mail hash/preview policy, Photos sidecar/EXIF/album/share validation, Apple export scope/account proof, original export completeness, ADP/shared-album/third-party-container scope, provider-native row diff, device inventory association, schema-version tracking, independent-review evidence slots, and ready/blocking counts. The plan is a reportability gate, not a commercial-grade claim. |
| M365 export report-grade validation plan | Partial++ | Microsoft 365/Teams/OneDrive/SharePoint/audit rows emit `m365_export_report_grade_validation_plan` with stable hash, source hash integrity, row citation, workload pivot, source-viewer locator, Teams thread/attachment/reaction inventory, OneDrive/SharePoint file-permission and retention/version/deleted pivots, audit-operation pivots, Purview/eDiscovery scope, original export package hash, Graph scope/pagination validation, Teams compliance and SharePoint permission reconciliation, provider-native row diff, schema-version tracking, independent-review evidence slots, and ready/blocking counts. The plan is a reportability gate, not a tenant-, permission-, or provider-complete claim. |

Every new parser should publish:

- Stable parser ID and version.
- Input evidence type.
- Normalized output model.
- Fixture path and expected output.
- Known limitations.

Validation package coverage now exposes #81 known-answer test manifests, #82 parser fixture corpus inventory, #83 parser false-positive/false-negative notes, #84 independent validation report attachment/hashing, and #85 validation-package automation metadata. The package self-assessment now records required JSON/Markdown/artifact-manifest output presence after generation, and the artifact manifest records package-file SHA256/size rows. These fields document release evidence readiness; they do not by themselves make a parser court-ready without attached test output and reviewer sign-off.
